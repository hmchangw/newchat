# roomlist-worker — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `377040a` (base `main`)  
**Overall score:** 3.7 / 5 (baseline 2026-09-01: 3.7, Δ -0.0)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

roomlist-worker is one of the best-engineered workers in the fleet: the disjoint-halves contract with broadcast-worker's preview writer holds exactly as CLAUDE.md describes, `msgbucket.NewerRow` ordering is applied on both the coalescer and the BSON filter, settlement is exclusively `jsretry.Settle`, shutdown ordering is explicit and load-bearing, and every business-logic function sits at 80–100% coverage. The 65.9% figure is mechanical — `main()` holds 30% of the statements — and the remaining findings are ops-facing rather than correctness bugs. Three reviewers independently flagged that the only bound on the mention map is derived as `4×CONSUMER_MAX_ACK_PENDING` with no validation, so a legal `0` or `-1` (JetStream "unlimited") silently disables the early drain the worker's memory model depends on. The operator README contradicts the binary: it omits `FLUSH_TIMEOUT` and the `2×FLUSH_TIMEOUT + FLUSH_INTERVAL < ACK_WAIT` fail-fast rule, and says the health endpoint checks only NATS when `/readyz` now also fails on a dead consume loop. Subscription writes filter on `(roomId, u.account)` with no `WarnMissingIndexes` unlike every sibling on the shared collection; the 10-minute `DefaultBackoff` tail pins the whole ack-pending budget for 5–10 minutes after a MongoDB outage ends, which `MaxDeliver=-1` makes unnecessary; `DeliverNewPolicy` on a durable means a deleted-and-recreated consumer silently loses every pointer and badge in the gap; the edit path can badge a message's own sender; and `docs/client-api.md` still names broadcast-worker as the `hasMention` writer. The store is stubbed by hand with a reasoned but rule-deviating no-mockgen comment.

| Dimension | Score |
|---|---|
| Code quality | 4 |
| Architecture | 4 |
| Test coverage | 2 |
| Maintainability | 4 |
| Integration | 4 |
| Performance | 4 |

**Findings by severity:** 0 critical, 1 high, 9 medium, 21 low, 11 nitpick (42 total).  
**Highest-risk dimension:** Test coverage (2).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 4

### Evidence

- [medium] Derived mention budget silently disables itself on a legal `CONSUMER_MAX_ACK_PENDING` — `roomlist-worker/main.go:145` — `newFlusher(store, 4*cfg.Consumer.MaxAckPending, …)`; `stream.ConsumerSettings` (`pkg/stream/consumer.go:22`) has no `Validate`, so `-1` (NATS "unlimited", the plausible operator override the README's "outage buffer" wording invites) or `0` yields a budget ≤ 0 and `flush.go:193` (`f.mentionBudget > 0 && …`) turns the early drain off. That drain is the only bound on the `mentions` map (`batch.go:42-46`, `flush.go:160-168`), and `validateFlushBudget` (`main.go:295`) checks every other knob but not this one — the exact "never accumulate, never livelock" invariant the worker is built around goes unenforced by config.
- [low] Store methods return bare `err` — `roomlist-worker/store_mongo.go:229-230`, `:240-241`, `:251-252` — `_, err := m.roomCol.BulkWrite(…); return err` in all three methods. CLAUDE.md §3 "never return bare err"; sibling stores (`broadcast-worker`, `message-worker`, `notification-worker`) have no such site. Impact is nil in practice: `mongoutil.Collection.BulkWrite` wraps with the collection name (`pkg/mongoutil/collection.go:176`) and `flush.go:277` adds the stage name, so the chain reads `flush subscription mentions: bulk write subscriptions: …` — but the store's own method name never appears, and the file-header comment (`store_mongo.go:100-103`) documents relying on the callee's wrap instead of following the rule.
- [low] Hand-written store stubs instead of mockgen — `roomlist-worker/store.go:64-67` — "No mockgen directive: the tests use hand-written stubs … because they assert call order and context cancellation, which gomock expectations do not express". CLAUDE.md §1/§4 mandate `//go:generate mockgen` + `mock_store_test.go`; gomock's `gomock.InOrder` and `DoAndReturn` (which receives `ctx`) express both concerns, so the stated rationale does not hold. Three stub types (`stubStore`, `panicStore`, `blockingStore` in `flush_test.go:21,250,422`) now duplicate what one generated mock would provide.
- [low] Operator README contradicts the code it documents — `roomlist-worker/deploy/README.md:57` says "The health endpoint deliberately checks only NATS", but `main.go:178-181` registers `consume.Check()` alongside it; the Tuning table (`README.md:47-51`) omits `FLUSH_TIMEOUT` entirely and states `CONSUMER_ACK_WAIT` "must exceed FLUSH_INTERVAL plus write latency", whereas `main.go:78`/`:308` fail startup unless `2×FLUSH_TIMEOUT + FLUSH_INTERVAL < ACK_WAIT`. An operator tuning from the README hits a fail-fast the README never mentions.
- [nitpick] Batch-level failure log inherits one message's identity — `roomlist-worker/flush.go:199-206` — `flushNow` derives the drain context from a single message's `handlerCtx` (request id, trace, `obs.ContextWithIdentity` user/room baggage), and `settle` logs the whole batch's failure with it (`flush.go:329`). A poison-batch ERROR will be attributed to whichever message tipped the budget, not to the batch; the periodic path carries no request id at all.
- [nitpick] `time.Sleep` pacing a feeder goroutine in a test — `roomlist-worker/flush_test.go:402` — synchronisation is via `require.Eventually`, so this is pacing rather than sync, but it is the pattern CLAUDE.md §3 bans and the loop would work as a tight `select` without it.

### Recommendations

- [medium] Extend `validateFlushBudget` (or add a sibling) to reject `Consumer.MaxAckPending <= 0` and derive the budget from the validated value — `main.go:78,145` — turns a silent loss of the mention-map bound into the same fail-fast the other flush knobs already get; add table cases `{0, -1}` to `TestValidateFlushBudget` (`main_test.go:250`).
- [low] Wrap each store return: `return fmt.Errorf("bulk update room last message: %w", err)` etc. — `store_mongo.go:229,240,251` — restores CLAUDE.md §3 compliance and keeps `errors.As(mongo.BulkWriteException)` in `classifyFlushErr` working (`%w` preserves it; `TestClassifyFlushErr` "wrapped bulk exception" already proves this).
- [low] Replace the three hand stubs with a mockgen `MockStore` (`//go:generate mockgen -source=store.go -destination=mock_store_test.go -package=main`) using `gomock.InOrder` for stage ordering and `DoAndReturn` for the deadline/blocking cases — `store.go:64`, `flush_test.go:21-67,250-256,422-446` — aligns with the repo convention and the "mocks not stale" CI check.
- [low] Bring `deploy/README.md` in line with `main.go`: document `FLUSH_TIMEOUT`, state the `2×FLUSH_TIMEOUT + FLUSH_INTERVAL < CONSUMER_ACK_WAIT` rule and the consume-loop readiness check — `README.md:47-58`.
- [nitpick] Have `flushNow` build its drain context from the flusher's own base context (trace-less, like the periodic path) and log the triggering request id as an attribute instead — `flush.go:199-206`, `:329`.

## 3. Architecture — score 4

### Evidence

- [medium] The only bound on the batch maps is derived from `MaxAckPending` and silently vanishes when that knob is set to 0 or -1 — `roomlist-worker/main.go:145` — `newFlusher(store, 4*cfg.Consumer.MaxAckPending, …)` makes the mention budget 0/-4, and `flusher.add` (`flush.go:70`) treats any non-positive budget as "no early drain". `batch.go:42-46` documents that `held`/`rooms`/`lastSeen` are bounded by MaxAckPending and only `mentions` needs the budget; with `CONSUMER_MAX_ACK_PENDING=-1` (JetStream unlimited) or `0` (server default) both bounds evaporate. `cfg.Pool.Validate()` runs at `main.go:87` but nothing validates `cfg.Consumer`, and `pkg/stream.ConsumerSettings` has no `Validate` (`pkg/stream/consumer.go`).
- [low] `DeliverNewPolicy` overrides the `DurableConsumerDefaults` DeliverAll invariant — `roomlist-worker/main.go:398` — justified for first deploy, but a deleted-and-recreated durable (ops mistake, consumer-config migration) silently skips every message in the gap: badges, `lastMsgAt` and sender `lastSeenAt` for those messages are lost with no replay path, the exact loss class `deploy/README.md` warns about for deploy ordering. Neither the README nor the code comment names this second trigger.
- [low] No `//go:generate mockgen` directive or `mock_store_test.go` — `roomlist-worker/store.go:8-11` — CLAUDE.md's per-service layout and testing rules prescribe mockgen; the hand-written `stubStore`/`blockingStore`/`panicStore` in `flush_test.go:21,250,422` are a documented, reasoned exception, but it is still a repo-convention deviation a reviewer should sign off explicitly.
- [low] `deploy/README.md` contradicts the readiness contract — `roomlist-worker/deploy/README.md` ("The health endpoint deliberately checks only NATS") vs `main.go:178-181`, which registers `consume.Check()` so `/readyz` fails once the loop dies. Ops-facing docs drift on the probe semantics.
- [nitpick] Room-pointer write stamps `updatedAt` with the message's `createdAt` rather than write time — `roomlist-worker/store_mongo.go:114` — a rename in `room-service` (`room-service/store_mongo.go:1922`, `updatedAt: now`) landing between message creation and the flush is regressed. No reader sorts or versions on `rooms.updatedAt` today (grep), so inert.
- [nitpick] Consumer pattern is a third shape beyond the two CLAUDE.md documents: `cons.Messages()` drained by one goroutine with no semaphore — `roomlist-worker/main.go:154,334` — justified in the comment (no per-message I/O), not mixed within the consumer.
- [nitpick] `bootstrapStreams` receives `wiring.CanonicalWildcard` as the stream subject instead of `wiring.CanonicalStream.Subjects` — `roomlist-worker/main.go:128` — identical today (`pkg/stream/pipeline.go:53,62`), but it is a second source of truth for the schema `pkg/stream` owns. Same shape as `broadcast-worker/bootstrap.go:28`.

### Recommendations

- [medium] Validate `cfg.Consumer.MaxAckPending > 0` at startup (fail fast like `validateFlushBudget`), or clamp the mention budget to a positive floor independent of MaxAckPending — `roomlist-worker/main.go:145` — restores the documented memory bound under every operator-settable value; ideally add `ConsumerSettings.Validate()` in `pkg/stream` so every worker gets it.
- [low] Document the durable-recreation loss in `deploy/README.md` (and the `main.go:395` comment) and state the operator remedy (never delete the durable; use `CreateOrUpdateConsumer` in place) — `roomlist-worker/main.go:398` — makes the one unrecoverable data-loss path visible to ops.
- [low] Either add the `//go:generate mockgen` directive (mocks unused but convention-compliant) or record the exception in CLAUDE.md's sanctioned-exceptions list — `roomlist-worker/store.go:8` — keeps `make generate` uniform and removes reviewer ambiguity.
- [low] Fix the README health paragraph to describe both probes (NATS + consume-loop readiness, and why MongoDB is deliberately excluded) — `roomlist-worker/deploy/README.md`.
- [nitpick] Pass `wiring.CanonicalStream.Subjects` (or the whole `stream.Config`) into `bootstrapStreams` — `roomlist-worker/main.go:128` — single source of truth for the stream schema.
- [nitpick] Drop `updatedAt` from the pointer `$set` or write `$$NOW`/write-time — `roomlist-worker/store_mongo.go:114` — prevents a future reader from inheriting a regressing timestamp.

## 4. Test coverage — score 2

### Evidence

- [high] coverage below repo minimum 80%, currently 65.9% (184/279 statements) — `roomlist-worker/main.go:65` — Per CLAUDE.md §4 this is a merge blocker. The gap is almost entirely `main()`: its 83 uncovered statements (`main.go:65-222`) are 29.7% of the service; with `main()` excluded the tree sits at 184/196 = 93.9%. The other 0% functions are `requestSelfShutdown` (`main.go:265-274`, 6 stmts), `pretouchJSON` (`pretouch.go:16`) and `NewMongoStore` (`store_mongo.go:23-28`, exercised only under the `integration` tag at `integration_test.go:24`).
- [medium] The graceful-shutdown ordering is untested — `roomlist-worker/main.go:189-221` — The eight `shutdown.WaitOn` closures encode the invariants the comments defend (`beginShutdown` before `iter.Stop`, `wg.Wait` before `flushCancel` so the final flush includes every intent, drain/final-flush timeouts). All are inline literals inside `main()`, so nothing can assert them; a reorder that drops the final batch on SIGTERM would pass CI.
- [medium] `BulkAdvanceLastSeen` and `BulkSetMentions` write models are never asserted at unit level — `roomlist-worker/store_mongo.go:149-153`, `:160-164` — Both loop bodies are 0% in the profile (the 80% figure comes only from the empty-map early return in `TestMongoStore_BulkWrites_EmptyMapNoOp`, `store_mongo_test.go:132`). The `$max lastSeenAt` monotonicity promised by `store.go:23-24` and the `mentionFilter`/`$set hasMention` pairing (the #467 clobber fix) are verified only by integration tests, unlike `roomLastMsgModels`, which was deliberately split out "so the filter/update pairing is assertable without a live Mongo" (`store_mongo.go:63-65`).
- [low] Transient failure in the third stage is not covered — `roomlist-worker/flush.go:176-178` — `flush_test.go` drives transient failures at stage 1 (`:130`) and stage 2 (`:158`) and a permanent one at stage 3 (`:118`), but never a transient `BulkSetMentions` error, so the early-return branch after two successful stages is 0%.
- [low] Poison-batch diagnostics are computed but never asserted — `roomlist-worker/flush.go:164-165`, `:202-206` — `stageCodes`/`mongoErrs` exist so an operator can grep stage-to-code pairs, yet no test checks the `"dropped poison batch"` line or its `mongo_stage_codes` attr, although `captureLogs` (`consumeloop_test.go:306`) already provides the harness.
- [low] `flushNow`'s zero-timeout fallback is untested — `roomlist-worker/flush.go:78-80` — every test that reaches `flushNow` builds the flusher with a non-zero timeout (`consumeloop_test.go:379`); the `DefaultFinalTimeout` substitution is 0%. Also untested: that a cancelled per-message `handlerCtx` does not abort the drain (`context.WithoutCancel`, `flush.go:81`).
- [low] Store is stubbed by hand rather than `mockgen` — `roomlist-worker/store.go:8-11` — CLAUDE.md §4 requires `go.uber.org/mock`; the comment justifies stubs by call-order and ctx assertions, both of which gomock expresses (`gomock.InOrder`, `DoAndReturn`). A documented, reasoned deviation, but a deviation.
- [nitpick] Feeder goroutine uses `time.Sleep` and is not joined — `roomlist-worker/flush_test.go:395-405` — It paces adds rather than synchronises (the real sync is `require.Eventually`), but the goroutine can still be mid-`f.add` when the test returns.

### Recommendations

- [high] Lift the body of `main()` after config parsing into `run(ctx, cfg, deps) error` (or at minimum extract the `shutdown.WaitOn` step list into `shutdownSteps(consume, iter, wg, flushCancel, flushDone, ...) []func(context.Context) error`) and unit-test it with the existing `fakeIterator`/`stubStore` — `main.go:145-221` — this is the only realistic route to 80%; it also pins the "final flush after consume drain" invariant.
- [medium] Split `lastSeenModels(map[subKey]time.Time)` and `mentionModels(map[subKey]time.Time)` out exactly as `roomLastMsgModels` is, and assert filter/update pairs (`$max` on `lastSeenAt`; `mentionFilter` + `$set hasMention`) — `store_mongo.go:147-167` — closes the two 0% loop bodies and makes the #467 contract fail fast without Docker.
- [medium] Add `TestFlusher_TransientFailureInMentionsStageNaks` (`failWith: {"mentions": errors.New(...)}`) asserting Nak and `order == [rooms,lastSeen,mentions]` — `flush.go:176-178`.
- [low] Test `requestSelfShutdown` by registering `signal.Notify(ch, syscall.SIGTERM)` in the test, calling it, and receiving on `ch` — `main.go:265` — safe in-process once a handler is installed; covers the success path (the two error branches are effectively unreachable).
- [low] Reuse `captureLogs` in a flush test to assert the poison branch logs `"room-list state flush dropped poison batch"` with a `mongo_stage_codes` entry of the form `subscription mentions=[9]` — `flush.go:202-206`.
- [low] Add a `flushNow(ctx)` test with `newFlusher(store, n, 0)` and an already-cancelled ctx, asserting the store still received the write with a deadline — `flush.go:76-84`.
- [nitpick] Either add a `//go:generate mockgen` directive and a `mock_store_test.go`, or keep the stubs and reference this file in `CLAUDE.md`'s sanctioned-exception list so the deviation is not re-litigated on every review — `store.go:8-11`.

## 5. Maintainability — score 4

### Evidence

- [medium] Operator docs contradict the config validation the binary enforces — `roomlist-worker/deploy/README.md:43-49` — the Tuning table says only "`CONSUMER_ACK_WAIT` must exceed `FLUSH_INTERVAL` plus write latency" and omits `FLUSH_TIMEOUT` entirely, but `main.go:78` + `validateFlushBudget` (`main.go:295-313`) exit(1) unless `2×FLUSH_TIMEOUT + FLUSH_INTERVAL < AckWait`. An operator following the README (e.g. `ACK_WAIT=15s` with the default `FLUSH_TIMEOUT=10s`) gets a boot failure. The same stale sentence is copied into `deploy/user/docker-compose.yml:21` and `deploy/bot/docker-compose.yml:21`.
- [medium] README states the health endpoint "deliberately checks only NATS" — `roomlist-worker/deploy/README.md:57-59` — but `main.go:176-181` registers `consume.Check()` alongside the NATS probe and `main.go:172` wires `requestSelfShutdown`, so a dead consume loop now fails `/readyz` and SIGTERMs the pod. An on-call engineer reading the README would misdiagnose that restart.
- [low] `main()` is 157 lines (`main.go:65-222`) and 0% covered (cov-func) — the eight shutdown closures at `main.go:189-221` encode ordering invariants the comments call load-bearing (`beginShutdown` before `iter.Stop`, `flushCancel` only after `wg.Wait`) yet nothing can assert them because the wiring is inline in `main`. Sibling mains are 402–569 lines so this is the repo norm, not an outlier.
- [low] Two drain entry points with different bounding rules — `flush.go:76-85` — `flushNow` re-implements the per-flush deadline that `pkg/flushloop/flushloop.go:122-127` (`bound`) already provides, plus a `timeout <= 0` fallback to `DefaultFinalTimeout` that `main.go:78` makes unreachable (the uncovered branch behind `flushNow` 83.3%). `flushloop`'s own package doc (`flushloop.go:5-13`) exists precisely because three hand-rolled drains drifted.
- [low] Magic multiplier — `main.go:146` — `newFlusher(store, 4*cfg.Consumer.MaxAckPending, …)`: the mention-budget factor lives only in a comment (`main.go:141-145`) and is absent from the README Tuning table, so an operator tuning `CONSUMER_MAX_ACK_PENDING` cannot see it also moves the early-drain threshold.
- [low] `flushOutcome.mongoErrs` (`flush.go:114-120`) exists to work around `errcode.Error`'s unexported cause — the comment admits `Classify` would log the driver text under "request failed". Using the request/reply error package for worker poison classification leaks: the service carries a parallel error channel just to keep the log greppable.
- [nitpick] `deploy/user/Dockerfile` and `deploy/bot/Dockerfile` are byte-identical (`cmp`), and the two `azure-pipelines.yml` differ only in `IMAGE_NAME`. The `deploy/{user,bot}/` split diverges from CLAUDE.md's documented `deploy/Dockerfile` layout but matches `broadcast-worker` and `notification-worker` — an established, undocumented convention.
- [nitpick] `streamManager` (`bootstrap.go:23-26`) is copy-pasted verbatim in 15 services; the comment at `:22` ("kept service-local so it doesn't pollute pkg/") is the repo's stated choice, so this is repo-wide debt, not this service's.
- [nitpick] Invariant comment pinned to a test name and a stack depth — `flush.go:209-217` — "three frames away" and `TestFlusher_PermanentThenTransientNaksAndSkipsThirdStage` will silently rot on the next rename; several other WHY comments exceed 15 lines (`main.go:277-294`, `store_mongo.go:63-85`, `flush.go:37-51`), which is thorough but slows a first read.

### Recommendations

- [medium] Rewrite `deploy/README.md` Tuning (`:43-49`) to state the `validateFlushBudget` rule verbatim, add `FLUSH_TIMEOUT` and the derived `4×MaxAckPending` mention budget, replace `:57-59` with the consume-loop readiness + self-SIGTERM behaviour, and fix the compose comments at `deploy/*/docker-compose.yml:21` — the README is the only operator-facing doc and it currently leads to a boot failure.
- [low] Extract `run(ctx, cfg, deps) error` from `main()` (`main.go:65-222`) with the shutdown closures built by a named function, so the ordering invariants get a unit test and `main` drops to config-parse + `os.Exit`.
- [low] Route `flushNow` through `flushloop` (export `Bound`, or add `flushloop.Drain(ctx, cfg, flush)`) and delete the unreachable `<= 0` fallback at `flush.go:78-80` — one bounding rule for both drain paths.
- [low] Name the multiplier (`const mentionBudgetPerAckPending = 4`) beside `validateFlushBudget` in `main.go` and reference it from the README.
- [nitpick] Collapse `deploy/{user,bot}/Dockerfile` into one `deploy/Dockerfile` (per-mode compose/pipeline only), or document the bot/user split in CLAUDE.md "When Creating Services".
- [nitpick] Rephrase `flush.go:209-217` to state the invariant ("`write` returns on the first transient stage, so `out.err` is never a mixed join") without the test name or frame count.

## 6. Integration — score 4

### Evidence

- [medium] Subscriptions writes filter on `(roomId, u.account)` with no index creation or `WarnMissingIndexes` — `roomlist-worker/store_mongo.go:151`, `:162`, constructor `:23-28` — Every sibling that issues this exact write on the shared collection verifies `roomId_1_u.account_1` at startup (`inbox-worker/main.go:562`, `bot-room-service/store_mongo.go:36`, `user-service/mongorepo/subscriptions.go:80`); this service does neither, contrary to CLAUDE.md's "create indexes in the store constructor or EnsureIndexes". With up to `MaxAckPending`=1000 held messages per 250ms flush, a missing index turns each `BulkAdvanceLastSeen`/`BulkSetMentions` into N collscans inside the 10s `FLUSH_TIMEOUT`, which then NAKs the batch forever under `MaxDeliver=-1`.
- [low] `docs/client-api.md` still attributes the local `hasMention` write to `broadcast-worker` — `docs/client-api.md:5288` ("`broadcast-worker` sets `hasMention`") and `:982` (deployment note about a mixed `broadcast-worker` fleet) — The write now lives in `roomlist-worker/handler.go:43-46` / `store_mongo.go:158-167`; `broadcast-worker/handler.go:409-412` explicitly says it makes no MongoDB badge write. The wire schema is unchanged, so this is prose drift, but the doc is the client-facing reference and now names the wrong owner and a stale rollout caveat.
- [low] Edit path can badge the message's own sender — `roomlist-worker/handler.go:77-93` vs `flush.go:122-125` — On create, self-mention is excluded only by stage ordering (lastSeenAt advanced before `mentionFilter` runs; `integration_test.go:109-126`). `EventUpdated` derives no `SenderAccount`, so the sender's `lastSeenAt` stays at `createdAt` < `editedAt` and `mentionFilter` (`store_mongo.go:55-61`) matches: an author who edits a message that @-mentions themself gets `hasMention=true`. Same path also re-badges every still-mentioned reader who read after the original (additive by design per the comment; I could not verify the pre-split `badgeNewlyMentionedAccounts` semantics from git — see NOTES).
- [low] `DeliverNewPolicy` on a durable whose loss is silent — `roomlist-worker/main.go:395-398` — Justified for first deploy, but if ops ever delete/recreate the durable (e.g. during a NATS incident), the consumer resumes at "new" and every pointer/badge/lastSeenAt for messages in the gap is lost permanently — exactly the failure `deploy/README.md` warns about for reverse deploy order — with no log line. Nothing in `deploy/README.md` says the durable must be preserved.
- [low] `rooms.updatedAt` is `$set` to the message's `createdAt` — `roomlist-worker/store_mongo.go:111-115` — Other writers stamp `updatedAt: time.Now()` (`room-worker/store_mongo.go:151`, `:793`; `room-service/store_mongo.go:1922`). A batch redelivered after a long outage (`MaxDeliver=-1`, hour-old messages) can move `updatedAt` backwards past a later rename. No reader of `Room.UpdatedAt` was found in handlers, so no current impact; it is a latent contract inconsistency.
- [nitpick] Consumer binds `chat.msg.canonical.{site}.>` (`main.go:134` via `wiring.CanonicalWildcard`) while `pkg/subject/subject.go:704-709` documents `MsgCanonicalMessageWildcard` (`.*`) as what message-event consumers should bind; `search-sync-worker/messages.go:129` follows that, `broadcast-worker/main.go:338` does not. Harmless today — the teams batch envelope lives on `chat.teams.msg.canonical.>` (`subject.go:391-398`) and a foreign payload decodes to `RoomID==""` and is acked (`handler.go:53`).

Verified clean (no finding): `MessageEvent`/`Message` JSON tags match `eventProjection` (`projection.go:25-48` vs `pkg/model/event.go:29-35`, `message.go:9-42`) and `projection_test.go` pins them; `EventUpdated` publishers always set `EditedAt` (`history-service/internal/service/messages.go:559`, `migration.go:81`). Pointer tie-break is `msgbucket.NewerRow` on both sides (`batch.go:87`, `store_mongo.go:42-50`; `broadcast-worker/preview_writer.go:114,120`) and history-service's freshness check is a pure identity compare (`history-service/internal/mongorepo/room.go:119`), its walk never writes `lastMsgId` (`service/rooms.go:186`). Cross-site badge guard is identical (`inbox-worker/main.go:574-580` vs `store_mongo.go:55-61`). `roomtimescache` deliberately excludes `lastMsgAt` (`pkg/roomtimescache/roomtimescache.go:16-27`) and `badgecache` is bumped by the notification path, so no invalidation is owed. `hasUnread`/`hasGroupMention`/sort derive from `Coalesce(lastUserMsgAt,lastMsgAt)` and `lastMentionAllAt` (`user-service/service/subscriptions.go:256-257,597`) matching the `$max`/freeze writes (`store_mongo.go:86-97,129-137`) and `docs/client-api.md:982-983,1022`. Config knobs (`Pool`, `Consumer`) are mounted from owning packages; bootstrap sets Name+Subjects only; no IDs generated, no events published, no Cassandra/`MESSAGE_BUCKET_HOURS`, no raw subjects.

### Recommendations

- [medium] Add `mongoutil.WarnMissingIndexes(ctx, subCol, "roomId_1_u.account_1")` (house convention for a shared collection) to `NewMongoStore` or an `ensureIndexes` called from `main.go:114` — `roomlist-worker/store_mongo.go:23` — surfaces the collscan before it manifests as a permanently NAKing batch.
- [low] Update `docs/client-api.md:5288` and `:982` to name `roomlist-worker` as the local badge/pointer writer and drop the mixed-`broadcast-worker`-fleet caveat — keeps the client reference truthful about ownership.
- [low] On `EventUpdated`, exclude `msg.UserAccount` from `MentionAccounts` in `deriveIntents` (`handler.go:88-92`) and add a table case + integration test for a self-mention edit — restores the create-path invariant that a sender is never badged by their own message.
- [low] Document in `deploy/README.md` that the `roomlist-worker` durable must never be deleted (or set `OptStartTime` from the last known ack on recreation) and log at ERROR when `CreateOrUpdateConsumer` reports a freshly created consumer with `DeliverNew` on a non-empty stream — makes gap-loss visible instead of silent.
- [nitpick] Bind `subject.MsgCanonicalMessageWildcard` (or fix the stale comment in `pkg/subject/subject.go:704-709`) so all canonical consumers agree on the filter — `main.go:134`.

## 7. Performance — score 4

### Evidence

- [medium] Post-outage recovery is throttled by a 10-minute NAK tail that also pins the ack-pending budget — `roomlist-worker/flush.go:218` — Held messages settle with `jsretry.DefaultBackoff` (`pkg/jsretry/jsretry.go:52-58`: 1s/5s/30s/2m/10m by NumDelivered). A delayed NAK keeps the message ack-pending server-side, so after a MongoDB outage longer than ~2.6 min all `MaxAckPending` (default 1000) slots are parked for a jittered 5–10 min. Once Mongo is back, JetStream delivers no new messages until parked ones return and free slots: `lastMsgAt`/`hasMention`/`lastSeenAt` for live traffic stall up to 10 min after recovery while `/readyz` stays green. DefaultBackoff's rationale ("without exhausting MaxDeliver") does not apply — `MaxDeliver=-1` (`main.go:392`) and the ack-pending budget, not the schedule, is the outage buffer.
- [medium] No custom metrics for the coalescer, so the two tuning knobs cannot be sized from evidence — `roomlist-worker/main.go:145`, `flush.go:89-100` — `deploy/README.md` says `CONSUMER_MAX_ACK_PENDING` must exceed `FLUSH_INTERVAL × peak rate`, but nothing exports batch size (`len(b.held)`, `len(b.mentions)`), flush duration, outcome (ok/transient/poison) or inline-drain count. Sibling hot-path workers ship a `nats_metrics.go` (`message-worker/nats_metrics.go:1-40`, `broadcast-worker/nats_metrics.go:56-88`); here the only signals are the consumer's `num_ack_pending` and one ERROR log per failed batch (`flush.go:206`).
- [low] `validateFlushBudget` ignores pull-buffer dwell created by the inline mention drain — `roomlist-worker/main.go:308`, `main.go:379-381`, `main.go:154` — The budget `2×FLUSH_TIMEOUT + FLUSH_INTERVAL < AckWait` (20.25s < 30s) assumes a message enters the batch on delivery. `flushNow` blocks the single consume goroutine for up to `FLUSH_TIMEOUT` (`flush.go:81-83`) while up to `PullMaxMessages(256)` already-delivered messages sit in the iterator buffer with AckWait running. Worst case 10s + 10s + 0.25s + 10s = 30.25s > 30s: the batch redelivers underneath a flush still writing. Writes are replay-safe (`store.go:13-15`), so the cost is duplicate bulk writes and a double settle, not correctness.
- [low] Every rollout leaves up to 256 pulled-but-unconsumed messages to redeliver after AckWait — `roomlist-worker/main.go:194`, `main.go:154` — `iter.Stop()` discards the client buffer; those messages' room-list state lands only after the 30s AckWait on the replacement pod. The final flush already runs after the loop drains (`main.go:207-215`), so `iter.Drain()` (o11y wrapper, `o11y@v0.12.0/nats/jetstream.go:636`) would let them join it.
- [low] Held-message memory scales with `CONSUMER_MAX_ACK_PENDING` and is undocumented — `roomlist-worker/batch.go:148`, `deploy/README.md` "Tuning" — Each `heldMsg` retains the full `jetstream.Msg` payload until its batch lands; ≤ ~30MB at the default 1000, but the README tells operators to raise the knob with peak rate and omits the linear RSS cost (100k → ~3GB during an outage, when the buffer is by design full).
- [nitpick] Mongo pool ceiling of 150 for at most two concurrent bulk writers — `roomlist-worker/main.go:49`, `pkg/mongoutil/poolconfig.go:25` — ticker flush and inline drain are the only writers (`flush.go:89-100`, `flush.go:76-84`).

Verified non-findings: sonic decode of a 9-field projection (`main.go:368`, `projection.go:25-49`, warmed in `pretouch.go:12-16`); no `map[string]interface{}`; zero reads, so projection/`$lookup` rules are moot; each stage is one unordered `BulkWrite` (`store_mongo.go:143,154,165`, `pkg/mongoutil/collection.go:169-173`) with ≤2 models/room, ≤1/subscription, ≤4×MaxAckPending mentions (`main.go:145`, `flush.go:66-71`); `{roomId,u.account}` filters (`store_mongo.go:151,162`) hit room-service's unique index (`room-service/store_mongo.go:124-125`); write maps bounded by `held`, mention map by the inline drain, next-batch pre-sizing clamped (`batch.go:155-166`, `pkg/flushloop/flushloop.go:45-50`); lock held only for the swap (`flush.go:90-97`); both flush paths deadline-bounded (`flush.go:81`, `flushloop.go:108,113`) and `MONGO_SERVER_SELECTION_TIMEOUT=2s` (`poolconfig.go:49`) turns a dead Mongo into a fast NAK; no `time.Sleep` sync, every goroutine has a termination path (`main.go:146-148,170-173,195-204`).

### Recommendations

- [medium] Cap the redelivery schedule for held messages — `flush.go:218` — a short-tail schedule (e.g. `LowLatencyBackoff` truncated at 30s, or `{1s,5s,15s,30s}`); `MaxDeliver=-1` removes the reason for a 10m tail, so recovery completes within ~30s of Mongo returning and parked slots stop starving live traffic.
- [medium] Add a `nats_metrics.go` mirroring the sibling workers — `flush.go:99`, `flush.go:192-207`, `main.go:379-381` — flush-duration and held/mention-size histograms, an outcome counter by `{ok,transient,poison}`, an inline-drain counter; these size `FLUSH_INTERVAL`/`CONSUMER_MAX_ACK_PENDING` and alert on the poison-drop path, which is otherwise a log line.
- [low] Fold the inline-drain dwell into `validateFlushBudget` — `main.go:308` — charge `3×FLUSH_TIMEOUT + FLUSH_INTERVAL` (or give the inline drain its own shorter bound and charge that), so the default 30s AckWait either fails fast or the defaults move (e.g. `FLUSH_TIMEOUT=5s`).
- [low] Drain rather than stop the iterator at shutdown — `main.go:194` — `iter.Drain()` lets the ≤256 buffered messages reach the final flush; note the deviation from the `iter.Stop()` convention inline.
- [low] Document held-message memory in `deploy/README.md` "Tuning" — "RSS ≈ MaxAckPending × max payload during an outage" beside `CONSUMER_MAX_ACK_PENDING`, so raising it for throughput also sizes the pod memory limit.
- [nitpick] Override `MONGO_MAX_POOL_SIZE` to a small value (e.g. 4) in `deploy/*/docker-compose.yml` — the writer count is two by construction.

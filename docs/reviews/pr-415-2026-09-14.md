# Branch review — PR #415

**PR:** [#415 — fix(history-service): evict subscription access cache on membership change](https://github.com/hmchangw/newchat/pull/415)
**Head:** `fix/history-subcache-invalidate-on-membership` @ `94759ea`
**Base:** `main` @ `6eb3b51`
**Date:** 2026-09-14
**Size:** 10 files, +973 / −38, 1 commit
**Fixes:** #414

## Services touched

| Service | Files |
|---------|-------|
| `history-service` | `cmd/main.go`, `cmd/subinvalidate.go`, `cmd/subinvalidate_test.go`, `internal/config/config.go`, `internal/config/config_test.go`, `internal/mongorepo/subscription.go`, `internal/mongorepo/subscription_test.go`, `internal/readcache/readcache.go`, `internal/readcache/readcache_test.go`, `internal/readcache/metrics_test.go` |

No `pkg/` files changed. No client-facing handler registered, so `docs/client-api.md` is correctly untouched (see Service chapter, concern (f)).

## Review method

PR mode. The PR head was fetched into a detached **git worktree** rather than checked out over the working tree, which allowed the per-service generalist to run alongside the 5 global lenses — six agents total, dispatched in parallel:

1. Per-service generalist (`history-service`)
2. Go expert
3. Test automation
4. Bug & security (incl. `make sast`)
5. Performance
6. Observability

## Findings by severity

| Severity | Count |
|----------|------:|
| critical | 1 |
| high | 10 |
| medium | 18 |
| low | 10 |
| nitpick | 5 |

## Top-line risk assessment

**Do not merge as-is.** The PR correctly identifies a real security bug (#414) and the *core* idea — active eviction of the L1 access-check cache, keyed identically to the read path — is sound, well-commented, and genuinely tested at the unit level (`readcache` coverage 92.4%, `go test -race ./history-service/...` passes clean).

The risk is not in the eviction itself but in the four hardening mechanisms bolted around it, each of which carries independent, unreviewed failure modes:

- **Availability regression (critical).** The generation counter that guards against evict/load races is *cache-global*, not per-key. A bulk membership operation — HR sync, org removal, a Teams room reconcile — bumps it thousands of times per second, which forces every concurrent in-flight load on *unrelated* keys to re-read up to 5 times and then fail closed with a hard error. That error surfaces as a failed `msg.history` read for users who have nothing to do with the membership change, with 5× read amplification onto a now-`primary`-pinned Mongo. Three lenses found this independently.
- **The headline mechanism is partly unverified.** The evict-cancels-in-flight-load path returns `context.Canceled` *before* the generation retry runs, so on a real ctx-respecting source the retry loop is dead code and an eviction errors a concurrent reader's request instead of re-reading. The test that appears to prove the retry uses a fake source that ignores its context.
- **The hole is not fully closed.** The suspend/resume barrier covers NATS *disconnects* only. A core-NATS slow-consumer drop — likely precisely during the bulk removals this feature exists for — loses evictions with no disconnect, no error, and no metric, silently reverting to TTL-only staleness. Cross-site (federated) removals publish no `subscription.update` at the destination site at all.
- **It is unobservable.** Nothing counts evictions, suspensions, retries, or either fail-closed path. A dead subscriber or a renamed subject degrades silently back into #414.

Two further issues are worth calling out because they mean the PR does not do what its description says: `SUBSCRIPTION_READ_PREFERENCE` never reaches the access read it claims to protect (the real path was already pinned to `primary`), and the eviction subscription is a non-queue wildcard that delivers every user's high-frequency `read`/`mute`/`favorite` events to every replica, decoded in full before being discarded ~99% of the time.

**Recommended shape:** land the eviction + the subject/ctx/metric fixes as a tight PR; split the semaphore, the cancel-on-supersede, the generation counter, and the read-preference change into separate changes that can each be reasoned about on their own.

### SAST caveat

`make sast` could **not** be fully run in this environment: `gosec` ran and is **clean** on this branch, but `govulncheck` was blocked by the proxy (`Forbidden` from `vuln.go.dev`) and `semgrep` could not be installed (`pipx` / `uv` version conflict). **Semgrep and govulncheck coverage for this branch is unverified here — CI's blocking `sast` job must confirm.**

---

# Service: history-service

Per-service generalist, judging the diff against the service's own conventions. All paths relative to the PR head.

## (a) Diff correctness

- **high — The new read-pref knob misses its stated target.** `internal/config/config.go:51-57` says it "backs the access-control reads (GetSubscription / GetHistorySharedSince)", but the access check routes through `subL2` → `subTier` over `subsPrimary`, which was *already* `readpref.Primary()` (`cmd/main.go:257-258`). `subRepo` (`cmd/main.go:235`, `base` at `:282`) now only serves `GetSubscription`, so the knob silently converts pin/unpin subscription reads to fail-closed strict primary and does nothing for authz. The security rationale in the comment — and the "operators who accept that trade-off" escape hatch — is inert as wired.
- **high — Federated membership changes never evict.** `inbox-worker/handler.go:402-408,472` deletes the local subscription replica and busts L2, but publishes no `subscription.update`; only `room-service/handler.go:868`, `room-worker/handler.go:111` and `user-service/service/apps.go:116` do. On the destination site a removed remote member keeps the L1 grant for the full `SubCacheTTL`. **#414 is closed same-site only.**
- **medium — `gen` is global** (`internal/readcache/readcache.go:63-65`): an evict of *any* key invalidates every in-flight load of every key. A bulk removal (`room-worker/handler.go:665` loops accounts) or a Suspend/Resume flap (`cmd/main.go:309-320`) can burn `maxLoadAttempts=5` (`readcache.go:207`) and return the fail-closed error at `:247` — user-visible read errors plus up to 5× Mongo reads per miss. (Escalated to **critical** in the consolidated action list; see Bug & Security and Performance chapters.)
- **medium — A superseded load cancels a *legitimate* concurrent read.** `remove` cancels the fetch ctx (`readcache.go:119-124`) and the loop treats `context.Canceled` as a terminal error (`:226`) rather than retrying.
- **medium — `inflight` map overwrite.** After `sf.Forget`, a second flight for the same key overwrites `c.inflight[key]` (`readcache.go:217`), so the earlier flight becomes uncancellable and the next `Evict` cancels the fresh, correct load instead.
- **low —** `readcache.go:247` should be a package-level sentinel so callers can use `errors.Is`, per CLAUDE.md §3 ("never compare errors by string").
- **low —** "0 disables it" (`readcache.go:36`, `:78`) and `if c.sem != nil` (`:181`) describe an unreachable state; `newTTLCache` always builds `sem` (`:95`).

## (b) Scope drift / refactor-readiness

- **medium —** The title promises eviction; the diff also ships a read-preference knob, a semaphore + env knob, cancel-on-supersede, and a suspend/resume barrier. Four hardening changes with independent risk profiles in one PR.
- **low —** `readcache.go` is now 526 lines (a generic cache plus three concrete caches). Split into `ttlcache.go` / `subscription.go` / `room.go` / `preview.go`.
- **low —** Invalidation *policy* lives in `package main` (`cmd/subinvalidate.go:40`) while the cache it drives is `internal/readcache`; `cmd/` now holds 4 non-`main` files.

## (c) Abstraction changes

- **medium — `subUpdateEnvelope`** (`cmd/subinvalidate.go:27-32`) hand-rolls a projection of `model.SubscriptionUpdateEvent` / `model.SubscriptionRemovedEvent`. Only `cmd/subinvalidate_test.go:31-42` (which marshals the real types) guards against a tag rename — the comment should say so. Separately, `"added"` / `"removed"` are now string literals in three services (`room-worker/handler.go:449,2211`; `user-service/service/apps.go:77,105`); make them shared constants.
- **low — `WithMaxInflight`** (`readcache.go:284-292`) is a variadic-options API for a single knob with a single caller (`cmd/main.go:286`), while `RoomCache` / `PreviewCache` pass the same value positionally (`:395`, `:504`). A plain parameter matches the file's existing style; the options type is premature.

## (d) Design coherence

- **high — Firehose subscription.** `chat.user.*.event.subscription.update` (`cmd/subinvalidate.go:78`) delivers *every* user's `read` / `mute_toggled` / `favorite_toggled` / `section_moved` event to every pod (`room-service/handler.go:1464` fires `"read"` on every read receipt), filtered only *after* unmarshal (`:40`, `:64`). There is no `SetPendingLimits` or slow-consumer handling — and a slow-consumer drop loses an eviction *without* tripping the Suspend barrier, which only covers disconnects.
- **medium — First service to use core-NATS fanout for authz invalidation.** Repo convention for inter-service events is JetStream (CLAUDE.md §6). A dedicated stream would give replay and delete most of the barrier machinery this PR adds.

## (e) Project-pattern adherence

**Good:** `subject.SubscriptionUpdateWildcard` / `ParseSubscriptionUpdateAccount` / `DecodeAccount` (`cmd/subinvalidate.go:51,60`) — no raw `fmt.Sprintf`, and the decode matches `pkg/natsrouter/params.go:111`. No new `pkg/model` event structs, so the `Timestamp int64` rule does not apply. Errors are wrapped with context (`:81`).

- **medium —** Handler chaining reads `raw.Opts.ReconnectedCB` / `DisconnectedErrCB` (`cmd/main.go:308,316`) — an unsynchronized read of fields nats.go writes under `nc.mu`, duplicating ownership of the handlers set in `pkg/natsutil/connect.go:84-90`. Add a chaining option there instead; no other service does this.
- **medium —** `slog.Warn` at `cmd/subinvalidate.go:53,62` discards the handler ctx (`:78`), losing trace/request-id correlation (cf. `room-service/handler.go:1809`).
- **low —** No eviction or suspension metric; while suspended every read records `Miss` (`readcache.go:254`), so the hit ratio cannot distinguish "barrier active" from "cold cache".
- **low —** `startSubCacheInvalidation` and the Suspend/Resume wiring (`cmd/main.go:295-321`) are untested; only `evictOnSubscriptionUpdate` is covered.

## (f) Client-API doc rule

**No finding (info).** The diff registers no `chat.user.{account}.request.…` handler, no `msg.send` handler, and no `auth-service` HTTP route, and changes no client-facing `pkg/model` struct. It only *subscribes* to an event that is already documented at `docs/client-api/events.md:48,93-95`. A consumer does not alter the wire contract, so `docs/client-api.md` is correctly untouched and **no `critical` doc finding is warranted.**

---

# Go expert

## critical

None at this lens.

## high

**1. `cancel`-on-supersede makes the generation-retry loop dead code in production.** `remove` cancels the in-flight `fetchCtx` (`internal/readcache/readcache.go:117-120`), but `getOrLoad` checks `err` *before* the stability check and returns (`:226-228`). A real Mongo load returns `context.Canceled`, so the retry loop at `:207-246` never runs on the exact race it was written for — the caller gets an error instead of a fresh read. It only *looks* exercised because `gatedSubSource` in `readcache_test.go` ignores its ctx. Pick one strategy: drop the cancel, or treat a `fetchCtx`-sourced `ctx.Err()` as "superseded → continue".

**2. The global `gen` and cancel-all now apply to `PreviewCache`, which is invalidated per room mutation.** `PreviewCache.Invalidate` (`readcache.go:497`) calls the same `remove` (`:111`) and is hit on every mutation (`internal/service/messages.go:690`). A preview invalidate for room X now bumps a cache-global generation that forces in-flight preview loads for *every other room* to re-read, and after 5 races returns a hard error (`:247`). Before this PR, `remove` was `lru.Remove` only. Scope the generation (per-key, or only on the sub cache) before merging.

## medium

**3. Unsynchronized read of `nats.Conn.Opts`.** `cmd/main.go:308` and `:316` read `raw.Opts.ReconnectedCB` / `raw.Opts.DisconnectedErrCB` directly. nats.go guards those fields with `nc.mu` (`SetReconnectHandler` locks; `doReconnect` reads under lock), so this is `-race`-visible. Also, a disconnect between `natsutil.Connect` and line 307 is silently missed. Better: a chaining option in `pkg/natsutil/connect.go:84-93`, set at connect time.

**4. Cache key duplicated by hand.** `account + "\x00" + roomID` appears at `readcache.go:312` and `:339`, with a comment asking readers to keep them identical. Extract `subKey(account, roomID string) string`.

**5. Dead sentinel + contradictory docs.** `readcache.go:36` says "0 disables it", `:78` mentions "a nil sem", but `newTTLCache:92-94` always allocates, so `if c.sem != nil` (`:181`) can never be false. `config.go` ("0 takes the package default") is the accurate one. Delete the dead branch and fix both comments.

**6. Semaphore slot held across all retries.** Acquired once at `:181-196` and released by `defer` after the loop, so one leader can pin a slot for up to 5×`fetchTimeout` (~50s), on top of a possible 10s acquire wait. Acquire per attempt, or put one deadline over the whole `DoChan` body.

## low

**7.** `cmd/subinvalidate.go:81` subscribes non-queue to `chat.user.*.event.subscription.update`, so every replica JSON-decodes every account's events — including high-frequency `read` / `mute_toggled` — before the action filter at `:44`. Acceptable in principle, but the fan-in is worth a note.

**8.** `errors.New(...)` at `readcache.go:247` is anonymous; make it a package sentinel so callers can use `errors.Is` instead of matching text.

**9.** `newTTLCache` grew a bare positional `int`; `defaultMaxInflight` is then passed explicitly at `:395`, `:399`, `:504` where `0` already means the same thing. Thread `CacheOption` down instead.

## nitpick

**10.** `for attempt := 0; attempt < maxLoadAttempts; attempt++` (`:208`) never uses `attempt` — Go 1.25 allows `for range maxLoadAttempts`.

**11.** The doc block above `remove` (`readcache.go:98-110`) opens with "getOrLoad returns…", so godoc attributes it to `remove`. Pre-existing, but this PR tripled its size.

## Clean

Error wrapping is consistently `fmt.Errorf("short description: %w", err)` — no bare `err`, no `"error: %w"`. No `time.Sleep` synchronization, no unterminated goroutines, no string error comparison, no silently swallowed errors. `evictor` is correctly consumer-defined and unexported; `encoding/json` (not sonic) is the right codec for this path; config validates and fails fast; lock ordering between `c.mu` and `singleflight` is non-nesting and safe.

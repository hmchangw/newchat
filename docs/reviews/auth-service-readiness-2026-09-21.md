# auth-service — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `f2b606d` (base `main`)  
**Overall score:** 3.3 / 5 (baseline 2026-09-01: 3.2, Δ +0.1)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

auth-service is a small, stateless JWT-minting front door whose code is idiomatic and SAST-clean, but its client contract has drifted from its behaviour in ways that overstate the security model. `docs/client-api.md` §2.2 says the JWT scope is "derived server-side from the principal's roles (admin > bot > user)"; the code never reads roles and every path mints the same `scoped_user` template, which `pkg/principal` openly documents as a not-yet-done follow-up. The same section documents only the SSO response shape, while the session-token path returns a NATS-encoded account with every directory field empty, and the dev-mode tokenless body that the frontend actually sends is absent from the request table. Three reviewers independently flagged that `BOTPLATFORM_URL` is re-declared with three different contracts across auth/upload/portal and is missing from auth-service's own compose and `.env.example`, so the README's documented local admin login ends in a 503. Two composition defects follow: dev-mode wiring hands the handler a nil `TokenValidator` that any request carrying `ssoToken` still dereferences (a panic caught by `gin.Recovery`, which itself bypasses the JSON log pipeline), and the request body is bound with no size cap on the one public unauthenticated endpoint. Session revocation by admin-service never reaches NATS, so a minted JWT stays valid up to 2h×1.1. Coverage is 61.9% because the 101-line `run()` inlines every config guard (signing-key type, account key validity, OIDC-required-unless-dev) with no test seam, while the handler half sits at 93%; the three auth branches triplicate the same mint tail, and `integration_test.go` is a unit test in disguise that proves nothing about the real NATS resolver.

| Dimension | Score |
|---|---|
| Code quality | 4 |
| Architecture | 4 |
| Test coverage | 2 |
| Maintainability | 3 |
| Integration | 3 |
| Performance | 4 |

**Findings by severity:** 0 critical, 5 high, 16 medium, 18 low, 10 nitpick (49 total).  
**Highest-risk dimension:** Test coverage (2).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 4

### Evidence

- [medium] Success-path and jitter-fallback logs are ctx-less, so they carry no request_id or trace correlation — `auth-service/handler.go:203`, `:262`, `:293`, `:125` — All four use `slog.Debug`/`slog.Error` instead of the `*Context` variants. The request ID lives only in the ctx (`pkg/ginutil/middleware.go:25`) and in the errcode logger (`handler.go:136`), so a successful mint logs an `account` with no `request_id`/`trace_id`, contrary to CLAUDE.md "include in all log lines"; `ginutil.AccessLog` (`middleware.go:67`) shows the correct `InfoContext` form.
- [medium] Panic recovery bypasses the JSON/OTLP log pipeline — `auth-service/main.go:115` — `gin.Recovery()` writes stack traces through gin's ANSI text logger to stderr, not `log/slog`. Same in all 9 Gin services (repo-wide convention), but for an auth front door an unparseable panic trace is an incident-time gap.
- [low] Key-derivation error discarded — `auth-service/main.go:63-65` — `signingKP.PublicKey()` error is folded into a generic message with no `%w`, so a genuine nkeys failure is reported as "not an account-type signing key". Violates "never ignore errors silently / always wrap".
- [low] All JSON-bind failures collapse to `missing_fields` / "natsPublicKey is required" — `auth-service/handler.go:139-142` — malformed JSON or wrong content-type yields the same envelope, which misleads client debugging; `ShouldBindJSON` distinguishes syntax errors from validation errors.
- [low] Session-branch account gate has a divergent, undocumented message and no reason — `auth-service/handler.go:249` vs `:190`/`:280` — "account contains invalid characters" differs from the sibling gates and from the documented row in `docs/client-api.md` (§2.2 error table lists only the "single NATS subject token" wording); none of the three carries a `WithReason`, so a client cannot branch on it.
- [low] Exported surface in `package main` — `auth-service/handler.go:27`, `:57`, `:63`, `:75-104`, `:135`, `:354` — `AuthHandler`, `NewAuthHandler`, `TokenValidator`, `BotplatformValidator`, `Option`, `With*`, `HandleAuth`, `HandleHealth` are exported though nothing imports a main package; CLAUDE.md says keep handler implementations unexported. Same drift in `tcard-service`/`portal-service`, so treat as repo-wide.
- [nitpick] Magic context key duplicated — `auth-service/handler.go:136` — `c.GetString("request_id")` re-spells ginutil's private key; `natsutil.RequestIDFromContext(c.Request.Context())` already holds the same value (`middleware.go:25`).
- [nitpick] Sentinel compared with `!=` rather than `errors.Is` — `auth-service/main.go:147` — correct today (ListenAndServe returns the bare sentinel) but off the repo's stated idiom.

### Recommendations

- [medium] Switch the four ctx-less log calls to `slog.DebugContext(ctx, …)` / `slog.ErrorContext` — `handler.go:125,203,262,293` — restores trace/identity correlation via the o11y handler; optionally add `"request_id", natsutil.RequestIDFromContext(ctx)` explicitly.
- [medium] Add a `ginutil.Recovery()` that uses `gin.CustomRecoveryWithWriter(io.Discard, fn)` and logs the panic with `slog.ErrorContext` + stack — `main.go:115` — one shared helper fixes all 9 Gin services; adopt here first.
- [low] Wrap the `PublicKey()` error: `return fmt.Errorf("derive signing key public key: %w", err)` and keep the account-type check as a separate branch — `main.go:63-65`.
- [low] Distinguish bind failures: on `*json.SyntaxError`/`*json.UnmarshalTypeError` return `BadRequest("malformed JSON body")`, keep `missing_fields` for validation — `handler.go:139`.
- [low] Extract the three account-format rejections into one package-level sentinel with a `WithReason(errcode.AuthInvalidAccount)` (add to `codes_auth.go`), use it at `:190`, `:249`, `:280`, and document the reason in `docs/client-api.md` §2.2.
- [low] Log a startup `slog.Warn` when `TLS_SKIP_VERIFY=true` in non-dev mode — `main.go:96-100` — mirrors the DevMode warning at `:88` so an insecure OIDC transport is visible in production logs.
- [nitpick] Unexport the handler type/ctor/options (`authHandler`, `newAuthHandler`, …) or leave as a repo-wide decision — `handler.go:63`.

## 3. Architecture — score 4

### Evidence

- [medium] Dev-mode wiring hands the handler a nil `TokenValidator`, but any request carrying `ssoToken` is still routed to it — `auth-service/main.go:89` + `auth-service/handler.go:323-338` — `NewAuthHandler(nil, …, true)` then `case req.SSOToken != "": h.handleSSO` calls `h.validator.Validate` on a nil interface: panic, caught by `gin.Recovery()` (`main.go:115`) as a 500 with no access-log line. The doc comment at `handler.go:299-302` promises "a dev-mode request carrying a token still validates normally", and the only test of that path (`handler_test.go:364-383`) injects a non-nil validator, so the real `main.go` composition is untested. Dev-only (`DEV_MODE=false` in prod), hence medium not high.
- [medium] The OIDC/botplatform config trio is re-declared per service instead of owned by `pkg/oidc`/`pkg/botauth` — `auth-service/main.go:34-36,43`, `upload-service/main.go:84-90`, `user-service/config/config.go:122-126,174` — and has already drifted: user-service reads `OIDC_TLS_SKIP_VERIFY`, the others `TLS_SKIP_VERIFY`; `BOTPLATFORM_URL` is optional here, `required,notEmpty` in upload/media, defaulted in portal. The "required when DEV_MODE is false" check is copy-pasted (`auth-service/main.go:91`, `upload-service/main.go:161`). `pkg/oidc.Config` (`pkg/oidc/oidc.go:147-154`) is the natural owner but carries no env tags — the CLAUDE.md shared-knob rule applied to a non-TTL knob.
- [low] `/readyz` is registered with zero checks, so it is byte-for-byte `/healthz` — `auth-service/routes.go:14` — while `docs/health-probes.md:11` defines readiness as NATS connectivity, which this service has none of. Process-only readiness is defensible for a stateless minting service, but it is undocumented and gives ops a probe that can never flip.
- [low] No per-request context deadline: sibling Gin services mount `ginutil.TimeoutConfig` (`portal-service/main.go:65,157`, `botplatform-service/main.go:124`, `tcard-service/main.go:43`); auth-service sets only socket timeouts (`auth-service/main.go:124-125`). The handler blocks on outbound OIDC JWKS refresh (10s client timeout, `pkg/oidc/oidc.go:165`) and botplatform validate; a stalled upstream outlives the 10s `WriteTimeout` with nothing cancelling the handler goroutine.
- [low] `handleSession` re-implements `botauth`'s error mapping — `auth-service/handler.go:396-407` duplicates `pkg/botauth/botauth.go:167-183` (`errors.As` → forward, else `Unavailable`+`WithCause`). Two places to change when the upstream contract shifts; `Authenticate` can't be reused because auth-service has no `x-user-id` to match.
- [nitpick] Handwritten `HandleHealth` sits beside the shared `health.ReadinessHandler` — `auth-service/routes.go:12-14`, `handler.go:522-524` — while `health.LivenessHandler()` (`pkg/health/health.go:43`) exists for exactly this.
- [nitpick] Doc drift: `CLAUDE.md:15` and `docs/architecture.md:300` describe auth as a "NATS callout service"; the service mints scoped user JWTs signed by an account signing key (`handler.go:490-497`) and nothing in the tree implements a callout.
- [nitpick] `auth-service/deploy/.env.example` omits `DEV_MODE`, `BOTPLATFORM_URL` and `NATS_JWT_EXPIRY_JITTER`, so the session-token branch is invisible to someone standing the service up from the template.

### Recommendations

- [medium] Close the nil-validator hole — `auth-service/handler.go:337-338` — either guard `if h.validator == nil { errhttp.Write(ctx, c, errcode.Unavailable("sso auth not configured", …)); return }` (mirroring the `bpValidator == nil` guard at `:390-394`) or wire a dev validator in `main.go`; add a unit test that constructs the handler exactly as `main.go:89` does (nil validator, devMode, `ssoToken` set) so the composition root is covered.
- [medium] Give the shared knobs one owner — add an env-tagged `oidc.EnvConfig{IssuerURL, Audiences, TLSSkipVerify}` with `Validate(required bool)` in `pkg/oidc` and a `botauth.Config{URL}` in `pkg/botauth`; mount them as named fields in auth/upload/user-service. Removes the name drift and the three copies of the DEV_MODE gate.
- [low] Mount `ginutil.TimeoutConfig` (`HTTP ginutil.TimeoutConfig` in config, `r.Use(cfg.HTTP.Middleware())` after `AccessLog`) — `auth-service/main.go:117` — so upstream stalls cancel the handler's context like every other Gin service.
- [low] Make readiness honest — `auth-service/routes.go:14` — either add a `health.Check` that the OIDC verifier's keyset loaded (or botplatform is reachable when configured), or state in `docs/health-probes.md` that auth-service readiness is process-only by design.
- [low] Extract `botauth.MapValidateError(err) error` and call it from both `Authenticate` and `handleSession` — `pkg/botauth/botauth.go:167-183`, `auth-service/handler.go:396-407`.
- [nitpick] Replace `HandleHealth` with `gin.WrapF(health.LivenessHandler())`, fix the "NATS callout" wording in `CLAUDE.md`/`docs/architecture.md`, and complete `deploy/.env.example`.

## 4. Test coverage — score 2

### Evidence

- [high] coverage below repo minimum 80%, currently 61.9% (109/176 statements) — `auth-service/main.go:53` — Every one of the 67 uncovered statements but 8 is in `run()` (59 statements, 0%). The handler/route half of the service is at 93% (109/117); the gap is structural, not a missing test case: `run()` inlines all config validation, handler construction, router/server wiring and shutdown, so none of it is reachable from a unit test without a live OIDC issuer, `obs.Init`, and a bound port.
- [high] Startup config guards are untested and untestable in place — `auth-service/main.go:59-68`, `:91-93` — The signing-key-type check, `AUTH_ACCOUNT_PUB_KEY` validity check and the "OIDC required when DEV_MODE=false" gate are the only things standing between a misconfigured deploy and minting unverifiable JWTs. All three are 0% and have no test seam (no `validateConfig`/`newHandler` function to call).
- [medium] `handleSession` error paths are unexercised (77.8%) — `auth-service/handler.go:240-244`, `:248-251`, `:257-260` — Three of the four error branches on the bot/admin lane have no test: principal with empty `Account` (must 401 `AuthInvalidToken`), an account that survives `subject.EncodeAccount` but still fails `IsValidAccountToken` (e.g. `*`/`>`/whitespace, must 400), and `signNATSJWT` failure (500, cause must not leak). The SSO lane has a dedicated test for each of the analogous cases (`handler_test.go:553`, `:571`, `:826`), so the session lane is objectively behind the standard the file already sets.
- [medium] `integration_test.go` is a unit test in disguise — `auth-service/integration_test.go:26-74` — It uses the in-memory `fakeValidator` and `httptest`, starts no container, and is a byte-for-byte subset of `TestHandleAuth_ValidToken` (`handler_test.go:93`). It proves nothing the unit suite does not. The one property only an integration test can establish — that a NATS server running the operator/account resolver with the scoped signing-key template actually accepts the minted `account:<x>`-tagged JWT and applies the `chat.user.{account}.>` grant — is untested anywhere in the repo (`SetScoped`/`IssuerAccount` appear only in `auth-service/*_test.go`, which only decode the JWT). `deploy/keycloak/realm-export.json` exists but no test consumes it either.
- [low] Unit router omits the production middleware chain — `auth-service/handler_test.go:85-90` — `setupRouter` registers routes on a bare `gin.New()`, so `c.GetString("request_id")` at `handler.go:136` is always `""` in tests and the request-ID → `errcode.WithLogValues` → error-log correlation path is never verified for this service. (`pkg/ginutil` tests the middleware in isolation; the composition is what is unverified.)
- [low] Hand-rolled fakes instead of `mockgen` — `auth-service/handler_test.go:32`, `:633` — CLAUDE.md §4 says mock with `go.uber.org/mock`. Both interfaces are single-method and the fakes are small, so this is defensible, but `fakeBPValidator` already carries call-count/last-arg bookkeeping that `gomock` would provide with `Times()`/argument matchers, and there is no `//go:generate` directive to keep it honest if `TokenValidator`/`BotplatformValidator` grow.
- [nitpick] Table-driven structure is used only where inputs vary (`handler_test.go:199`, `:490`, `:511`, `:571`) — 7 `t.Run` sites across 29 top-level tests. The many `TestHandleAuth_SessionToken_*` / `TestHandleAuth_DevMode_*` functions repeat the same 12-line request/recorder boilerplate; a per-lane table would halve the file and make missing cases (finding 3) visible by omission.

### Recommendations

- [high] Extract `validateConfig(cfg config) error` (main.go:59-68, :91-93) and `newHandler(ctx, cfg, kp nkeys.KeyPair) (*AuthHandler, error)` (main.go:77-106) from `run()`, keep `run()` as pure wiring, and add table-driven tests for each guard (user-key seed, non-account pubkey, prod mode without issuer/audiences, `BOTPLATFORM_URL` set/unset) — brings the tree to roughly 85% and puts the deploy-safety guards under test for the first time.
- [high] Add the three missing `handleSession` cases (`handler.go:240`, `:248`, `:257`) mirroring the existing SSO-lane tests; assert reason code and that the 500 body does not contain "generating NATS token".
- [medium] Make `integration_test.go` earn its build tag: run `testutil.NATS`-style container in operator mode with the scoped signing-key template from `docker-local/setup.sh`, mint a JWT through the handler, and assert `nats.Connect` with that JWT succeeds and can `Publish` on `chat.user.<account>.x` but is denied on `chat.user.other.x`. If a JetStream/operator-mode helper is out of scope, delete the file rather than keep a duplicate that inflates the "integration" signal.
- [medium] Have `setupRouter` apply `ginutil.RequestID()` (and optionally `AccessLog`) and add one test that sends `X-Request-ID` and asserts it is echoed on the response and appears in the captured error log line for a 401 — verifies `handler.go:136` end-to-end.
- [low] Collapse the per-lane `TestHandleAuth_SessionToken_*` and `_DevMode_*` functions into two table-driven tests with a shared `doAuth(t, router, body) (*httptest.ResponseRecorder)` helper — reduces boilerplate and makes lane parity reviewable.
- [nitpick] Add `//go:generate mockgen` for `TokenValidator`/`BotplatformValidator` into a `mock_validator_test.go`, or leave a one-line comment on each fake stating the mockgen exemption is deliberate.

## 5. Maintainability — score 3

### Evidence

- [high] Service is below the 80% coverage floor (61.9%) because all wiring lives in one untestable 101-line `run()` — `auth-service/main.go:53-153` — `run()` parses config, validates both nkeys (59-68), decides the OIDC-vs-dev handler (87-106), builds the middleware stack (108-118) and runs the server; `cov-func.txt` shows `main`/`run` at 0.0% while every `handler.go` function is 78-100%. The config cross-field rule (`OIDC_ISSUER_URL`/`OIDC_AUDIENCES` required unless `DEV_MODE`, line 91) and the nkey-type checks are real logic with no test, and it is the only function in the service over ~80 lines.
- [medium] The three auth branches triplicate the same mint tail — `auth-service/handler.go:189-216`, `:247-272`, `:279-302` — each repeats validate-account-token → `obs.ContextWithIdentity` → `c.Request.WithContext` → `errcode.WithLogValues` → `signNATSJWT` → identical `"generating NATS token: %w"` write → `c.JSON(authResponse{...})`. Adding a claim to the JWT, a response field, or a log attribute is a three-site edit and the branches already drift (SSO/dev tag `account`, session tags `account`+`nats_account`).
- [medium] Deploy config has drifted from the config struct — `auth-service/deploy/.env.example:1-26` and `auth-service/deploy/docker-compose.yml:13-22` — neither mentions `BOTPLATFORM_URL` or `NATS_JWT_EXPIRY_JITTER` (declared at `main.go:31,43`), and `.env.example` omits `DEV_MODE`; `grep -rn BOTPLATFORM_URL docker-local/ auth-service/deploy/ docs/*.md` returns nothing. The session-token branch (the newest feature) cannot be enabled locally from the documented knobs; `.env.example:1` also claims it is "Generated by setup.sh", which lives in `docker-local/`, not here.
- [medium] `parseDescription` is duplicated across services with diverging arity — `auth-service/handler.go:340-352` (3 returns) vs `upload-service/middleware.go:247-256` (2 returns) — both split the OIDC `description` claim `"employeeId, engName, chineseName"`. The claim format is owned by `pkg/oidc.Claims` (`pkg/oidc/oidc.go:221-234`); a format change now needs two service edits and can silently disagree.
- [low] Constructor mixes five positional params (including a bare `devMode bool`) with functional options — `auth-service/handler.go:104` — `NewAuthHandler(validator, signingKey, accountPubKey, jwtExpiry, devMode, opts...)`; every new dependency changes the signature at `main.go:89,105` and all 28 tests, while `bpValidator`/jitter already go through `Option`. Boolean-trap call sites (`..., 2*time.Hour, false)`) are unreadable without the signature.
- [low] The `integration` build tag guards a test that uses no container — `auth-service/integration_test.go:26-74` — it is `httptest` + `fakeValidator` only (same as `TestHandleAuth_ValidToken`, `handler_test.go:93`) yet is excluded from `make test` and pays `testutil.RunTests` setup (`:76`). It is dead weight that also hides the fact that the OIDC path has no real-issuer test (Keycloak realm export sits unused in `deploy/keycloak/`).
- [low] Dead/undelivered contract surface: `errcode.AuthInvalidRequest` (`pkg/errcode/codes_auth.go:11`) is declared "emitted by auth-service" but no non-errcode file references it; `handleSession` logs `p.Roles` (`handler.go:262`) and does nothing with them while `pkg/principal/scope.go:8-15` documents role-scoped signing keys as a "planned follow-up" — TODO debt parked in a doc comment rather than tracked.
- [low] Bind failure always reports `natsPublicKey is required` / `missing_fields` — `auth-service/handler.go:139-143` — `ShouldBindJSON` also fails on malformed JSON, so a syntax error is reported as a missing field; the table at `handler_test.go:199-225` only checks the code, not the reason, so the mislabel is untested.
- [nitpick] Two probe styles in a 15-line router — `auth-service/routes.go:13-14` — `/healthz` is a hand-rolled `HandleHealth` (`handler.go:354`) while `/readyz` is `health.ReadinessHandler(5*time.Second)` with zero checks (always 200); the 5s timeout suggests something is probed. Use `health.LivenessHandler()` for both and drop the custom method, or comment why readiness is a no-op here.
- [nitpick] History comment instead of why — `auth-service/handler.go:167-168` — "Behavior unchanged from the pre-extension code path" describes a past diff, not the current intent. The grant table at `:305-321` is self-described as "documentation only" and must be mirrored in `docker-local/setup.sh:68-74` and `docs/client-api.md §2.1` — three hand-synced copies.

### Recommendations

- [high] Split `run()` into `validateConfig(cfg) error`, `newHandler(ctx, cfg) (*AuthHandler, error)` and `newRouter(h, sdk) *gin.Engine` — `auth-service/main.go:53-153` — table-test the config rules and nkey checks; this alone lifts the tree above the 80% floor without touching handler logic.
- [medium] Collapse the three tails into one `(h *AuthHandler) mint(ctx, c, natsPubKey, account, natsAccount string, info userInfoResp)` — `auth-service/handler.go:189-216, 247-272, 279-302` — the branches then only differ in how they obtain the principal, which is the actual variation.
- [medium] Move description parsing to `pkg/oidc` (e.g. `func (c *Claims) DirectoryFields() (employeeID, engName, chineseName string)`) and delete both service copies — `auth-service/handler.go:340`, `upload-service/middleware.go:247`.
- [medium] Add `BOTPLATFORM_URL`, `NATS_JWT_EXPIRY_JITTER` and `DEV_MODE` to `deploy/.env.example` and `deploy/docker-compose.yml`, and fix the "Generated by setup.sh" header — `auth-service/deploy/.env.example:1`, `docker-compose.yml:13-22`.
- [low] Make `devMode` and `validator` options (`WithDevMode()`, `WithOIDCValidator(v)`) so `NewAuthHandler(signingKey, accountPubKey, jwtExpiry, opts...)` stops changing shape — `auth-service/handler.go:104`.
- [low] Either drop `integration_test.go` or make it a real OIDC round-trip against the bundled Keycloak realm via a `pkg/testutil` container — `auth-service/integration_test.go:26`.
- [low] Remove `errcode.AuthInvalidRequest` or wire it to the malformed-JSON bind case, and assert the reason in `TestHandleAuth_MissingFields` — `pkg/errcode/codes_auth.go:11`, `auth-service/handler.go:139`.

## 6. Integration — score 3

### Evidence

- [high] `docs/client-api.md` §2.2 claims the JWT scope is "derived server-side from the principal's roles (admin > bot > user)"; the code never reads `Principal.Roles` — every path mints the same `scoped_user` template with only an `account:` tag — `auth-service/handler.go:247-256`, `docs/client-api.md:205` — `pkg/principal/scope.go:11-17` explicitly says role-scoped keys are a not-yet-done follow-up. A security-relevant statement in the client contract is false; readers will assume bots/admins get narrower or wider grants than they do.
- [high] §2.2 success-response table describes only the SSO shape; the session-token path returns `user.account` in NATS-encoded form (`name.shortcode.bot` → `name_shortcode_bot`) with `email`/`employeeId`/`engName`/`chineseName`/`deptName`/`deptId` all empty — `auth-service/handler.go:263-272`, `docs/client-api.md:218-227` — the table says `user.account` is "derived from `preferred_username`" and `user.email` is "OIDC email claim", neither true for bots/admins; the dot→underscore rewrite is only inferable from §2.1's "use verbatim" note.
- [medium] The dev-mode tokenless body `{account, natsPublicKey}` is a live client contract (`chat-frontend/src/context/NatsContext/NatsContext.jsx:213`, `docker-local/README.md:541-545`) but §2.2's request table has no `account` field and states "neither → 400 missing_token" unconditionally — `auth-service/handler.go:36,159-160`, `docs/client-api.md:196-202`.
- [medium] `BOTPLATFORM_URL` is re-declared with three different contracts — auth-service optional/branch-disabling (`auth-service/main.go:43`), upload-service `required,notEmpty` (`upload-service/main.go:90`), portal-service defaulted to `http://botplatform-service:8080` (`portal-service/main.go:44`) — and auth-service's compose is the only consumer that does not pass it (`auth-service/deploy/docker-compose.yml:13-22` vs `upload-service/deploy/docker-compose.yml:35`). Net effect: the README's documented password login (`docker-local/README.md:546-548` → portal `/api/v1/login` → `POST /api/v1/auth {authToken}`) ends in `503 upstream_unavailable` on a fresh local stack (`handler.go:222-226`). CLAUDE.md's shared-knob rule says the tag belongs once in the owning package (`pkg/botauth`).
- [medium] Session revocation does not reach NATS: admin-service deactivate / password reset / revoke-all delete botplatform sessions and bust `sessioncache` (`admin-service/handler.go:483,591-599,697-707`), but a NATS JWT already minted stays valid up to `2h × 1.1` jitter (`auth-service/handler.go:322-336`, `main.go:30-31`) and nothing in the repo uses account revocation lists or a `$SYS` disconnect. The session-mode refresh re-sends the same `authToken` (`chat-frontend/src/context/NatsContext/useJwtRefresh.js:98-100`), so lockout lands at the next re-mint (~80% of lifetime). Acknowledged only in `docs/superpowers/specs/2026-06-11-portal-service-design.md:371-373`, not in §2.2 or the admin §9 deactivate docs.
- [medium] Derived view drift: `docs/client-api/request-reply.md:57` still says "Exchanges an SSO token for a signed NATS user JWT" while canonical §2.2 covers `ssoToken | authToken` — CLAUDE.md says the derived views must never drift from `client-api.md`.
- [low] Session path with an empty `Principal.Account` replies reason `invalid_sso_token` (`errcode.AuthInvalidToken`) — `auth-service/handler.go:240-243` — §2.2 documents `invalid_token` for session-token failures and §6 tells clients to redirect to SSO re-login on `invalid_sso_token`; wrong branch for a bot/admin caller, and the case is absent from the error table.
- [low] §6 catalog lists `invalid_request` as emitted by auth-service (`docs/client-api.md:6927`) but `errcode.AuthInvalidRequest` has zero call sites repo-wide; body-parse failures map to `missing_fields` (`handler.go:139-142`).
- [low] `/readyz` is registered with zero probes so it can never return 503 (`auth-service/routes.go:14`, `pkg/health/health.go:52-66`), and `/healthz` is a hand-rolled `gin.H{"status":"ok"}` (`handler.go:354-356`) instead of `health.LivenessHandler`. Neither probe is in `client-api.md` although CLAUDE.md says every auth-service route must be, and other services do document `GET /healthz` (`docs/client-api.md:8997`).

### Recommendations

- [high] Rewrite `docs/client-api.md:205` to state that roles are currently ignored and all principals receive the `scoped_user` template (§2.1 grants), or implement the role-scoped signing keys the comment in `pkg/principal/scope.go:14-17` promises — the doc must not overstate the security model.
- [high] Add a "session-token response" subsection to §2.2: `user.account` is `subject.EncodeAccount(principal.Account)` (dots → `_`), all directory fields empty; add the dev-mode `account` request field with a "DEV_MODE only" note — `docs/client-api.md:196-227`.
- [medium] Move `BOTPLATFORM_URL` into a `botauth.Config` struct mounted by every consumer, and add `BOTPLATFORM_URL=${BOTPLATFORM_URL:-http://botplatform-service:8080}` to `auth-service/deploy/docker-compose.yml` plus `.env.example` so the documented local admin login works out of the box.
- [medium] Document the JWT-lifetime revocation window in §2.2 and the admin deactivate/password-reset sections; if immediate lockout is required, shorten `NATS_JWT_EXPIRY` for session-token principals or add NATS account revocation on deactivate.
- [medium] Fix the one-line summary in `docs/client-api/request-reply.md:57` to match canonical §2.2.
- [low] Return `errcode.BotplatformInvalidToken` (reason `invalid_token`) at `handler.go:241` and add the row to the §2.2 error table; delete the stale `invalid_request` row at `docs/client-api.md:6927` or remove the unused constant.
- [low] Wire `/readyz` with a cheap OIDC-issuer probe (or drop the route to avoid a misleading always-green readiness) and use `health.LivenessHandler` for `/healthz` — `routes.go:13-14`.

## 7. Performance — score 4

### Evidence

- [medium] `POST /api/v1/auth` reads an unbounded request body — `auth-service/handler.go:139` — `c.ShouldBindJSON(&req)` decodes straight from `c.Request.Body` with no `http.MaxBytesReader`; the server sets only `ReadTimeout`/`WriteTimeout` (`auth-service/main.go:124-125`, default 1 MiB `MaxHeaderBytes` does not cover bodies). A legitimate body is a few KB (two tokens + a 56-char nkey), yet a client can stream tens of MB into `ssoToken` and each such request holds it in heap for up to 10 s. The sibling Gin service already caps bodies (`botplatform-service/bot_handlers.go:193`).
- [medium] Botplatform validator keeps only 2 keep-alive connections under a 64-way burst — `auth-service/main.go:79` — `restyutil.New("", restyutil.WithTimeout(5*time.Second))` inherits the cloned `DefaultTransport` (`pkg/restyutil/restyutil.go:61`, `MaxIdleConnsPerHost=2`), while `botauth.Validator` allows `maxInFlight=64` concurrent upstream validations (`pkg/botauth/botauth.go:42`). A bot/admin login burst therefore pays a fresh TCP(+TLS) handshake for every validation beyond two in flight. `media-service/main.go:112` and `upload-service/main.go:191` size the same client with `WithMaxIdleConns(32)`.
- [medium] Unknown-`kid` SSO tokens each force a synchronous JWKS round trip to the issuer — `pkg/oidc/oidc.go:234` → go-oidc `jwks.go:160-176` (v3.17.0) — on a cache miss `verify` falls through to `keysFromRemote`, coalesced only among *concurrent* callers; there is no negative cache or refetch rate limit. `/api/v1/auth` is unauthenticated, so a forged-`kid` spray (or a fleet retrying stale tokens after a Keycloak key rotation) converts every request into an issuer fetch bounded only by the 10 s client timeout (`pkg/oidc/oidc.go:165,179`). Legitimate tokens keep hitting the cache first (`jwks.go:161-167`), so latency for good logins is unaffected — the cost lands on Keycloak's JWKS endpoint and on this service's goroutine/conn budget.
- [low] No per-request deadline on the handler context, and `WriteTimeout` equals the OIDC client timeout — `auth-service/main.go:108-117,124-125` — unlike `botplatform-service/config.go:31`, `tcard-service/main.go:43`, `portal-service/main.go:65`, auth-service does not mount `ginutil.TimeoutConfig`. Outbound calls are bounded on their own (OIDC 10 s `pkg/oidc/oidc.go:179`; botauth 10 s + resty 5 s `pkg/botauth/botauth.go:36`), so nothing hangs, but a JWKS refetch that runs to its 10 s limit returns after the 10 s `WriteTimeout` has expired and the 401/503 is dropped on the floor as a reset.
- [low] Kubelet probes are access-logged at Info — `pkg/ginutil/middleware.go:62-76`, mounted globally at `auth-service/main.go:117` with `/healthz` and `/readyz` registered under the same chain (`auth-service/routes.go:13-14`). The o11y middleware skips those paths for spans (`o11y/gin/options.go:59-67`), `AccessLog` does not; every probe costs a JSON log line plus `ClientIP()` parsing. Steady noise, not a hot spot.
- [nitpick] `cryptoRandFloat` allocates two `big.Int`s per login — `auth-service/handler.go:121-129` — reading 8 bytes from `crypto/rand` and masking to 53 bits gives the same uniform `[0,1)` with zero allocations. Microseconds either way.
- [nitpick] `pkgoidc.Validate` builds a `Claims.Extra` map the handler never reads — `pkg/oidc/oidc.go:250-278` vs `auth-service/handler.go:204-215` — per-login map decode plus a 20-key delete loop for an unused field. Shared package; cost is microseconds.

### Recommendations

- [medium] Wrap the body before binding: `c.Request.Body = http.MaxBytesReader(c.Writer, c.Request.Body, 16<<10)` (or a `ginutil` helper mirroring `botplatform-service/bot_handlers.go:192-197`) — `auth-service/handler.go:138-143` — bounds per-request heap on the one public unauthenticated endpoint; map the `MaxBytesError` to the existing `AuthMissingFields` 400.
- [medium] Add `restyutil.WithMaxIdleConns(64)` (match `botauth.maxInFlight`) to the botplatform client — `auth-service/main.go:79` — removes per-request handshakes during bot-login bursts; same fix already applied in media-service and upload-service.
- [medium] Put a small guard around the kid-miss path in `pkg/oidc` — either a short negative cache keyed on `kid` (e.g. 30–60 s) or a token-bucket on `keysFromRemote` triggered from `Validate` (`pkg/oidc/oidc.go:231-241`) — turns a forged-`kid` spray from N issuer fetches into ~1 per window while still honouring genuine key rotation on the first miss.
- [low] Mount `ginutil.TimeoutConfig` (`REQUEST_TIMEOUT`, default 10 s) as the other Gin services do, and raise `WriteTimeout` to `REQUEST_TIMEOUT + 2s` (or lower the OIDC client timeout below it) — `auth-service/main.go:108-126` — so a slow issuer produces a real error envelope rather than a socket reset.
- [low] Skip `/healthz`/`/readyz` in `AccessLog` (reuse `o11ygin.DefaultSkipPaths()`), or register the probes on a group without it — `auth-service/main.go:117`, `auth-service/routes.go:13-14`.
- [nitpick] Replace the `big.Int` draw with `rand.Read` into `[8]byte` + `binary.BigEndian.Uint64(...) >> 11` — `auth-service/handler.go:121-129`.

## 8. Prioritized action list

Ordered by severity (`critical` first), then by the dimension most likely to carry a correctness or contract cost (integration, architecture, coverage, code quality, performance, maintainability). Each item is a recommendation from the chapter named; the `file:line` and rationale are quoted from it.

1. **[high]** _Integration_ — Rewrite `docs/client-api.md:205` to state that roles are currently ignored and all principals receive the `scoped_user` template (§2.1 grants), or implement the role-scoped signing keys the comment in `pkg/principal/scope.go:14-17` promises — the doc must not overstate the security model.
2. **[high]** _Integration_ — Add a "session-token response" subsection to §2.2: `user.account` is `subject.EncodeAccount(principal.Account)` (dots → `_`), all directory fields empty; add the dev-mode `account` request field with a "DEV_MODE only" note — `docs/client-api.md:196-227`.
3. **[high]** _Test coverage_ — Extract `validateConfig(cfg config) error` (main.go:59-68, :91-93) and `newHandler(ctx, cfg, kp nkeys.KeyPair) (*AuthHandler, error)` (main.go:77-106) from `run()`, keep `run()` as pure wiring, and add table-driven tests for each guard (user-key seed, non-account pubkey, prod mode without issuer/audiences, `BOTPLATFORM_URL` set/unset) — brings the tree to roughly 85% and puts the deploy-safety guards under test for the first time.
4. **[high]** _Test coverage_ — Add the three missing `handleSession` cases (`handler.go:240`, `:248`, `:257`) mirroring the existing SSO-lane tests; assert reason code and that the 500 body does not contain "generating NATS token".
5. **[high]** _Maintainability_ — Split `run()` into `validateConfig(cfg) error`, `newHandler(ctx, cfg) (*AuthHandler, error)` and `newRouter(h, sdk) *gin.Engine` — `auth-service/main.go:53-153` — table-test the config rules and nkey checks; this alone lifts the tree above the 80% floor without touching handler logic.
6. **[medium]** _Integration_ — Move `BOTPLATFORM_URL` into a `botauth.Config` struct mounted by every consumer, and add `BOTPLATFORM_URL=${BOTPLATFORM_URL:-http://botplatform-service:8080}` to `auth-service/deploy/docker-compose.yml` plus `.env.example` so the documented local admin login works out of the box.
7. **[medium]** _Integration_ — Document the JWT-lifetime revocation window in §2.2 and the admin deactivate/password-reset sections; if immediate lockout is required, shorten `NATS_JWT_EXPIRY` for session-token principals or add NATS account revocation on deactivate.
8. **[medium]** _Integration_ — Fix the one-line summary in `docs/client-api/request-reply.md:57` to match canonical §2.2.
9. **[medium]** _Architecture_ — Close the nil-validator hole — `auth-service/handler.go:337-338` — either guard `if h.validator == nil { errhttp.Write(ctx, c, errcode.Unavailable("sso auth not configured", …)); return }` (mirroring the `bpValidator == nil` guard at `:390-394`) or wire a dev validator in `main.go`; add a unit test that constructs the handler exactly as `main.go:89` does (nil validator, devMode, `ssoToken` set) so the composition root is covered.
10. **[medium]** _Architecture_ — Give the shared knobs one owner — add an env-tagged `oidc.EnvConfig{IssuerURL, Audiences, TLSSkipVerify}` with `Validate(required bool)` in `pkg/oidc` and a `botauth.Config{URL}` in `pkg/botauth`; mount them as named fields in auth/upload/user-service. Removes the name drift and the three copies of the DEV_MODE gate.


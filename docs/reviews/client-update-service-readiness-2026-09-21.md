# client-update-service — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `bf684cf` (base `main`)  
**Overall score:** 3.2 / 5 (baseline 2026-09-01: 3.3, Δ -0.1)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

client-update-service distributes the desktop client's binaries to the whole fleet, and it is the weakest link in that chain in ways that matter more than its size suggests. Nothing verifies the integrity of what it serves: no checksum, no signature, no digest header, and the download route is unauthenticated, so a client cannot tell a tampered executable from a good one and no record exists of which bytes were published or by whom. The read timeout is hardcoded at thirty seconds while the service advertises a two-gigabyte upload cap, and Go's read timeout bounds the whole request body — so admin-service's documented ten-minute relay budget is silently capped, and any artifact needing more than about a hundred megabytes of transfer is severed mid-body. This was flagged in the previous audit and is still unfixed. Publication is not atomic either: the two objects are written by independent uploads with no locking, so a mid-pair failure or two concurrent uploads leave a mismatched descriptor and executable, and both callers get a success. Cache invalidation is process-local with a twenty-four-hour default, so on more than one replica the documented "a re-upload busts the cache" claim holds only on the pod that received the upload, and a client can be handed a new descriptor with an old binary. On the download path there is no conditional-request support, no range support and no cache directives, so every startup poll re-transfers the whole body, no proxy can absorb a release herd, and an interrupted download restarts from zero. The singleflight fill runs on the first requester's context, so one client hanging up fails every concurrent waiter. Coverage is 76.8% and the entire gap is `run`, which is where the read timeout lives.

| Dimension | Score |
|---|---|
| Code quality | 4 |
| Architecture | 3 |
| Test coverage | 2 |
| Maintainability | 4 |
| Integration | 3 |
| Performance | 3 |

**Findings by severity:** 0 critical, 10 high, 21 medium, 13 low, 3 nitpick (47 total).  
**Highest-risk dimension:** Test coverage (2).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 4

### Evidence

- [high] `MINIO_DOWNLOAD_TIMEOUT` is re-declared with its own `envDefault` in two services — `client-update-service/config.go:24` (also `upload-service/main.go:101`) — CLAUDE.md §Configuration: "A knob shared by more than one service is declared once, in the package that owns the thing it configures… Never re-declare the env tag and `envDefault` in a service." Both currently say `5m`, so there is no live divergence, but `pkg/minioutil` exposes no config type at all (`grep 'env:' pkg/minioutil/*.go` → no hits), so nothing prevents one side drifting. This is the exact failure mode the rule exists to stop.
- [medium] `contentDisposition` duplicates an existing helper with weaker behaviour — `client-update-service/version.go:205-207` vs `upload-service/handler.go:485-491` — same function name, same purpose, two implementations. This one is `fmt.Sprintf("attachment; filename=%q", fileName)`; Go's `%q` leaves printable non-ASCII as raw UTF-8, which RFC 6266 forbids in a quoted-string (must be ISO-8859-1), so `réport.exe` reaches the client mojibake'd. upload-service emits RFC 5987 `filename*=UTF-8''…` and has table tests for it (`upload-service/handler_test.go:704`). No injection risk — `%q` escapes CR/LF — so this is correctness/consistency, not security.
- [medium] Access-log lines are emitted with no trace correlation — `client-update-service/middleware.go:85` — `slog.Info(...)` passes `context.Background()` to the handler chain, and the o11y SDK derives `trace_id`/`span_id` solely from the context (`flywindy/o11y@v0.12.0/internal/log/handler.go:70-71`, `trace.SpanFromContext(ctx)`). Every request line therefore lands uncorrelated, against the CLAUDE.md observability contract ("all trace-correlated"). `c.Request.Context()` already carries the o11ygin span (installed at `main.go:70`, before this middleware at `:73`); the repo convention is `slog.InfoContext` (`pkg/ginutil/middleware.go:67`, `media-service/middleware.go:50`, `botplatform-service/middleware.go:27`). One-token fix.
- [medium] Request-ID and access-log middleware are hand-rolled instead of `pkg/ginutil` — `client-update-service/middleware.go:67-94` — `pkg/ginutil` is documented as "the serving-layer toolkit shared by the HTTP services: request-ID, access log and CORS middleware" and seven services use `ginutil.RequestID()`/`AccessLog()` (auth, user, portal, admin, tcard, botplatform, teams-room-inspector). The local copy bypasses `idgen.ResolveRequestID` — the stated "single owner of mint-vs-pass-through policy" (`pkg/idgen/idgen.go:179`) — and so silently discards a malformed inbound `X-Request-ID` where `ginutil.RequestID` logs a Warn, losing the signal that a caller is sending bad correlation IDs.
- [low] `err != http.ErrServerClosed` instead of `errors.Is` — `client-update-service/main.go:101` — works today because `ListenAndServe` returns the sentinel unwrapped, but the repo majority (11 of 15 non-tool sites, e.g. `admin-service/main.go:174`, `media-service/main.go:143`) uses `errors.Is`, which is what CLAUDE.md §Error Handling asks for and what survives a future wrap.
- [nitpick] Four bare `return err` pass-throughs — `cache.go:77`, `cache.go:87`, `version.go:167`, `store_minio.go:96` — all are internal hand-offs where the callee already wrapped (`store_minio.go:41,48,50`) and the caller re-wraps (`version.go:152,188`), so no context is actually lost. Flagged only so a reviewer does not re-derive it.

### Recommendations

- [high] Move `MinioDownloadTimeout` into a shared `minioutil.Config` (or equivalent named field, following `mongoutil.PoolConfig`) and mount it in both services — `config.go:24`, `upload-service/main.go:101` — removes the only CLAUDE.md MUST violation in the service.
- [medium] Delete the local `contentDisposition` and lift upload-service's RFC 5987 version into a shared helper used by both — `version.go:205` — fixes non-ASCII filenames and kills a name collision that reads as "same function" but is not.
- [medium] Change `slog.Info` → `slog.InfoContext(c.Request.Context(), …)` — `middleware.go:85` — restores `trace_id`/`span_id` on every access-log line for a one-word edit. upload-service/middleware.go:76 shares the defect.
- [medium] Replace `requestIDMiddleware` with `ginutil.RequestID()` and keep only the access log local (or extend `ginutil.AccessLog` with an optional extra-attrs hook) — `middleware.go:67`, `main.go:72-73` — restores the malformed-inbound Warn.
- [low] Use `errors.Is(err, http.ErrServerClosed)` — `main.go:101`.
- [low] Promote `"request_id"` to a named const beside `ctxServiceAccount` — `middleware.go:19,72,86`

### Reviewer notes

- SAST folded in as instructed: `gosec` medium+/confidence-low is clean repo-wide and the 20 repo-owned semgrep rules report 0 findings, so there is **no SAST finding under `client-update-service/` to escalate**. `govulncheck` and the semgrep registry packs (`p/golang`, `p/security-audit`) were blocked by the sandbox egress policy (403) and were NOT retried — dependency-vulnerability and registry-rule coverage for this service is therefore UNVERIFIED, not clean. I invented no CVE findings.
- Verified locally and cleanly: `go vet ./client-update-service/...` → exit 0; `go build ./client-update-service/...` → exit 0.
- Positives that held up under reading and are not re-litigated above: `pkg/errcode` tiering is correct throughout (Tier-1 constructors in handlers, exactly one Tier-2 adapter — `errhttp.Write` — and no log-AND-return anywhere); infra failures are returned as raw wrapped `fmt.Errorf` so they collapse to `internal` (`version.go:152,188`); `errors.Is`/`errors.As` are used for sentinel and `*http.MaxBytesError` checks (`version.go:51,148,184`), never string compares; no token ever reaches a log or an error string (`config.go:114-133` names accounts only, asserted by `config_test.go:103` and `middleware_test.go:157`); comments explain WHY, not WHAT.
- Unit coverage for this tree is 76.8% (given, not re-derived) — below the 80% floor, but that is the test-coverage dimension's finding, not scored here. The one gap that touches this dimension is that `contentDisposition`'s output encoding is only partially asserted (`version_test.go:201,243` use `Contains`), which is why the `%q` defect survived.

## 3. Architecture — score 3

### Evidence

- [high] MinIO knobs re-declared per service, against CLAUDE.md §6 "declared once, in the package that owns the thing it configures" — `client-update-service/config.go:19-24` — the same five `MINIO_*` tags exist in `media-service/config.go:70-74` and `upload-service/main.go:95-101`, and have already drifted: `MINIO_BUCKET` is `required` here, `envDefault:"avatars"` in media-service, bare in upload-service. `pkg/minioutil` exposes no Config to mount, so every new consumer repeats it.
- [high] Cache invalidation is process-local, so the published contract "a re-upload of the same name busts the cache" holds only on the replica that received the upload — `version.go:113`, `cache.go:53-61`, `docs/client-api.md:8981` (and `:8919`). `blobCache` is an in-process LRU with no shared invalidation channel (the service touches no NATS) and no revalidation against MinIO — the bucket is the source of truth but an entry is never re-`Stat`ed. With N>1 replicas, N-1 pods serve the superseded artifact for up to `CACHE_TTL` (default 24h, `config.go:50`), and since descriptor and executable are cached independently a client can get a new `.yaml` with an old `.exe`.
- [high] `ReadTimeout` hardcoded at 30s while the service advertises a 2 GiB upload cap — `main.go:80` vs `config.go:47`. `http.Server.ReadTimeout` bounds the whole request body, and nothing here extends it per request (no `http.NewResponseController` in the tree), unlike `admin-service/client_update.go:401-410` on the relaying side. Only `WriteTimeout` is configurable, so admin-service's documented 10m relay budget (`docs/client-api.md:8309`) is silently capped at 30s of transfer.
- [medium] Nothing verifies the integrity of what this software-distribution endpoint serves — `version.go:97-115`, `store.go:22-28`. `Put` records no checksum or signature, `blobInfo` carries none, no `ETag`/`Digest` header reaches the client, and the design doc lists "checksums/signatures" as out of scope (`docs/superpowers/specs/2026-08-03-client-update-service-design.md:313`). The only integrity boundary is the upload token plus bucket ACLs; a downloader (unauthenticated, `routes.go:17`) cannot tell a tampered executable from a good one.
- [medium] No version identity, and publication is not atomic — `version.go:84-91`. Despite the name there is no version-comparison logic at all (grep finds only the token compare, `middleware.go:42`); "a version" is two mutable object names under a fixed prefix (`version.go:21`). The two `storeFormFile` calls are independent `Put`s with no locking, so a mid-pair failure or two concurrent uploads leave a mismatched pair and both callers get 200. Documented (`docs/client-api.md:8940-8948`), but it means no rollback, no immutable release, no coherence signal.
- [low] `blobCache.gen` is one global counter, not per key — `cache.go:23,56,81`. An upload of `app.exe` makes an in-flight singleflight fill of `app.yaml` discard its result, so unrelated keys re-fetch from MinIO.
- [low] `obsShutdown` is skipped on every startup failure after `obs.Init` — `main.go:51-61`. A MinIO connect or `ensureBucket` failure returns without flushing the exporter, dropping the telemetry that explains the crash-loop.
- [nitpick] Handler methods split across `handler.go` (health only) and `version.go` — `handler.go:22`, `version.go:43,123`. Defensible, but a reader following the CLAUDE.md layout finds almost nothing in `handler.go`.

### Recommendations

- [high] Move the `MINIO_*` block into a `minioutil.Config` mounted as a named field (`envPrefix` where a service needs one) and migrate all three consumers — `config.go:19-24` — one declaration, no further drift.
- [high] Make `ReadTimeout` configurable (or extend the read deadline in the upload handler as admin-service does) and assert it is ≥ the relaying service's budget at startup — `main.go:80`.
- [high] Either cut `CACHE_TTL` to minutes, revalidate a hit against the object's `ETag` before serving, or add an authenticated purge the publisher fans out to all replicas — `cache.go:31-37`, `version.go:113`. Until then narrow the doc's cache-bust claim to single-replica deployments.
- [medium] Store the SHA-256 as object metadata on `Put`, surface it in `blobInfo`, emit it as a response header so the fleet can verify before executing — `store.go:22-28`, `version.go:181-195`.
- [medium] Make a publish atomic and versioned: write the pair under an immutable `<version>/` prefix and flip one small pointer object last — `version.go:84-91` — removes the mismatched-pair window, gives rollback, and makes cache entries immutable by key.
- [low] Key the invalidation generation by object key — `cache.go:23,56,81`.
- [low] Run the shutdown functions on the startup-failure path so `obsShutdown` always flushes — `main.go:51-61`.

## 4. Test coverage — score 2

### Evidence

- [high] Coverage below repo minimum 80%, currently 76.8% (212/276 statements) — `client-update-service/main.go:27` — 50 of the 64 uncovered statements are `main()`+`run()`; there is no `main_test.go`, unlike 19 sibling services that have one. Excluding `main.go` the rest of the tree is 212/226 = 93.8%, so the shortfall is entirely startup wiring plus three small store/handler branches.
- [medium] `run()` is a 47-statement monolith with nothing extracted, so none of it can be unit-tested — `client-update-service/main.go:27-105` — config load, obs init, MinIO connect, bucket ensure, router build, server build and shutdown wiring are one function. Sibling services keep `main_test.go` for extracted helpers; here even `buildRouter`/`buildServer` shapes (`r.MaxMultipartMemory = maxMultipartMemory`, `ReadTimeout`, `WriteTimeout: cfg.HTTPWriteTimeout`, middleware order) are asserted nowhere — a middleware dropped from line 70-73 breaks no test.
- [medium] `minioVersionStore.Open` is 38.5% — `client-update-service/store_minio.go:43-52` — the whole Stat half (8 statements) is unreachable in unit tests because `minioutil.ObjectStore.GetObject` returns the concrete `*minio.Object` (`pkg/minioutil/minio.go:26`), whose `Stat()` cannot be faked. `store_minio_test.go:113` covers only the GetObject error. Worse, the generic (non-`NoSuchKey`) Stat failure at `store_minio.go:50` is covered by *no* test at any tag: the two integration cases are success (`integration_test.go:35`) and `NoSuchKey` (`integration_test.go:54`).
- [medium] `cancelReadCloser.Close` is 0% and its contract is untested — `client-update-service/store_minio.go:93-97` — no test asserts `c.cancel()` runs on Close. Dropping it would leak a `context.WithTimeout` timer per download for the full `MINIO_DOWNLOAD_TIMEOUT` (default 5m) and no test would fail; the integration round-trip calls `rc.Close()` but asserts nothing about cancellation.
- [medium] The partial-publish path is untested — `client-update-service/version.go:88-91` — `TestHandleUpload_StoreError_500` (`version_test.go:152`) only fails the *first* Put. When `executeFile`'s Put fails after `configFile`'s succeeded, the service returns 500 leaving an orphaned config object in MinIO (a half-published release the client fleet can fetch). That behaviour is neither tested nor pinned.
- [low] Seven near-identical upload-rejection tests assert only the status code, so all 400 causes are interchangeable — `client-update-service/version_test.go:77-150` — `InvalidName`, `EqualNames`, `MissingConfigFile`, `MissingExecuteFile`, `EmptyFile`, `WrongConfigExt`, `MalformedMultipart` each assert `400` and nothing about the body. Swapping any two `errcode.BadRequest` messages in `version.go:56-81` passes. CLAUDE.md §4 also prefers one table-driven test here; `config_test.go`/`middleware_test.go` do follow that pattern, these do not.
- [low] `storeFormFile`'s open-error branch is uncovered — `client-update-service/version.go:99-101` — 1 statement; low value, but it is free to cover with a `*multipart.FileHeader` whose temp file has been removed.

### Recommendations

- [high] Close the 9-statement gap to 80% with the store tests that are cheapest and most valuable: cover `cancelReadCloser.Close` (3 stmts) directly and the `Open` Stat branches (8 stmts). That alone yields 223/276 = 80.8%; adding `version.go:88-91` + `:99-101` gives 226/276 = 81.9%.
- [high] To make `store_minio.go:43-52` unit-testable at all, stop depending on `minioutil.ObjectStore` for reads — `client-update-service/store_minio.go:23` — define the service's own one-method getter returning an interface with `Read`/`Close`/`Stat` (interfaces belong to the consumer, CLAUDE.md §3) so both Stat error branches and the NotFound mapping are exercisable without MinIO.
- [medium] Extract `run()`'s testable parts — `client-update-service/main.go:63-82` — a `buildRouter(cfg, handler, sdk) *gin.Engine` and `buildServer(cfg, r) *http.Server`, tested in a new `main_test.go` for middleware order, `MaxMultipartMemory` and both timeouts. This is the sibling-service convention and removes ~20 of the 47 untestable statements.
- [medium] Add a test for the exeFile-Put failure — `client-update-service/version.go:88` — asserting 500 *and* the resulting state, so the orphaned-config-object behaviour is a decision on record rather than an accident.
- [medium] Wire a per-service coverage gate using the repo's own tool — `Makefile:155` shows the pattern (`go run ./tools/coveragecheck -profile … -include client-update-service/ -exclude main.go -min 80`) — the `-exclude soak_main.go` precedent at `Makefile:171` is exactly this case, and it prevents the number sliding back below the floor silently.
- [low] Fold the seven upload-rejection tests into one table-driven `TestHandleUpload_Rejections` with `t.Run`, asserting status **and** a distinguishing substring of each message — `client-update-service/version_test.go:77-150`.

## 5. Maintainability — score 4

### Evidence

- [medium] `requestIDMiddleware` is a drifted private fork of `pkg/ginutil.RequestID` — `client-update-service/middleware.go:67-78` — CONFIRMED. It re-implements the mint/pass-through policy inline (`idgen.IsValidUUID` + `GenerateRequestID`) instead of calling `idgen.ResolveRequestID`, which `pkg/ginutil/middleware.go:20-31` documents as "the single owner of mint-vs-pass-through policy". The fork has already fallen behind: it silently drops the `slog.WarnContext("minted request_id (inbound invalid)")` that both the shared version and `media-service/middleware.go:22-24` emit, so a caller sending a malformed X-Request-ID is undiagnosable here. `upload-service/middleware.go:58-69` is the same stale copy; `media-service` is a current copy. Three forks, two generations.
- [medium] `accessLogMiddleware` logs with `slog.Info`, not `slog.InfoContext` — `client-update-service/middleware.go:85` — both reference implementations (`pkg/ginutil/middleware.go:66`, `media-service/middleware.go:50`) use `InfoContext` so the line carries the request's span context into the o11y SDK handler installed by `obs.Init` (`pkg/obs/obs.go:171-177`). Handing the handler a background context makes the one per-request log line the odd one out in an otherwise trace-correlated service. `upload-service/middleware.go:76` shares the defect.
- [medium] `MINIO_DOWNLOAD_TIMEOUT` is declared twice with its own `envDefault` — `client-update-service/config.go:24` and `upload-service/main.go:101` (both `5m`) — CONFIRMED. CLAUDE.md §Configuration: a knob shared by more than one service is declared once in the package that owns the thing it configures (here `pkg/minioutil`) and mounted as a named field. Two tag-level defaults can drift independently; nothing prevents it.
- [medium] `contentDisposition` diverges from upload-service's RFC 5987 version — `client-update-service/version.go:205-207` (`filename=%q`) vs `upload-service/handler.go:483-490` (`filename*=UTF-8''<pct-encoded>`) — CONFIRMED. `validFileName` (version.go:30-38) admits non-ASCII names, so a UTF-8 artifact name is emitted raw in a header field that RFC 6266 restricts to ISO-8859-1. Two services, two answers to one problem, neither in `pkg/`.
- [low] MinIO store scaffolding is duplicated verbatim across two services — `client-update-service/store_minio.go:88-96` vs `upload-service/store_minio.go:46-55` — `cancelReadCloser` is byte-identical, and the surrounding struct (`client`/`bucket`/`downloadTimeout`), constructor and Stat-probe-then-stream `Open` are the same design copied twice. This is the clearest `pkg/minioutil` candidate in the service.
- [low] Bucket creation escapes the `minioutil` abstraction via a runtime type assertion — `client-update-service/main.go:55-58` with `store_minio.go:62-85` — `minioutil.Connect` returns `ObjectStore`, which has no `MakeBucket`, so the service declares its own `bucketClient` and asserts back to it, failing at startup with a `%T` message if a future `minioutil` wrapper stops satisfying it. A compile-time seam in `minioutil` (which already owns `BucketExists` at `pkg/minioutil/minio.go:39`) would remove the trap.
- [low] The cacheable/non-cacheable split leaks across three functions and costs a second store round trip — `version.go:134-159`, `164-178`, `181-195` — an oversized object is opened once by `loadObject` (which reads only `info.ContentType`, discards it, and returns `cacheable=false`), then re-opened by `streamObject`. The singleflight result for that path carries nothing usable, and `h.cache.get(key)` runs twice (version.go:139 and cache.go:72). `loadObject` also reaches into `h.cache.maxObjectBytes` directly (version.go:170) rather than asking the cache.
- [low] Six upload-rejection tests are copy-paste of one another and assert only the status code — `version_test.go:77-150` — same-logic input variations that CLAUDE.md §4 says should be table-driven, and none asserts the message, so swapping two 400 branches would keep the suite green.
- [low] `run()` is ~79 lines and 0% covered — `main.go:27-106` — config, obs, MinIO connect, bucket assertion, cache, router, listener and shutdown in one function, with no seam. All of the service's 23.2% uncovered statements are here (cov-func: `main.go:27 run 0.0%`).
- [nitpick] No TODO/FIXME debt, no dead code, no file over 500 lines (largest: `version_test.go` 489, `version.go` 207), longest function `HandleUpload` 50 lines (`version.go:43-93`), `go vet ./client-update-service/...` clean. Comments are consistently WHY-comments and `docs/client-api.md` §12 documents both endpoints accurately.

### Recommendations

- [medium] Delete `requestIDMiddleware`/`accessLogMiddleware` here and use `ginutil.RequestID()` + a thin wrapper over `ginutil.AccessLog()` — `middleware.go:67-95` — kills two of the three forks' drift at once (do the same in upload-service); if the `service_account` field is the reason for the fork, add an extra-fields variant to `ginutil` rather than keeping a copy.
- [medium] Move `MINIO_DOWNLOAD_TIMEOUT` into a `minioutil.DownloadConfig` mounted as a named field — `config.go:24` — restores the single-declaration rule and lets the two MinIO services share one default.
- [medium] Promote one `contentDisposition` (the RFC 5987 one) into `pkg/ginutil` or `pkg/minioutil` and call it from both — `version.go:205`, `upload-service/handler.go:485` — one behaviour for non-ASCII artifact names instead of two.
- [low] Move `cancelReadCloser` + the Stat-probe `Open` into `pkg/minioutil` and expose bucket ensure there — `store_minio.go:36-96`, `main.go:55-58` — removes the duplicate and the runtime type assertion together.
- [low] Have `loadObject` return the still-open reader for the non-cacheable case so `HandleDownload` streams it instead of re-opening — `version.go:159`, `164-178` — one store round trip per oversized download and one fewer state to reason about.
- [low] Collapse `version_test.go:77-150` into one table-driven test asserting status **and** message — makes the validation branches distinguishable and turns "add a rule" into "add a row".

## 6. Integration — score 3

### Evidence

- [high] The hardcoded 30s `ReadTimeout` silently breaks admin-service's documented 10m relay contract — `client-update-service/main.go:80` — `http.Server.ReadTimeout` bounds reading the *entire request including the body*, so any upload over 30s is severed mid-body (2 GiB at `UPLOAD_MAX_BYTES` default, `config.go:47`, needs ~570 Mbps sustained). `admin-service` budgets the same transfer at `CLIENT_UPDATE_UPLOAD_TIMEOUT` = 10m (`admin-service/config.go:81`) and `docs/client-api.md:8309` only constrains `HTTP_WRITE_TIMEOUT`, which governs the response and cannot help. The sibling gets it right and says why (`upload-service/main.go:38,206`). Flagged in `docs/reviews/client-update-service-readiness-2026-08-31.md:141` and still unfixed on this tree.
- [high] `MINIO_DOWNLOAD_TIMEOUT` is re-declared with its own `envDefault` in two services — `client-update-service/config.go:24` and `upload-service/main.go:101` (both `5m`) — CLAUDE.md §6 requires a knob shared by >1 service to be declared once in the owning package and mounted as a named field. `pkg/minioutil` has no config struct at all, so the whole `MINIO_*` block is duplicated three ways and has already drifted on requiredness/defaults: `MINIO_BUCKET` is `required` here (`config.go:23`), `envDefault:"avatars"` in `media-service/config.go:74`, bare in `upload-service/main.go:99`.
- [medium] `docs/client-api.md` §12 omits two implemented `400` cases — `client-update-service/version.go:70-73` (path-unsafe filename → `"invalid file name"`) and `:75-78` (identical config/execute names → `"…must have different names"`) are both deliberate and unit-tested (`version_test.go:82`, `:94`), but the response table at `docs/client-api.md:8960` lists only missing/empty parts, wrong extension and malformed multipart. admin-service relays the upstream message verbatim to the admin console (`admin-service/client_update.go:127`), so an operator sees a string the canonical doc never mentions.
- [medium] The published artifact pair has no provenance record anywhere in the chain — `admin-service/client_update.go:341` writes the audit entry with empty subject/target and `nil` details (documented as intentional, `docs/client-api.md:8270`), `client-update-service` stores no checksum, signature or manifest metadata, and the object key is simply the caller-supplied filename (`version.go:27`). Nothing in the repo records *which* bytes were published, by whom, when.
- [medium] The manifest follows no updater convention and is never parsed — `version.go:79-82` validates only the `.yaml`/`.yml` extension; there is no `version`/`files`/`sha512`/`releaseDate` handling, i.e. it is neither an electron-updater `latest.yml` feed nor a Sparkle appcast. Grep for `electron-updater|autoUpdater|latest.yml|appcast|setFeedURL` returns nothing tree-wide, so there is no in-repo client to compare against: the descriptor is an opaque blob inherited from the legacy service (`docs/superpowers/specs/2026-08-03-client-update-service-design.md:9-11,101`). Compliance with any feed format is unverifiable and unenforced, and the unauthenticated `GET` (`routes.go:17`) serves an unsigned executable.
- [medium] `MINIO_BUCKET` is made overridable in this service's compose — `client-update-service/deploy/docker-compose.yml:24` uses `${MINIO_BUCKET:-chat-updates}` while `docker-local/README.md:583` states `MINIO_BUCKET` is one of four settings that must stay literal "because a single shared value would break them". The compose also reads `docker-local/.env`, so setting `MINIO_BUCKET` there points this service at upload-service's bucket; only the `chat.go/chat-versions/` prefix then keeps the keys apart.
- [low] The upload contract is looser upstream than downstream — `admin-service/client_update.go:226-273` relays every file part it receives (up to 16) unvalidated, while `version.go:46,59` reads only `configFile`/`executeFile` and silently discards the rest, returning `200`. Neither §9.17 nor §12 says so.
- [low] No `ReadHeaderTimeout` — `main.go:77-82`. `ReadTimeout` is currently the only slowloris guard; fixing the finding above removes that cover. `upload-service/main.go:205` sets 5s.

### Recommendations

- [high] Make the read deadline configurable and default it to `HTTP_WRITE_TIMEOUT`, adding `ReadHeaderTimeout: 5s` in the same change — `main.go:77-82` — restores admin-service's 10m relay budget and keeps the slowloris bound. Extract `newServer(cfg, handler) *http.Server` so the timeout matrix is testable — `run()` being untested is why this survived.
- [high] Add a `minioutil.Config` (endpoint, credentials, SSL, bucket, download timeout) and mount it as a named field in `client-update-service`, `upload-service` and `media-service` — `config.go:19-24`, `upload-service/main.go:95-101`, `media-service/config.go:70-74` — one declaration, one default, per CLAUDE.md §6.
- [medium] Add the two missing `400` rows to `docs/client-api.md:8960` (path-unsafe filename; identical config/execute names) and mirror them in `docs/client-api/request-reply.md:238-241`.
- [medium] Record a SHA-256 of each stored object as MinIO user metadata and pass the two filenames + digests into the admin audit entry — `version.go:97-115`, `admin-service/client_update.go:341` — a published client binary should be attributable.
- [medium] Decide and document the manifest convention — either state in §12 that the `.yaml` is opaque and owned entirely by the desktop client, or adopt electron-updater's `latest.yml` shape and validate it on upload (`version.go:79`). Today neither side can be checked against the other.
- [low] Pin `MINIO_BUCKET` literally in `client-update-service/deploy/docker-compose.yml:24`, matching the rule in `docker-local/README.md:583`.
- [low] Either reject unexpected form parts in `HandleUpload` (`version.go:46-63`) or document that extras are ignored in §9.17 and §12.

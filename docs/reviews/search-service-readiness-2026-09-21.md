# search-service — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `6c6670e` (base `main`)  
**Overall score:** 3.0 / 5 (baseline 2026-09-01: 3.3, Δ -0.3)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

search-service is a tidy NATS request/reply front end over Elasticsearch whose enrichment, caching and admission-control layers are well built, but it ships two client-facing prototypes as if they were finished: `search.users` proxies to a third-party HR endpoint whose path, body and response shape are admitted guesses (three `TODO(searchUsers-thirdparty)` markers, yet `USERS_API_URL` is required and the RPC is registered and documented), and `search.apps` discards the caller's `account` so every name-matching app is returned unscoped, with the planned fix written as two `$lookup` stages that CLAUDE.md forbids. The `search.apps` aggregation is also the only Mongo read without a projection. Four reviewers independently flagged that the per-request timeout is declared twice (`SEARCH_REQUEST_TIMEOUT` and the router guard's `REQUEST_TIMEOUT`, both 10s, both applied), that the `RoomInfoClient` is poked into the handler after construction, and that the service creates indexes on `apps` and `subscriptions` collections other services own while treating `users` as verify-only. On the performance side the message-search body grows unbounded with the caller's restricted-room count and client-supplied `roomIds`, `offset` is never capped against ES's 10 000 result window, and the restricted-rooms access map sits in Valkey for five minutes with no invalidation path. Coverage is 68.9% overall and 79.5% under the pipeline's own `main.go` filter, so the service misses its own 80% gate by half a point; the generated mocks are dead code in favour of hand-written fakes, and `room_client.go` has no test of any kind.

| Dimension | Score |
|---|---|
| Code quality | 3 |
| Architecture | 3 |
| Test coverage | 2 |
| Maintainability | 3 |
| Integration | 3 |
| Performance | 4 |

**Findings by severity:** 0 critical, 5 high, 22 medium, 22 low, 7 nitpick (56 total).  
**Highest-risk dimension:** Test coverage (2).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 3

### Evidence

- [high] `search.apps` aggregation has no projection — `search-service/query_apps.go:51-55` — Pipeline is `$match → $skip → $limit`; `SearchAppsByName` (`store_mongo.go:146-154`) decodes whole `apps` documents. CLAUDE.md §6: "every find/aggregation MUST specify an explicit projection". The other three Mongo reads (`store_mongo.go:61,89,116`) do project.
- [medium] Degraded-path WARN logs are emitted without context, so they carry neither `request_id` nor trace correlation — `handler.go:261`, `handler.go:291`, `enrich.go:44`, `enrich.go:91`, `enrich.go:100`, `enrich.go:173` — All six use `slog.Warn` instead of `slog.WarnContext(ctx, …)`. The o11y base handler derives trace IDs from ctx (`o11y/internal/log/handler.go:71`); `logEmptyResult` (`handler.go:194-204`) shows the intended pattern. CLAUDE.md §3: include the request ID "in all log lines". These fire precisely during Valkey/Mongo/room-service brown-outs.
- [medium] Live `search.apps` RPC accepts `account` and discards it; access guard unimplemented — `query_apps.go:49` (`_ = account`) — Every name-matching app is returned to any caller. Documented as "Planned behavior" (`docs/client-api.md:4419`) but registered (`handler.go:76`). The TODO also plans two `$lookup` stages, forbidden by CLAUDE.md §6 without a documented exception. Security dimension should weigh the authorization impact.
- [medium] `search.users` ships a placeholder wire contract on a live RPC — `users_client.go:21-23`, `:40-43`, `:59-60` — Three `TODO(searchUsers-thirdparty)` markers: body, path (`/search`) and response shape are guesses, yet `USERS_API_URL` is `required` (`main.go:58`) and the handler is registered (`handler.go:77`).
- [low] Generated mockgen mocks are dead code; tests use hand-rolled fakes — `mock_store_test.go` (10 `NewMock*` ctors, referenced by no other file); `handler_test.go:25-60` (`fakeStore`), `:644-660` (`fakeUsers`) — CLAUDE.md §4 says mock with `go.uber.org/mock`; the 291-line file is regenerated for zero consumers.
- [low] `RoomInfoClient` injected by field assignment after construction — `main.go:289` (`handler.room = newRoomClient(nc)`) vs `handler.go:57` — CLAUDE.md §3: dependencies injected via constructor; `enrich.go:158` then nil-guards `h.room`.
- [low] `newHandler` mutates the caller's `*handlerConfig` while defaulting — `handler.go:57-70` — Defaults are written through the pointer, then the struct is copied; the caller's config silently changes.
- [low] Redundant `request_id` re-passed to `WithLogValues` in every handler — `handler.go:95,147,218,304,340` — `natsrouter.RequestID()` already sets it (`pkg/natsrouter/middleware.go:52`, doc: "handlers don't need to re-pass it").
- [nitpick] Dead `errors.As` special-case — `handler.go:164-171` — `errcode.Classify` walks the chain (`pkg/errcode/classify.go:17`), so a plain `%w` wrap classifies identically.
- [nitpick] Bare `return err` pass-through — `metrics.go:157-160` — CLAUDE.md §3 says never return bare `err`; harmless since the callee wraps, but inconsistent with the file.

### Recommendations

- [high] Add a terminal `$project` limited to the fields `SearchAppsResponse` serialises — `query_apps.go:51-55` — MUST compliance; stops shipping full app documents (assistant config etc.) over the wire.
- [medium] Implement the `search.apps` access guard as a separate `subscriptions` query rather than the planned `$lookup` pair — `query_apps.go:38-49` — Closes the caller-scoping gap without a forbidden server-side join.
- [medium] Switch the six degraded-path warnings to `slog.WarnContext(ctx, …)` and drop the manual `request_id` args — `handler.go:261,291`, `enrich.go:44,91,100,173` — Restores trace/request correlation on the lines that matter during outages.
- [medium] Finish `httpUsersClient` against the real third-party spec, or gate `search.users` registration behind a flag until it exists — `users_client.go`, `handler.go:77` — A guessed contract on a required URL fails at first call looking like an upstream outage.
- [low] Pick one test-double strategy: consume the mockgen mocks in `handler_test.go`, or delete the `//go:generate` directive and the generated file — `store.go:11`, `mock_store_test.go`.
- [low] Inject `RoomInfoClient` through `newHandler` and take `handlerConfig` by value — `handler.go:57`, `main.go:289`.
- [low] Convert the ten `TestHandler_SearchApps_*` and eight `TestHandler_SearchRooms_*` single-scenario functions to table-driven subtests — `handler_test.go:495-620` — Only 2 `t.Run` across 986 lines.

## 3. Architecture — score 3

### Evidence

- [medium] Per-request timeout is declared twice and both fire — `search-service/main.go:71` (`SEARCH_REQUEST_TIMEOUT`, default 10s) and `search-service/main.go:136` mounting `natsrouter.GuardConfig` (`REQUEST_TIMEOUT`, default 10s, `pkg/natsrouter/guard.go:24`). The router applies the guard deadline (`main.go:297`) and every handler then re-wraps with `withRequestTimeout` (`handler.go:81-86`, `handler.go:105`). Two operator knobs for one concern; the service-local one violates the "shared knob declared once in the owning package" rule, and the effective deadline is silently `min(a,b)`.
- [medium] `room` dependency is injected by field assignment, not the constructor — `search-service/main.go:289` (`handler.room = newRoomClient(nc)`), `handler.go:57` (`newHandler` takes store/mongo/users/cache only). Tests mirror it (`handler_test.go:976`, `enrich_test.go:33`), and `enrich.go:357` carries a `h.room == nil` guard that exists only to tolerate the half-constructed handler.
- [medium] Index ownership crosses service boundaries inconsistently — `search-service/store_mongo.go:43-45` creates `{u.account, roomId}` on `subscriptions` (owned by room-service, whose `ensureIndexes` at `room-service/store_mongo.go:125-145` does not define this spec) and `store_mongo.go:37-39` creates `{name:1}` on `apps`, while the same function treats `users.account_1`/`apps.assistant_name_idx` as verify-only "owned by user-service" (`store_mongo.go:47-50`). A read-only consumer minting indexes on a writer's collection is an undocumented cross-service contract.
- [medium] `search.apps` is a shipped prototype whose planned fix uses a forbidden pattern — `search-service/query_apps.go:454` discards `account` (`_ = account`), so results are not subscription-scoped (`docs/client-api.md:4417-4419` admits this), and the TODO at `query_apps.go:443-453` plus the index rationale at `store_mongo.go:31-35` plan `$lookup` stages against `subscriptions` and `rooms`, which CLAUDE.md forbids for new code.
- [medium] `search.users` is registered on a client-facing subject (`handler.go:77`) but proxies to a placeholder upstream contract — `search-service/users_client.go:485-487`, `:504-507`, `:523` (`TODO(searchUsers-thirdparty)`, guessed body, hard-coded `/search` path). The `SearchUsersClient` seam (`store.go:88-90`) is the right boundary, but the endpoint cannot be production-correct until the contract exists.
- [low] `SearchStore` is a transport pass-through that leaks the search engine into the handler — `search-service/store.go:14` (`Search(ctx, indices, body json.RawMessage)`), `store_es.go:116-122` adds only an error wrap; the handler owns index patterns (`handler.go:34-35`, exported mutable `MessageIndexPattern` at `query_messages.go:612`) and raw ES DSL builders. Handler unit tests must hand-craft ES response envelopes, and swapping the backend touches the handler package.
- [low] Readiness reflects NATS only and never flips on drain — `search-service/main.go:310-312` registers `natsutil.HealthCheck(nc)` alone (ES, Mongo, Valkey excluded although `searchengine.Ping` exists at `pkg/searchengine/searchengine.go:57`), and the health server is stopped after the DB disconnects (`main.go:344`) with no draining check as `user-service/main.go:275-283` does.
- [low] Router assembled by hand instead of `natsrouter.DefaultGuarded` — `search-service/main.go:292-297` vs `pkg/natsrouter/guard.go:57-71`, whose stated purpose is to stop a service applying half the guard. Middleware order also differs from `Default` (`pkg/natsrouter/router.go:142`): `RequestID()` runs before `Recovery()`, so a panic in the request-ID middleware is unrecovered.
- [low] ES backend knobs re-declared in three services — `search-service/main.go:32-38`, `search-sync-worker/main.go:55-59`, `data-migration/es-index-migrator/config.go:13-16` — while `pkg/searchengine.Config` (`pkg/searchengine/factory.go:15-23`) carries no env tags. The index-name knobs (`main.go:122-124`) are likewise duplicated with an inline "MUST match search-sync-worker exactly" comment instead of a shared declaration.
- [nitpick] `newHandler` mutates the caller's `*handlerConfig` before copying it by value — `search-service/handler.go:58-70`; pass by value or return the normalised copy.

### Recommendations

- [medium] Delete `SearchConfig.RequestTimeout` and `handler.withRequestTimeout`; rely solely on `Guard.RequestTimeout` via `natsrouter.DefaultGuarded(nc, "search-service", cfg.Guard, WithSiteID, WithMetrics)` — `main.go:71`, `main.go:292-297`, `handler.go:81-86` — one knob, standard middleware order, no half-applied guard.
- [medium] Add `room RoomInfoClient` to `newHandler`'s parameters and drop the `h.room == nil` guard — `handler.go:57`, `main.go:289`, `enrich.go:357` — restores constructor DI and makes the dependency visible in tests.
- [medium] Move the `subscriptions {u.account, roomId}` and `apps {name}` index creation to their owning services (room-service / user-service) and downgrade search-service to `WarnMissingIndexes` for both — `store_mongo.go:37-45` — one owner per index, matching the policy the file already applies to `users`.
- [medium] Redesign the `search.apps` access guard without `$lookup`: query `subscriptions` by `{u.account, roomType: botDM}` (projected to bot account names), then `apps` with `$in` — `query_apps.go:443-454` — closes the documented scoping gap within CLAUDE.md's Mongo rules.
- [medium] Gate `search.users` behind a feature flag or unregister it until the third-party contract is fixed — `handler.go:77`, `users_client.go:504-523` — a client-facing route should not depend on a guessed wire shape.
- [low] Give `SearchStore` domain methods (`SearchMessages`, `SearchRooms`, `SearchOrgs`, `GetUserRoomDoc`) and move the DSL builders and index patterns behind `store_es.go` — `store.go:13-16`, `query_*.go` — handler tests then mock typed results, and `MessageIndexPattern` stops being an exported mutable global.
- [low] Add `searchengine.Ping`, Mongo and Valkey checks plus a draining flag to readiness, and shut the health server down last — `main.go:310-344` — mirrors `user-service/main.go:275-283`.

## 4. Test coverage — score 2

### Evidence

- [high] coverage below repo minimum 80%, currently 68.9% (506/734 statements) — `search-service/deploy/azure-pipelines.yml:64-70` — Applying the pipeline's own `main.go` exclusion (`awk '$0 !~ "/main\\.go:"'`, line 67) to the provided profile gives 79.5% (496/624), still under the `MIN_COVERAGE_SERVICE: '80'` gate at line 70; the comment at lines 28-29 ("Both pkg and service meet the repo-wide 80% minimum") is stale. Excluding `main.go`, `store_mongo.go` and the two clients, business logic is at 92.9%, so the shortfall is concentrated in adapters, not handlers.
- [medium] `room_client.go` has no test of any kind — `search-service/room_client.go:26-47` — `newRoomClient`/`GetRoomsInfo` are referenced by no `_test.go` file. The three failure branches (`natsutil.RequestFailure` at line 37, `errcode.Parse` passthrough at 39-41, decode at 43-45) are the only cross-site RPC this service makes and are unverified; `enrich_test.go:17` substitutes a `fakeRoom`.
- [medium] Per-request timeout guard is never exercised — `search-service/handler.go:81-86` — The `context.WithTimeout` branch at line 85 has 0 unit hits (every unit fixture leaves `RequestTimeout` at 0, `handler_test.go:89-98`); integration fixtures set 5s (`integration_apps_test.go:41`) but no test uses a slow dependency to prove the deadline fires and surfaces as an error.
- [low] `users_client.go` untested at unit level, partial at integration level — `search-service/users_client.go:60-62,66-68,73-76` — `integration_users_test.go:46-101` covers 2xx and 503 only; the Bearer-token branch, transport error and `resp.Result()` type-assert failure never run, and no test asserts the `Authorization` header.
- [low] Reachable handler branches with zero hits — `search-service/handler.go:275,289-292,353-355,132-134,181-183,246-248` — plain ES-prefetch failure with a healthy cache (275; only the combined failure at 273 is tested); `SetRestricted` failure warn (289-292; `fakeCache.setErr` at `handler_test.go:62` is declared but never set); `searchUsers` limit clamp (353-355; every other handler has a `_SizeClamped` test, e.g. `handler_test.go:195,460,596,923`); malformed ES body at handler level for messages/rooms/orgs (parsers are tested directly in `response_test.go`, the wrapping path is not).
- [low] `TestEnrichMessages_DegradesOnAllErrors` never reaches the apps-lookup failure it names — `search-service/enrich_test.go:152-162` — sender is `alice` (non-bot) and `subsErr` empties the bot set, so `AppsByAssistantNames` is never called; `enrich.go:99-101` is the only uncovered statement in `enrich.go`. `fetchRoomNames` (`enrich.go:156-187`) is only ever driven with one site per call (`enrich_test.go:124-139`), so the semaphore/mutex fan-out at 163-183 never runs concurrently under `-race`.
- [low] Generated mocks are dead code; tests use hand-written fakes — `search-service/mock_store_test.go:35,90,144,228,267` — none of the five `NewMock*` constructors is referenced; `fakeStore`/`fakeCache`/`fakeMongo`/`fakeUsers` (`handler_test.go:25,59,232,645`), `fakeRoom` (`enrich_test.go:17`), `cachedTestMongo` (`store_cache_test.go:287`) and `stubEngine` (`store_es_test.go:14`) do the work. CLAUDE.md §4 prescribes `go.uber.org/mock`; either adopt the mocks or drop the `//go:generate` at `store.go:10`.
- [nitpick] Repetitive per-handler tests instead of table-driven — `search-service/handler_test.go:177-193,400-435,550-572,715-739,880-946` — EmptyQuery/Whitespace/NegativeSize/EmptyResult variants are copied five times as separate funcs; two `t.Run` uses in 986 lines. `TestMongoStoreInterfaceShape` (`handler_test.go:962-964`) is a no-op test that belongs as a package-level `var _` like `store_cache.go:155`.
- [nitpick] `time.Sleep` polling loop in the CCS fixture — `search-service/integration_ccs_test.go:239-255` — `require.Eventually` expresses the same wait. (The inline `GenericContainer` at line 108 is the CLAUDE.md-sanctioned CCS exception and registers `Terminate` at 135.)

### Recommendations

- [high] Lift unit coverage over 80% under the CI filter — an `httptest`-backed unit test for `users_client.go:36-73` (token header, 2xx, non-2xx, transport error, bad body) needs no build tag, plus a zero-config `newHandler` test for `handler.go:58-69`; together ~25 statements, enough to clear the gate without touching integration.
- [medium] Add an integration test for `room_client.go` — use `setupRouter`'s shared NATS (`setup_shared_test.go:75-93`) with a stub `QueueSubscribe` on `subject.RoomsInfoBatch(site)` (pattern in `user-service/roomclient/client_integration_test.go`) covering success, errcode envelope, timeout and malformed reply.
- [medium] Prove the request timeout — `handler.go:81-86` — unit test with `RequestTimeout: 50ms` and a `fakeStore.Search` that blocks on `ctx.Done()`, asserting the handler returns and `errcode.Classify` yields `internal`.
- [low] Cover the remaining handler branches by setting `fakeCache.setErr`, returning a malformed body from `fakeStore`, and adding `TestHandler_SearchUsers_LimitClamped`.
- [low] Fix `TestEnrichMessages_DegradesOnAllErrors` to use a bot sender so `appsErr` is hit, and add a two-site case for `fetchRoomNames` so `-race` exercises the fan-out.
- [low] Decide on mocks: migrate the fakes to the generated `MockMongoStore` etc., or remove `mock_store_test.go` and the `//go:generate` line so `make generate` stops producing unused code.
- [nitpick] Update the stale coverage comment in `deploy/azure-pipelines.yml:28-33` once the number is back above 80%.

## 5. Maintainability — score 3

### Evidence

- [high] `search.users` ships a placeholder third-party client wired live in production — `search-service/users_client.go:21`, `:40`, `:59` — Three `TODO(searchUsers-thirdparty)` comments state the request body and `/search` path are invented pending the real spec; `main.go:57-61` makes `USERS_API_URL` required so every deployment must point it somewhere; `docs/client-api.md:4509` documents the RPC as live; the client has 0% unit coverage (`users_client.go:39`). A new engineer cannot tell contract from scaffolding.
- [medium] `search.apps` prototype discards its access-guard parameter and its TODO plans a forbidden `$lookup` — `search-service/query_apps.go:38-49` — `_ = account` with a comment deferring the guard to a `$lookup subscriptions` stage; `store_mongo.go:33-34` says the `name` index "supports the $lookup/$group stages the spec'd pipeline will add". CLAUDE.md forbids `$lookup`, so the recorded completion path cannot be followed as written; the debt has no legal exit.
- [medium] `main()` is 197 lines and dependency injection is split across two mechanisms — `search-service/main.go:153-349`, `:289` — `newHandler` takes four deps by constructor (`handler.go:57`) while the fifth is a post-construction field write `handler.room = newRoomClient(nc)`; tests copy the same pattern (`enrich_test.go:33`). Adding a dependency now has two conventions to choose from, and `main.go` is excluded from the coverage gate (`deploy/azure-pipelines.yml:99-106`).
- [medium] Defaults and per-request timeouts are declared in triplicate — `search-service/main.go:67-71`, `handler.go:58-69`, `query_messages.go:242-245` — `DocCounts`/`MaxDocCounts`/`RestrictedRoomsCacheTTL`/`RecentWindow` each have an `envDefault`, a second fallback in `newHandler`, and (for RecentWindow) a third in `recentWindowToGte`. Separately, `SEARCH_REQUEST_TIMEOUT` (`handler.go:81-86 withRequestTimeout`) and natsrouter Guard `REQUEST_TIMEOUT` (`pkg/natsrouter/guard.go:24`, applied at `main.go:297`) both default to 10s and both wrap the same handler — two operator knobs for one behavior.
- [medium] `enrichMessages` is 134 lines with the room-type switch duplicated — `search-service/enrich.go:18-151` — key collection, subscription lookup, partitioning (`:54-72`), two batch lookups, RPC fan-out, and assembly (`:111-138`) live in one function; `switch meta.RoomType` appears at `:60` and `:115`. A new room type must be added in two places in the same function.
- [low] Every handler re-attaches `request_id` that the router middleware already set — `search-service/handler.go:95,147,218,304,340` — `natsrouter.RequestID()` calls `c.WithLogValues("request_id", …)` at `pkg/natsrouter/middleware.go:46`; search-service is the only service repeating it, and a sixth handler will copy the boilerplate.
- [low] `h.mongo == nil` is tolerated only where tests need it — `search-service/enrich.go:38-41,88,97` vs `handler.go:318` — enrich.go guards `h.mongo` "e.g. base message-search tests", but `searchApps` dereferences it unguarded. An "optional" dependency contract that exists for tests and is inconsistently honored is a trap for the next reader.
- [low] Documented foot-gun left in place — `search-service/main.go:104-109` — `ESConfig` and `SearchConfig` share the `SEARCH_` prefix; the comment instructs future authors to manually cross-check every new field for env shadowing instead of using distinct prefixes.
- [low] Stale/misleading comments and an asymmetric index knob — `search-service/deploy/azure-pipelines.yml:92-95` (says builder is on `1.25.8-alpine`; `Dockerfile:1` is `1.25.13-alpine`), `query_messages.go:18` (`messages-*` hardcoded while `SPOTLIGHT_INDEX`/`SPOTLIGHT_ORG_INDEX`/`USER_ROOM_INDEX` are required env — renaming the messages index is a code change, the others are config).
- [nitpick] Exported identifiers in `package main` nothing imports — `search-service/store.go:13,21,29,59`, `main.go:32`, `query_messages.go:18` — `SearchStore`, `MongoStore`, `UserRoomDoc`, `ESConfig`, `MessageIndexPattern`; CLAUDE.md says export only what other packages consume. Repo-wide habit, taste only.

### Recommendations

- [high] Resolve the `search.users` placeholder: either land the real HR contract or stop registering the RPC (config-gated, `handler.go:77`) and drop the `required` on `USERS_API_URL` (`main.go:58`) — a deployed endpoint whose wire shape is admitted guesswork is unreviewable and misleads clients reading `docs/client-api.md`.
- [medium] Rewrite the `query_apps.go:38-49` TODO to a rule-compliant plan (subscriptions query → app-ID set → `$in` on `apps`), consume `account`, and delete the `$lookup` sentence at `store_mongo.go:33-34` — the recorded roadmap must not contradict CLAUDE.md's `$lookup` ban.
- [medium] Single-source the defaults: delete the fallbacks in `newHandler` (`handler.go:58-69`) and `recentWindowToGte` (`query_messages.go:242-245`), reject non-positive values in `SearchConfig.Validate` (`main.go:89`), and keep exactly one request timeout (drop `SEARCH_REQUEST_TIMEOUT` + `withRequestTimeout` in favor of the Guard middleware, or vice versa).
- [medium] Split `enrichMessages` (`enrich.go:18`) into `partitionRooms` and `assembleRooms` so the room-type switch exists once, and pass `RoomInfoClient` through `newHandler` (`handler.go:57`) so `main.go:289` and `enrich_test.go:33` stop poking the field.
- [low] Delete the five `c.WithLogValues("request_id", …)` calls (`handler.go:95,147,218,304,340`), keeping only `"account"` — the middleware already provides it.
- [low] Give `ESConfig` its own prefix (e.g. `SEARCH_ES_`) and remove the warning at `main.go:104-109`; fix the stale Go-version comment at `deploy/azure-pipelines.yml:92-95`.

## 6. Integration — score 3

### Evidence

- [high] `search.users` is a client-facing RPC whose upstream contract is a placeholder — `search-service/users_client.go:21`, `:40`, `:59-60` — three `TODO(searchUsers-thirdparty)` markers; the client POSTs `{query,offset,limit}` to a guessed `/search` path and `deploy/docker-compose.yml:22` defaults `USERS_API_URL` to `users-api-mock`. It is registered on `chat.user.{account}.request.search.{siteID}.users` and documented at `docs/client-api.md:4509`, so clients will code against a `model.SearchUser` shape (`pkg/model/search.go:154`, also TODO) never validated against the real HR endpoint.
- [high] `REQUEST_TIMEOUT` re-declared with its own `envDefault` — `search-service/main.go:71` (`SEARCH_REQUEST_TIMEOUT`, 10s) duplicates `pkg/natsrouter/guard.go:24` (`REQUEST_TIMEOUT`, 10s), which the service also mounts (`main.go:136`) and applies (`main.go:297`). Each handler re-wraps the already-deadlined router context (`handler.go:81`), so the effective bound is the min of two knobs; raising `REQUEST_TIMEOUT` is silently clamped. CLAUDE.md: a shared knob is declared once in its owning package, never re-declared.
- [medium] `search.apps` is unscoped to the caller — `search-service/query_apps.go:38-49` (`_ = account`); the subscription access guard is a TODO. Documented as "prototype" at `docs/client-api.md:4411`, but it ships on a client-facing subject.
- [medium] Restricted-room access has an uninvalidated 5-minute staleness window — `search-service/handler.go:258` caches the ES `restrictedRooms` map in Valkey (`store_valkey.go:23`); nothing in the repo deletes that key, and `query_messages.go:99-109` emits Clause A/B for every cached rid with no terms-lookup gate. A user removed from a restricted room keeps searching its post-HSS messages until `RESTRICTED_ROOMS_CACHE_TTL` (`main.go:69`) lapses; a new member is blind for the same window. Undocumented in `docs/client-api.md`.
- [medium] Message read pattern hard-coded while the writer's prefix is operator-chosen — `search-service/query_messages.go:18` (`messages-*`) vs `search-sync-worker/main.go:60` (`MSG_INDEX_PREFIX`, `required`, any `<base>-v<N>`). Spotlight/org patterns are derived from config (`main.go:179-191`); a non-`messages-` prefix yields 200 + zero hits under `ignore_unavailable`. `searchindex.IndexPattern` (`pkg/searchindex/version.go:33`) exists for exactly this.
- [medium] Creates indexes on collections other services own — `search-service/store_mongo.go:37` (`apps{name}`), `:42` (`subscriptions{u.account,roomId}`), while `:47` applies the repo's "owned by user-service; verify + warn only" rule to the same collections. `room-service/store_mongo.go:125-145` owns `subscriptions` and already keeps four `u.account`-prefixed indexes that serve the `{u.account, roomId $in}` filter; the extra index is ownerless write amplification on the hottest collection.
- [low] Index-doc contract duplicated rather than shared — `search-service/store.go:28` cites `search-sync-worker/user_room.go userRoomUpsertDoc`, which no longer exists (writer type: `pkg/searchindex/userroomdoc.go:68`); `response.go:124` `orgSearchHit` mirrors `search-sync-worker/spotlight_org.go:193`, a contract living in a service, not `pkg/searchindex`. Field-level check today: every read field matches the writers, and `model.Attachment`/`Card` alias the cassandra types (`pkg/model/attachment.go:9-11`) so the hit decode is wire-identical.
- [low] Docs drift — `docs/client-api/request-reply.md:1421` lists only `"query is empty"` for `search.messages`, though the handler and the canonical doc also return `bad_request` for negative `size`/`offset`; `docs/client-api.md:4561` (Search Users) omits the negative `offset`/`limit` `bad_request` at `handler.go:346`.
- [nitpick] `deploy/azure-pipelines.yml:22` claims the builder is `1.25.8-alpine` and pins `GO_VERSION: '1.25.9'` (`:25`); Dockerfile, `go.mod` and `.github/workflows/ci.yml:68` are all `1.25.13`.

Verified clean: all five subjects and `subject.RoomsInfoBatch` come from `pkg/subject`, no raw subjects; the service publishes no events (no JetStream/outbox/INBOX use, so the `Timestamp` rule is vacuous); no Cassandra, no ID generation; `valkeyutil.Config`, `mongoutil.PoolConfig`, `natsrouter.GuardConfig`, `logctx.Config` are mounted from their owners; `SkipKeys:true` is honoured by `room-service/handler.go:1243` and the ≤100-room batch is under `MAX_BATCH_SIZE=1000` (`room-service/main.go:63`); ES env names match search-sync-worker's.

### Recommendations

- [high] Gate `search.users` on the real third-party contract — `users_client.go:59-60` — finish the adapter against the actual spec (path, body, auth, response fields) with a recorded-fixture test, or unregister the subject and mark `docs/client-api.md:4509` "not yet available".
- [high] Delete `SearchConfig.RequestTimeout` and `withRequestTimeout` — `main.go:71`, `handler.go:81` — rely solely on `cfg.Guard.TimeoutMiddleware()`; replace `SEARCH_REQUEST_TIMEOUT` with `REQUEST_TIMEOUT` in `deploy/docker-compose.yml:41`.
- [medium] Implement subscription scoping for `search.apps` (a Go-side filter via `SubscriptionsByRoomIDs` avoids `$lookup`) — `query_apps.go:38-49` — then drop the "prototype" caveat at `docs/client-api.md:4411`.
- [medium] Invalidate `searchservice:restrictedrooms:{account}` from search-sync-worker's membership/HSS handler (it already sees every change), or gate Clause A/B on the live ES doc — `handler.go:258`, `store_valkey.go:23` — and document the residual window.
- [medium] Derive the message read pattern from config — `query_messages.go:18` — add an unprefixed `MSG_INDEX_PREFIX` (same name as the writer) and build `[]string{searchindex.IndexPattern(p), "*:"+searchindex.IndexPattern(p)}` at startup, as `SPOTLIGHT_INDEX` is handled at `main.go:179`.
- [medium] Switch `store_mongo.go:37-46` to `mongoutil.WarnMissingIndexes` like `:47-50`; if `{u.account, roomId}` is truly needed, add it to room-service's owned set with a justification.
- [low] Decode the user-room doc via `searchindex.UserRoomUpsertDoc`, move `SpotlightOrgIndex` into `pkg/searchindex` — `store.go:28`, `response.go:124` — and sync `request-reply.md:1421` / `client-api.md:4561` with the handler's `bad_request` cases.

## 7. Performance — score 4

### Evidence

- [medium] Message-search query body grows without bound in the caller's restricted-room count and in client-supplied `roomIds` — `search-service/query_messages.go:99-109`, `:111-145` — `globalAccessClauses` appends two `bool` clauses (≈4 and ≈7 leaf clauses) per entry in the cached `restrictedRooms` map, and `scopedAccessClauses` inlines every `req.RoomIDs` element in a `terms` clause; neither the handler (`handler.go:446-452`) nor `normalizePagination` caps them. A long-tenured user in a few hundred HSS-restricted rooms ships a multi-hundred-KB body per `bool_prefix` keystroke and can trip ES `indices.query.bool.max_clause_count`, which surfaces as `internal` (`handler.go:477`) rather than a shed request.
- [medium] `offset` is validated only for sign, never capped — `search-service/handler.go:724-735` — `from`+`size` is forwarded verbatim (`query_messages.go:55-56`, `query_rooms.go:313-314`, `query_orgs.go:436-437`). Past `index.max_result_window` (10 000) ES answers 400, which `searchengine` wraps as a plain error (`pkg/searchengine/adapter.go:371-377`) and the boundary classifies as `internal` with an ERROR log; below that, a deep `from` on the two-pattern CCS search (`query_messages.go:18`) costs `from+size` docs per shard per cluster on every page.
- [medium] `SearchAppsByName` aggregation has no `$project` stage — `search-service/query_apps.go:415-419`, consumed at `store_mongo.go:707-718` — whole `apps` documents (incl. `appViewUrl` map, `sponsors`, `channelTab`) are fetched and decoded; CLAUDE.md requires an explicit projection on every find/aggregation. Mitigating: `model.App` is the wire type, so the over-fetch is bounded by `MaxDocCounts=100`; the spec'd follow-up (`:402-412`) adds two `$lookup`s that will need inline justification.
- [medium] HR users Resty client keeps the stdlib default of 2 idle keep-alive connections — `search-service/main.go:246-249` — only `WithTimeout` is passed; `restyutil.New` clones `http.DefaultTransport` (`pkg/restyutil/restyutil.go:60-62`) so `MaxIdleConnsPerHost=2`. With `MAX_CONCURRENCY=256` (`pkg/natsrouter/guard.go:21`) concurrent `search.users` typeahead calls to one host, every request beyond two in flight pays a fresh TCP/TLS handshake. `restyutil.WithMaxIdleConns` exists and five sibling services use it.
- [low] Range filter uses un-rounded date math, defeating the ES node query cache — `search-service/query_messages.go:38` — `now-8760h` is evaluated to a fresh millisecond on every request, so the `createdAt` filter clause can never be served from the filter cache. Rounding (`now-8760h/h`) makes it cacheable for an hour. The neighbouring `sortedRIDs` comment (`:259-261`) cites cacheability as a goal that this clause undermines.
- [low] `track_total_hits: true` is requested on the rooms and orgs searches but the total is never returned — `search-service/query_rooms.go:315`, `query_orgs.go:438` vs `pkg/model/search.go:109-111`, `:178-180` — exact hit counting on every keystroke for a value the response type cannot carry.
- [low] Enrichment lookups run serially — `search-service/enrich.go:87-106` — `UsersByAccounts`, `AppsByAssistantNames` and `fetchRoomNames` are independent once `subs` is known but execute back-to-back; the room-info RPC alone carries a 5 s budget (`room_client.go:14`), so enrichment latency is the sum of three round trips rather than the max.
- [low] Restricted-rooms Valkey cache has a 5 m TTL and no invalidation path — `search-service/handler.go:638-642`, `store_valkey.go:765-773` — nothing outside this service (bar `tools/seed-sample-data`) touches the key, while `search-sync-worker/user_room.go:46,70-82` updates the ES source of truth on every membership/HSS change. Staleness is bounded only by `SEARCH_RESTRICTED_ROOMS_CACHE_TTL`; perf-wise fine, but the access-control semantics belong to the security/integration reviewers (see NOTES).

### Recommendations

- [medium] Cap `len(req.RoomIDs)` (e.g. 100) with `errcode.BadRequest`, and cap the number of restricted-room clauses emitted per query (or move the HSS floor into the user-room doc so a single terms-lookup covers it) — `query_messages.go:99-145`, `handler.go:446` — bounds ES body size per keystroke and turns a `max_clause_count` rejection into a client-side 400.
- [medium] Cap `offset` so `offset+size <= 10 000` in `normalizePagination`, returning `errcode.BadRequest` — `handler.go:724-735` — converts an ES 400 into a proper client error and stops deep CCS pages; document the cap in `docs/client-api.md`.
- [medium] Add a terminal `$project` matching `model.App`'s bson tags to `buildSearchAppsPipeline` — `query_apps.go:415-419` — satisfies the projection rule now and keeps the future `$lookup` stages from carrying whole documents through `$group`.
- [medium] Pass `restyutil.WithMaxIdleConns(n)` (n ≈ expected concurrent `search.users` calls, ≤ `MAX_CONCURRENCY`) when building `usersRC` — `main.go:246-249` — removes per-request handshakes under typeahead load.
- [low] Emit `now-<window>/h` (or `/d`) in `recentWindowToGte` — `query_messages.go:38, 238-257` — makes the range clause cacheable across requests.
- [low] Drop `track_total_hits` from rooms/orgs bodies (or expose `total` in the response types) — `query_rooms.go:315`, `query_orgs.go:438`.
- [low] Run the users/apps/room-name lookups in `enrichMessages` concurrently (`errgroup`, all best-effort) — `enrich.go:87-106` — cuts enrichment latency to the slowest lookup.

## 8. Prioritized action list

Ordered by severity (`critical` first), then by the dimension most likely to carry a correctness or contract cost (integration, architecture, coverage, code quality, performance, maintainability). Each item is a recommendation from the chapter named; the `file:line` and rationale are quoted from it.

1. **[high]** _Integration_ — Gate `search.users` on the real third-party contract — `users_client.go:59-60` — finish the adapter against the actual spec (path, body, auth, response fields) with a recorded-fixture test, or unregister the subject and mark `docs/client-api.md:4509` "not yet available".
2. **[high]** _Integration_ — Delete `SearchConfig.RequestTimeout` and `withRequestTimeout` — `main.go:71`, `handler.go:81` — rely solely on `cfg.Guard.TimeoutMiddleware()`; replace `SEARCH_REQUEST_TIMEOUT` with `REQUEST_TIMEOUT` in `deploy/docker-compose.yml:41`.
3. **[high]** _Test coverage_ — Lift unit coverage over 80% under the CI filter — an `httptest`-backed unit test for `users_client.go:36-73` (token header, 2xx, non-2xx, transport error, bad body) needs no build tag, plus a zero-config `newHandler` test for `handler.go:58-69`; together ~25 statements, enough to clear the gate without touching integration.
4. **[high]** _Code quality_ — Add a terminal `$project` limited to the fields `SearchAppsResponse` serialises — `query_apps.go:51-55` — MUST compliance; stops shipping full app documents (assistant config etc.) over the wire.
5. **[high]** _Maintainability_ — Resolve the `search.users` placeholder: either land the real HR contract or stop registering the RPC (config-gated, `handler.go:77`) and drop the `required` on `USERS_API_URL` (`main.go:58`) — a deployed endpoint whose wire shape is admitted guesswork is unreviewable and misleads clients reading `docs/client-api.md`.
6. **[medium]** _Integration_ — Implement subscription scoping for `search.apps` (a Go-side filter via `SubscriptionsByRoomIDs` avoids `$lookup`) — `query_apps.go:38-49` — then drop the "prototype" caveat at `docs/client-api.md:4411`.
7. **[medium]** _Integration_ — Invalidate `searchservice:restrictedrooms:{account}` from search-sync-worker's membership/HSS handler (it already sees every change), or gate Clause A/B on the live ES doc — `handler.go:258`, `store_valkey.go:23` — and document the residual window.
8. **[medium]** _Integration_ — Derive the message read pattern from config — `query_messages.go:18` — add an unprefixed `MSG_INDEX_PREFIX` (same name as the writer) and build `[]string{searchindex.IndexPattern(p), "*:"+searchindex.IndexPattern(p)}` at startup, as `SPOTLIGHT_INDEX` is handled at `main.go:179`.
9. **[medium]** _Integration_ — Switch `store_mongo.go:37-46` to `mongoutil.WarnMissingIndexes` like `:47-50`; if `{u.account, roomId}` is truly needed, add it to room-service's owned set with a justification.
10. **[medium]** _Architecture_ — Delete `SearchConfig.RequestTimeout` and `handler.withRequestTimeout`; rely solely on `Guard.RequestTimeout` via `natsrouter.DefaultGuarded(nc, "search-service", cfg.Guard, WithSiteID, WithMetrics)` — `main.go:71`, `main.go:292-297`, `handler.go:81-86` — one knob, standard middleware order, no half-applied guard.


# user-service — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `c63a2fa` (base `main`)  
**Overall score:** 3.2 / 5 (baseline 2026-09-01: 3.3, Δ -0.1)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

The sub-package layout is genuine rather than cosmetic — clean dependency direction, compile-time interface pins, mocks where CLAUDE.md puts them — and four dimensions score 4. One root cause surfaces in three of them: `HTTPConfig` re-declares `mongoutil.PoolConfig`'s fields instead of mounting the struct under `envPrefix:"HTTP_"`, on the strength of a comment that is wrong, so the HTTP Mongo client is built without `WithPool` and silently runs on the driver's 30-second server-selection timeout while the NATS path fails in 2 seconds; a quiet Mongo pins every HTTP request for its whole budget. Integration found a live subject bug: `subject.SettingsUpdate`/`ChatlistUpdate` interpolate the raw account where the sibling builder encodes it, so a bot's own settings and chatlist sync land on a subject outside its JWT scope. Cross-site status/settings/chatlist replication is a sequential PubAck loop inside the request path with no retry — a lost `muteAllNotifications` leaves the remote pushing until the user touches settings again. Coverage is 54.0%: all six chatlist RPCs and their store methods are untested at every layer, and one unit test boots a real `nats-server`.

| Dimension | Score |
|---|---|
| Code quality | 4 |
| Architecture | 4 |
| Test coverage | 1 |
| Maintainability | 3 |
| Integration | 3 |
| Performance | 4 |

**Findings by severity:** 1 critical, 6 high, 13 medium, 21 low, 7 nitpick (48 total).  
**Highest-risk dimension:** Test coverage (1).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 4

### Evidence

- [high] HTTP Mongo pool knobs re-declared in the service instead of mounting the owner's `mongoutil.PoolConfig` with `envPrefix:"HTTP_"` — `user-service/config/config.go:51-58`, `:180-194`, `:246` — CLAUDE.md: "Never re-declare the env tag and envDefault in a service". `PoolConfig`'s tags already carry `MONGO_` (`pkg/mongoutil/poolconfig.go:25-32`), so `Pool mongoutil.PoolConfig \`envPrefix:"HTTP_"\`` would read `HTTP_MONGO_MAX_POOL_SIZE` exactly as intended; the comment at `:180-182` claiming the tags "cannot be reused under HTTP_MONGO_" is wrong. Concrete consequences: the HTTP pool defaults to 128 vs the owner's 150, a hand-rolled `validateMongoPool` duplicates `PoolConfig.Validate`, and the HTTP Mongo client (`user-service/main.go:145-151`, built with `WithMaxPoolSize/WithMinPoolSize/WithMaxIdleTime`, never `WithPool`) silently keeps the driver's 30s `ServerSelectionTimeout` instead of the shared 2s bound (`pkg/mongoutil/poolconfig.go:49,67-68`) — a quiet MongoDB hangs HTTP handlers for their whole 30s budget.
- [medium] Whole-document reads with no explicit projection — `user-service/mongorepo/apps.go:84` (`GetApp` → `FindByID`), `:139-140` (`GetAppsByAssistants` → bare `FindMany`), `user-service/mongorepo/subscriptions.go:886` (`GetAppSubscription` → bare `FindOne`) — CLAUDE.md "Always project precisely: every find/aggregation MUST specify an explicit projection". Every other read in the repo layer projects; these three fetch the full `apps`/`subscriptions` docs (the app doc carries channel-tab/URL sub-documents the callers never read).
- [low] Unbounded fan-out in `GetThreadUnreadSummary` — `user-service/service/threadunread.go:58-79` — one goroutine per distinct site with a `WaitGroup` but no semaphore, while every sibling fan-out in the service (`subscriptions.go:496`, `badge.go`, `threads.go:224`, `threadunread.go:147-148`) bounds itself with `s.fanout()`. Sites come from row data, not config, so the bound is only the 500-row read cap.
- [low] `service.New` takes 14 positional dependencies including two adjacent same-typed `EventPublisher` values (`pub, clientPub`) — `user-service/service/service.go:202`; wired twice in `user-service/main.go:231` and `:239-244`. A swap of `publisher.New(js)`/`publisher.NewCore(nc)` compiles and silently sends federation events over core NATS.
- [low] `badgeCache` interface duplicated in `main` to work around the unexported one in `service` — `user-service/main.go:62-71` mirrors `user-service/service/service.go:92-101`, with `noopBadgeCache` (`main.go:77-84`) living in `main` rather than beside the consumer. Two copies of one contract that must be kept in step by hand.
- [low] Hand-rolled `chunkStrings` duplicates stdlib `slices.Chunk` already used in the same package — `user-service/service/threadunread.go:179-195` vs `user-service/service/subscriptions.go:323`.
- [low] Stringly-typed list type crosses the service/repo boundary with no shared constant — `user-service/service/subscriptions.go:24` (`validListTypes`) vs literals `"current"/"rooms"/"apps"` re-spelled in `user-service/mongorepo/subscriptions.go:280-290` and `:359`. A drift on either side returns an empty page rather than failing.
- [low] Four `$lookup` sites lack the inline `// $lookup justification:` comment CLAUDE.md requires — `user-service/mongorepo/subscriptions.go:101`, `:156`, `:682`, `:732` — while the sibling joins in the same package carry one (`subscriptions.go:854`, `apps.go`, `threadsubscriptions.go`). All four blame to one 2026-08-25 commit, so grandfathering is unverifiable.
- [nitpick] Parameter named `cap` shadows the builtin — `user-service/service/badge.go:111` (`cappedUnion(ids, trigger, cap int)`).
- [nitpick] `UserStatusView` mapping copy-pasted four times — `user-service/service/me.go:26-32`, `service/status.go:31-37`, `:56-62`, `:81-87`; `GetStatusByName`/`GetProfileByName` (`status.go:19`, `:44`) are byte-identical apart from the wrap string.
- [nitpick] Exported handler methods without doc comments — `user-service/service/status.go:19`, `:65`; `service/apps.go:17`, `:134`; `service/subscriptions.go:732`. Also `user-service/store.go:13` puts a service-slice interface (`subscriptionLister`) in a file the repo layout reserves for the Store interface.

### Recommendations

- [high] Replace `HTTPConfig.MongoMaxPoolSize/MinPoolSize/MaxIdleTime` with `HTTPPool mongoutil.PoolConfig \`envPrefix:"HTTP_"\``, validate via `cfg.HTTPPool.Validate()`, and build the HTTP client with `mongoutil.WithPool(cfg.HTTPPool)` — `config/config.go:51-58,183-194,246`, `main.go:145-151` — deletes `validateMongoPool`, aligns defaults with the owner, and gives the HTTP client the 2s server-selection bound it currently lacks. Env names are unchanged, so no deploy edits.
- [medium] Add explicit projections to `GetApp`, `GetAppsByAssistants`, `GetAppSubscription` — `mongorepo/apps.go:84,140`, `mongorepo/subscriptions.go:886` — listing the fields `AppSubscriptionFromApp` and the reactivation event consume; add a tag-vs-projection guard test like `TestSubscriptionFieldsProjection_MatchesModelTags`.
- [low] Bound `GetThreadUnreadSummary`'s per-site goroutines with the same `sem := make(chan struct{}, s.fanout())` pattern used at `threadunread.go:147-148`.
- [low] Introduce a `service.Deps` struct (or named options) for `service.New` and export `BadgeCache` + `NoopBadgeCache` from `service` — `service/service.go:92-101,202`, `main.go:62-84` — removes the duplicated interface and the swap-prone positional publishers.
- [low] Replace `chunkStrings` with `slices.Chunk` (`threadunread.go:63,179-195`) and move the list-type vocabulary into a typed constant set shared by `service` and `mongorepo` (`service/subscriptions.go:24`, `mongorepo/subscriptions.go:280-290,359`).
- [nitpick] Rename `cap` → `limit` in `cappedUnion` (`badge.go:111`), factor a `statusView(u *model.User)` helper for the four copies, and add doc comments to the five exported handlers listed above.

## 3. Architecture — score 4

### Evidence

- [medium] Cross-site user_status/settings/chatlist replication is fire-and-forget direct JetStream publish, not OUTBOX — `user-service/service/settings.go:138-158`, `user-service/service/status.go:101-121`, `user-service/service/chatlist.go:202-223`, `user-service/publisher/publisher.go:26-31` — Each handler loops `allSiteIDs` sequentially, awaits PubAck under the request ctx, and on failure only `slog.Warn`s ("the next set re-broadcasts"). A lost publish leaves the remote replica stale indefinitely (remote `notification-worker` reads the settings replica for push decisions per the comment at settings.go:129-131). Because the handler ctx carries `HANDLER_TIMEOUT` (main.go:263), nats.go uses that deadline instead of its 5s default (`nats.go@v1.50.0/jetstream/jetstream.go:1234-1238`), so one unreachable peer can consume the whole 15s budget after the Mongo write already committed. `pkg/outbox` exists precisely to make request/reply federation durable (`pkg/outbox/outbox.go:1-4, 22-47`); the user_* types are in neither partition. CLAUDE.md names user-service as a direct publisher, so this is a robustness gap, not a MUST violation.
- [low] `service.badgeCache` is unexported, so `main` re-declares it structurally and hosts the no-op implementation — `user-service/main.go:62-84` vs `user-service/service/service.go:92-101` — Two copies of one consumer interface; a method added to the service copy only breaks the `service.New` call site, and the null object lives outside the package that owns the contract.
- [low] `service.New` takes 14 positional dependencies and is built twice with duplicated client/publisher wiring; the HTTP instance shares NATS-pool repos — `user-service/service/service.go:202`, `user-service/main.go:231-232, 239-244` — `httpSvc` receives `threadSubRepo` and `ssoTokenRepo` bound to the NATS-path Mongo client (main.go:175-176, 242, 244), so the "HTTP-only pool" property (main.go:141-144) holds only because `subscriptionLister` (`user-service/store.go:13-16`) exposes two methods; a future HTTP route would silently cross pools.
- [low] `service/` couples business logic to the NATS transport — `user-service/service/status.go:19`, `user-service/service/settings.go:55`, `user-service/service/chatlist.go:45` (every handler takes `*natsrouter.Context`) — Only `ListSubscriptionsFor`/`CountSubscriptionsFor` (`user-service/service/subscriptions.go:65, 741`) are transport-neutral; the rest cannot be reused by the HTTP surface without the same split, so `service/` is in practice the NATS handler layer rather than a service layer.
- [low] `GetThreadUnreadSummary` fans out without the shared semaphore — `user-service/service/threadunread.go:58-80` — Every sibling fan-out bounds goroutines with `s.fanout()`/`badgeSeedFanout()` (`badge.go:47`, `subscriptions.go:813`, `threadunread.go:148`, `threads.go:224`); this one spawns one goroutine per site unbounded. Practically capped by `len(ALL_SITE_IDS)`, but it is the one path that ignores `MAX_SITE_FANOUT`.
- [nitpick] `$lookup` justification comments are applied inconsistently — `user-service/mongorepo/subscriptions.go:101, 156, 682, 732` carry none while `subscriptions.go:854`, `apps.go:92`, `threadsubscriptions.go:48` do — The file has been touched repeatedly (#382, #461, #491 per `git log`), so the grandfather clause is thin for the unannotated sites.

### Recommendations

- [medium] Route `user_status_updated`/`user_settings_updated`/`user_chatlist_updated` through `outbox.Publish` and add the three types to `outbox.ConcurrentEventTypes` (they are last-write-wins / HWM-guarded per the handler comments) — `user-service/service/settings.go:132-159`, `status.go:93-122`, `chatlist.go:200-223`, `pkg/outbox/outbox.go:22` — Makes the replica durable against a down peer, drops the per-peer PubAck wait out of the request path, and lets `publisher.Publisher` (JetStream) be retired in favour of the local OUTBOX publish. Update CLAUDE.md's "cross-site publishers" sentence in the same PR.
- [medium] Until the outbox migration lands, run the per-peer INBOX publishes concurrently under a short detached-with-deadline ctx (or `s.fanout()` semaphore) so one stalled gateway cannot spend the whole handler budget after commit — `user-service/service/settings.go:138`, `status.go:101`, `chatlist.go:202`.
- [low] Export `service.BadgeCache` (or add `service.NoopBadgeCache`) and delete the mirror in `main` — `user-service/main.go:62-84`, `user-service/service/service.go:92`.
- [low] Replace the 14-arg constructor with a `service.Deps` struct and build the HTTP-path repos from `httpDB` for every repo (or make `httpSvc` a narrower type that only holds `subs/users/apps/rooms/history/badge`) — `user-service/service/service.go:202`, `user-service/main.go:239-244` — Removes the latent pool-crossing and makes the two instances' differences explicit.
- [low] Bound `GetThreadUnreadSummary` with the same `sem := make(chan struct{}, s.fanout())` pattern its siblings use — `user-service/service/threadunread.go:58-80`.
- [nitpick] Add `// $lookup justification:` lines to the four unannotated joins in `user-service/mongorepo/subscriptions.go:101, 156, 682, 732`.

## 4. Test coverage — score 1

### Evidence

- [critical] coverage below repo minimum 80%, currently 54.0% — `user-service/main.go:87` — Aggregate 1264/2339 statements across the sub-packages; per CLAUDE.md §4 this is un-mergeable. The top-level `main` package is 28.0%: `main` (`main.go:87`), `startHTTPServer` (`main.go:353`), `oidcValidator` (`oidc.go:15`) and every `noopBadgeCache` method (`main.go:79-84`) are 0%. `startHTTPServer` is unit-testable as written (takes `cfg`, `svc`, `sdk`, `sso`, `serveFailed`) yet neither its listen-failure branch (`main.go:394-397`) nor the serve-failed self-SIGTERM path (`main.go:403-410`) is exercised.
- [high] Six client-facing NATS handlers have zero tests at any layer — `user-service/service/chatlist.go:27` — `GetChatlist`, `CreateChatlistSection` (:45), `DeleteChatlistSection` (:74), `RenameChatlistSection` (:91), `ReorderChatlistSections` (:116), `SetChatlistSectionSortMode` (:131), plus `mutateChatlist` (:153) and both fan-out publishers (:190, :200) are 0%. They are registered on `chat.user.…` subjects (`service/service.go:278-283`). `service/chatlist_test.go` only covers the pure helpers (`validateSectionName`, `isPermutation`, …); no test builds the handler with the mocks in `service_test.go:32`. The store side is equally untested: `mongorepo/users.go:167 GetUserChatlist` and `:177 UpdateUserChatlist` have no call site in any `mongorepo/*_test.go`, so the `$set` of `chatlistUpdatedAt` (`users.go:185`) that guards the cross-site HWM has never been verified against Mongo. CLAUDE.md §4: "Every handler method must have tests for valid input, invalid input, store errors, and edge cases."
- [high] Unit test spins up a real NATS server — `user-service/historyclient/client_test.go:26` — `startTestNATS` embeds `nats-server/v2` in an untagged `package historyclient` test, violating "Never connect to real databases, NATS, or external services in unit tests." The sibling clients do it correctly (`roomclient/client_integration_test.go:1,30,36`, `presenceclient/client_integration_test.go:1,30` — `//go:build integration` + `testutil.NATS` + `TestMain`). Even with the server, `GetThreadList` (`historyclient/client.go:33`) is 0%: the file tests only `RoomsGet`.
- [medium] HTTP count handler error paths untested — `user-service/handler.go:80` — Blocks `:80-83` (bind failure → 400) and `:86-89` (service error → `errhttp.Write`) have zero hits; `handler_test.go:244 TestHandler_CountBindsUnread` covers only the success mapping, while `ListSubscriptions` gets full malformed-query and error-code tables (`handler_test.go:150,169`). Function is 63.6%.
- [medium] Thread-list degradation branches untested — `user-service/service/threads.go:207` — The local-site `GetThreadList` failure branch (`:207-210`, sets `failed=true`) and both cancelled-context guards in the cross-site fan-out (`:226-228`, `:235-238`) are 0%; `enrichLocalThreads` is 57.1%. These are exactly the "site down / request cancelled mid-fan-out" paths the comment at `:216-218` promises degrade gracefully.
- [medium] `RegisterHandlers` never exercised — `user-service/service/service.go:268` — 0%. No test asserts the 30 subject→handler bindings; a dropped or mis-patterned `natsrouter.Register` line would ship silently.
- [low] Best-effort publish failure not covered — `user-service/service/apps.go:129` — `publishSubscriptionUpdate` is 50%: the `Publish` error/log branch (`:129-131`) and `:112-114` are unhit, so the "best-effort, never fails the request" contract is asserted only by the happy path.
- [low] `chatlist_test.go` ignores the repo test conventions — `user-service/service/chatlist_test.go:10` — Uses raw `t.Errorf` loops instead of testify and no `t.Run`/table for its ok/bad name lists (`:11-24`), unlike every other file in the package (e.g. `subscriptions_test.go` with 14 subtests).

### Recommendations

- [critical] Add handler tests for the six chatlist RPCs using `newSvc` mocks — `service/chatlist_test.go` — Table per handler: user missing (`NotFound`), duplicate name (`Conflict`/`UserChatlistDuplicateName`), built-in immutable, bad permutation, store error on `GetUserChatlist`/`UpdateUserChatlist`, and assert `pub.Publish` is called for each site in `AllSiteIDs` except self. This alone lifts `service` past 90% and removes the largest client-facing blind spot.
- [high] Add `mongorepo` integration tests for `GetUserChatlist`/`UpdateUserChatlist` — `mongorepo/users.go:167,177` — Mirror `settings_test.go`: never-customised → `Chatlist == nil`, inactive user → `(nil, nil)`, and assert the raw document's `chatlistUpdatedAt` equals `state.LastUpdatedAt` after the write.
- [high] Move `historyclient/client_test.go` to `//go:build integration`, `testutil.NATS(t)` + `TestMain(testutil.RunTests)`, and add `GetThreadList` cases — `historyclient/client_test.go:26` — Brings it in line with `roomclient`/`presenceclient` and drops `nats-server` from the unit binary.
- [medium] Cover `startHTTPServer` — `main.go:353` — Bind to `:0`, assert the returned server is listening and `cancelInFlight` cancels `BaseContext` with `service.ErrShuttingDown`; second case: pre-occupy a port and assert the listen error and that `cancelInFlight` already ran. Use a noop `o11y.SDK`.
- [medium] Extend `handler_test.go` count cases — `handler.go:80-89` — Add `unread=maybe` → 400 and a mock returning `errcode.NotFound`/raw error → 404/500, matching the list-handler tables.
- [medium] Add degraded-path cases for `ListUserThreads` — `service/threads.go:207,226,235` — Mock `history.GetThreadList` to fail for the local site and pass a pre-cancelled context for the cross-site loop; assert `failed` rows and no panic.
- [low] Add a `RegisterHandlers` smoke test — `service/service.go:268` — Register against a `natsrouter.New` over a nil/embedded conn or assert route count; guards the subject table.

## 5. Maintainability — score 3

### Evidence

- [high] HTTP Mongo pool knobs re-declare `mongoutil.PoolConfig`'s env tags and validator instead of mounting the shared struct with `envPrefix` — `user-service/config/config.go:51-58`, `:183-194` — CLAUDE.md: "Never re-declare the env tag and envDefault in a service… a service that needs its own env prefix puts envPrefix on the field". The comment at `:180-182` says the tags "cannot be reused under HTTP_MONGO_", but `envPrefix:"HTTP_"` yields exactly `HTTP_MONGO_MAX_POOL_SIZE` (precedent: `history-service/internal/config/config.go:129`, `broadcast-worker/main.go:81`). The copy has already drifted: it lacks `ServerSelectionTimeout`, so `main.go:145-151` wires the HTTP client via three per-field options rather than `WithPool`.
- [medium] `service.New` takes 14 positional dependencies and is called twice with near-duplicate repo construction — `user-service/service/service.go:202`, `user-service/main.go:231-232`, `:239-244`. Every test helper re-lists all 14 (`service/service_test.go:52`, `subscriptions_test.go:26`); adding one dependency touches `New`, both `main` call sites and every helper. `main()` is 247 lines (`main.go:87-333`).
- [medium] Bounded fan-out is hand-rolled five times with the same `wg`+`sem` shape — `service/subscriptions.go:496` (generic `fanOutChunks`), `:813` (`unreadRooms`), `service/badge.go:47`, `service/threads.go:224`, `service/threadunread.go:148` — while `threadunread.go:58-79` (`GetThreadUnreadSummary`) spawns one goroutine per site with no semaphore at all, so the `MAX_SITE_FANOUT` bound is inconsistently applied.
- [medium] Cross-site INBOX publish loop copied three times — `service/status.go:93-122`, `service/settings.go:132-159`, `service/chatlist.go:200-223` — identical 25-line `for _, dest := range s.allSiteIDs` bodies differing only in event type and log message. A change to the envelope (e.g. a new `InboxEvent` field) is a three-file edit.
- [low] Oversized files and functions: `service/subscriptions.go` 874 lines mixing handlers, chunk planning, preview truncation, payload-cap context keys and unread math; `mongorepo/subscriptions.go` 898 lines; `service/subscriptions_test.go` 1558 lines. `unreadRooms` 97 lines (`subscriptions.go:778-874`), `GetThreadUnreadSummary` 89 (`threadunread.go:22-110`), `ListUserThreads` 88 (`threads.go:59-146`), `BadgeCountBatch` 86 (`badge.go:19-104`), `config.Load` 104 (`config/config.go:197-300`).
- [low] Test files do not map to sources: `service/enrich_test.go` (846 lines, 33 tests) and `service/threadfit_test.go` exercise code living in `subscriptions.go`/`threads.go`; there is no `enrich.go`. A new engineer must grep to find `fanOutChunks`' tests.
- [low] `GetProfileByName` is byte-identical to `GetStatusByName` except the error string — `service/status.go:44-63` vs `:19-38` (verified with `diff`); the `models.UserStatusView{…}` literal is repeated four times (`status.go:31`, `:56`, `:81`, `me.go:26`).
- [low] `chunkStrings` (`service/threadunread.go:179-195`) reimplements `slices.Chunk`, already used at `service/subscriptions.go:323`.
- [low] `badgeCache` interface and `noopBadgeCache` live in `main.go:66-84` as a structural mirror of the unexported `service.badgeCache` (`service/service.go:92`) — the comment admits it "can't be named here". Leaky seam; the no-op belongs beside the interface it satisfies.
- [nitpick] Knob naming drifts between transports: `MAX_SUBSCRIPTION_LIMIT` (`config.go:77`) vs `HTTP_SUBSCRIPTION_MAX_LIMIT` (`:63`); `HANDLER_TIMEOUT` (`:82`) vs `HTTP_HANDLER_TIMEOUT` (`:46`). ~45 top-level knobs in one struct.

### Recommendations

- [high] Replace `HTTPConfig.MongoMaxPoolSize/MinPoolSize/MaxIdleTime` with `Pool mongoutil.PoolConfig \`envPrefix:"HTTP_"\``, delete `validateMongoPool`, and wire the HTTP client with `mongoutil.WithPool(cfg.HTTP.Pool)` — `config/config.go:51-58,183-194`, `main.go:145-151` — removes the CLAUDE.md violation and picks up `ServerSelectionTimeout` and any future pool field for free; operator-facing names are unchanged.
- [medium] Collapse `service.New`'s 14 positionals into a `Deps` struct (repos, clients, publishers, badge) + `cfg`; build the two instances in `main.go` from one `newDeps(db)` helper — `service/service.go:202`, `main.go:231-244` — one place to add a dependency; test helpers shrink to a struct literal.
- [medium] Extract `publishInbox(c, eventType, payload, now)` and route `publishStatus`/`publishSettingsInbox`/`publishChatlistInbox` through it — `status.go:93`, `settings.go:132`, `chatlist.go:200` — one envelope, one log line, one test.
- [medium] Add a small `forEachBounded(ctx, n, fanout, fn)` helper (or reuse `fanOutChunks`) and use it in `unreadRooms`, `BadgeCountBatch`, `enrichCrossSiteThreads`, `ClearAllThreadUnread`, and give `GetThreadUnreadSummary` the same bound — `threadunread.go:58-79` — consistent back-pressure and one cancellation-recheck to get right.
- [low] Split `service/subscriptions.go` into `subscriptions.go` (handlers, `unreadRooms`) and `enrich.go` (`enrichWithRoomInfoAndLastMsg`, chunking, splitting, truncation, `fanOutChunks`), so `enrich_test.go` finally has a source file — also brings the file under ~500 lines.
- [low] Make `GetProfileByName` delegate to a shared `statusView(u)` helper and delete `chunkStrings` in favour of `slices.Chunk` — `status.go:44-63`, `threadunread.go:179`.
- [low] Export `service.BadgeCache` and move `NoopBadgeCache` into `service` — `main.go:62-84` — deletes the mirrored interface.

## 6. Integration — score 3

### Evidence

- [high] HTTP Mongo client re-declares pool knobs instead of mounting `mongoutil.PoolConfig` under a prefix — `user-service/config/config.go:51-58,180-194`, `user-service/main.go:145-151` — The comment at config.go:180-182 claims PoolConfig's tags "cannot be reused under HTTP_MONGO_", yet `message-gatekeeper/main.go:59` and `history-service/internal/config/config.go:129` mount `BreakerConfig` with `envPrefix` exactly as CLAUDE.md prescribes. Real consequence: `WithMaxPoolSize/WithMinPoolSize/WithMaxIdleTime` never set `ServerSelectionTimeout` (only `WithPool` does — `pkg/mongoutil/poolconfig.go:67-69`, `tuning.go:58-59`), so the HTTP client runs on the driver's 30s default (`pkg/mongoutil/mongo.go:53`), equal to `HTTP_HANDLER_TIMEOUT`; a quiet Mongo pins every HTTP request for its whole budget and an `HTTP_MAX_CONCURRENCY` slot, while the NATS path fails in 2s.
- [high] `BADGE_CACHE_TTL` declared with its own `envDefault` in three writers — `user-service/config/config.go:141`, `room-service/main.go:124`, `inbox-worker/main.go:65` — `pkg/badgecache` carries no env tags; the config comment itself says "Keep identical across the badge cache's writers", which is the tag-level drift CLAUDE.md forbids. `BADGE_MARKER_TTL` (config.go:148) exists only here; room-service/inbox-worker construct without `WithMarkerTTL` (`room-service/main.go:345`, `inbox-worker/main.go:892`) so marker=set TTL (`pkg/badgecache/badgecache.go:106`). Harmless today because only user-service calls Seed/Reseed, but unguarded.
- [medium] `settings.update`/`chatlist.update` fan-out for a bot caller lands on the wrong subject — `pkg/subject/subject.go:266-274` interpolates the raw account while `SubscriptionUpdate` (:257-260) encodes; natsrouter decodes `{account}` (`pkg/natsrouter/params.go:111`), so `service/settings.go:124` and `service/chatlist.go:192` publish `chat.user.weather.site-a.bot.event.settings.update` — outside the bot's JWT scope (`chat.user.weather_site-a_bot.>`) and inside user `weather`'s `chat.user.weather.>` wildcard (`docs/client-api.md:180`). Bots (which the docs say can log into the frontend) never receive their own settings/chatlist sync.
- [medium] Cross-site status/settings/chatlist replication is a synchronous, sequential, lossy JetStream publish in the request path — `service/status.go:101-121`, `service/settings.go:138-158`, `service/chatlist.go:202-222`, `publisher/publisher.go:27` — PubAck waits under the handler ctx (nats.go `jetstream/jetstream.go:1234-1238` keeps a ctx deadline as-is), so a slow/leaderless remote INBOX holds a `MAX_CONCURRENCY` slot (`main.go:250-253`, shared with every read endpoint) for up to `HANDLER_TIMEOUT`=15s (`config.go:82`) per mutation, after which the event is dropped ("next set re-broadcasts"). A lost `muteAllNotifications=true` leaves the remote notification-worker (`inbox-worker/handler.go:258-262` is the only consumer) pushing until the user touches settings again. CLAUDE.md sanctions user-service as a direct publisher, but OUTBOX exists for precisely this failure.
- [low] Doc drift in `docs/client-api.md` — `:4647` says "19 NATS request/reply endpoints" vs 28 listed and registered (`service/service.go:269-296`); `:4649` "No other endpoint emits a client-facing event" contradicts `setAppSubscription` (`:5700`) and chatlist mutations (`:5222`); `:9126` `HTTP_MAX_CONCURRENCY` "default 256" vs `config.go:41` (512); Chatlist RPC table (`:5237`) names `chatlist.section.setSortMode` while the builder and both subject tables are lowercase `setsortmode` (`pkg/subject/subject.go:1560-1565`, `docs/client-api/request-reply.md:1554`).
- [nitpick] `ADMIN_ACCT_PREFIX` re-declared with `envDefault` in nine services (`config.go:130` et al.) though `pkg/model` owns it — repo-wide pattern, not specific to this service.

### Recommendations

- [high] Replace `HTTPConfig.MongoMaxPoolSize/MinPoolSize/MaxIdleTime` and `validateMongoPool` with `HTTPPool mongoutil.PoolConfig \`envPrefix:"HTTP_"\`` validated via `.Validate()` and applied with `mongoutil.WithPool(cfg.HTTPPool)` — `config.go:51-58,180-194`, `main.go:145-151` — restores the 2s server-selection bound on the HTTP pool and deletes the duplicate validator.
- [high] Introduce a `badgecache.TTLConfig` (set TTL, marker TTL, count cap) with the env tags, mounted as a named field by user-service, room-service and inbox-worker — `config.go:141-151` — so the three writers cannot diverge.
- [medium] Make `subject.SettingsUpdate`/`ChatlistUpdate` call `EncodeAccount` like `SubscriptionUpdate` — `pkg/subject/subject.go:266-274` — and add a bot-account case to `service/settings_test.go`/`chatlist_test.go` asserting the encoded subject.
- [medium] Route the three inbox replications through `outbox.Publish` on the concurrent lane (or, minimally, publish after the reply with a per-publish timeout well under `HANDLER_TIMEOUT`) — `status.go:117`, `settings.go:154`, `chatlist.go:218` — so a degraded peer neither stalls the caller nor loses the event.
- [low] Fix the four doc drifts — `docs/client-api.md:4647,4649,5237,9126`.

## 7. Performance — score 4

### Evidence

- [medium] `badge.count.batch` accepts an unbounded account list and keeps seeding after its caller has given up — `user-service/service/badge.go:20` (only checks non-empty) and `:56-68` (breaks on `c.Err()`, which is the 15s HANDLER_TIMEOUT at `user-service/config/config.go:82`, not the caller's deadline). notification-worker sends one un-chunked RPC per home site with a 5s timeout (`notification-worker/handler.go:366-385`, `notification-worker/badge_client.go:19`). Each miss runs the $lookup aggregate over up to MAX_SUBSCRIPTION_LIMIT=1000 rows (`user-service/mongorepo/subscriptions.go:829-837`) plus per-site RPCs, 8 at a time. Because a NATS request timeout does not propagate to the responder, a large-room push on a cold marker (BADGE_MARKER_TTL 10m, `config.go:148`; bumps never refresh it) leaves up to ~10s of orphaned Mongo work per batch, and MAX_CONCURRENCY(256) x 8 such aggregates is the reachable peak.
- [medium] Whole-document fetches with no projection (CLAUDE.md "Always project precisely"): `GetAppSubscription` `user-service/mongorepo/subscriptions.go:886`; `GetApp` `user-service/mongorepo/apps.go:85`; `GetAppsByAssistants` `apps.go:140`. The last runs on every `subscription.list` page containing botDMs (`user-service/service/subscriptions.go:166`) and every thread page (`service/threads.go:297`). `mongoutil.FindOne/FindByID/FindMany` add no default projection (`pkg/mongoutil/collection.go:43-73`).
- [medium] `GetDMSubscription` `$lookup`s the full users document (`user-service/mongorepo/subscriptions.go:732`, localField/foreignField form, no pipeline projection) to read three fields (`:737-740`); a users doc carries settings, permissions, chatlist and priorityContacts. No inline `$lookup justification` either. Related: `ListApps` (`apps.go:95-111`) runs its correlated sub-lookup for every app in the catalog before `$sort/$skip/$limit` (appended at `pkg/mongoutil/collection.go:145-148`), and the sub-pipeline has no `$project`, so whole subscription docs are materialised only to be `$size`d (`apps.go:107`).
- [low] `FindChannelsByMembers` runs two correlated `$lookup`s across all of the account's channel subs and then an in-memory `$sort` on the joined `__matchedRoom.createdAt` (`user-service/mongorepo/subscriptions.go:673-718`) on every page; no index can serve that sort, and the `$regexMatch` inside `$expr` (`:688`) evaluates per candidate row. Bounded by the account's channel count.
- [low] Cross-site INBOX publishes on the synchronous reply path are sequential and each blocks on a JetStream PubAck: `user-service/service/status.go:477-497`, `service/settings.go:636-656`, `service/chatlist.go:202-222` (`publisher/publisher.go:307`). SetStatus/SetSettings latency grows with ALL_SITE_IDS, and one slow peer holds the reply until the handler deadline; not durable either (room-service's equivalent rides OUTBOX).
- [low] `GetThreadUnreadSummary` spawns one goroutine per site with no semaphore (`user-service/service/threadunread.go:402-404`), unlike every other fan-out in the service (`s.fanout()` at `service/subscriptions.go:813`, `threadunread.go:491`). Bounded only by distinct site count.
- [nitpick] Two `SubscriptionRepo` instances (NATS pool `user-service/main.go:169`, HTTP pool `:240`) each own a 100k-entry sort-key LRU (`config/config.go:107`), doubling memory for the same rooms and halving the hit rate of each.

### Recommendations

- [medium] Cap `req.Accounts` in `BadgeCountBatch` (BadRequest above a documented N) and chunk in notification-worker's `fetchUnreadCounts`; derive each seed's ctx with a deadline at or below the caller's 5s so orphaned aggregates stop when the push has already moved on — `service/badge.go:20`, `notification-worker/handler.go:383`.
- [medium] Add explicit projections to `GetAppSubscription` (reuse `subscriptionFieldsProjection()`), `GetApp` and `GetAppsByAssistants` (an `appFieldsProjection()` with a tags-match test like `TestSubscriptionFieldsProjection_MatchesModelTags`) — `mongorepo/subscriptions.go:886`, `mongorepo/apps.go:85,140`.
- [medium] Convert the users join in `GetDMSubscription` to the pipeline form with `$project {account, chineseName, engName}` and add the justification comment — `mongorepo/subscriptions.go:732`. For `ListApps`, page the apps first (indexed name sort + skip/limit) and resolve `isSubscribed` with one `$in` over the page's bot names, which also retires that `$lookup` — `mongorepo/apps.go:91-111`.
- [low] Fan the per-site INBOX publishes out concurrently under `s.fanout()`, or route them through OUTBOX like room-service, so reply latency stops scaling with site count — `service/status.go:477`, `settings.go:636`, `chatlist.go:202`.
- [low] Use the `s.fanout()` semaphore in `GetThreadUnreadSummary` for consistency with the other fan-outs — `service/threadunread.go:402`.
- [nitpick] Share one `sortKeyCache` between the NATS and HTTP `SubscriptionRepo` instances — `main.go:169,240`.

## 8. Prioritized action list

Ordered by severity (`critical` first), then by the dimension most likely to carry a correctness or contract cost (integration, architecture, coverage, code quality, performance, maintainability). Each item is a recommendation from the chapter named; the `file:line` and rationale are quoted from it.

1. **[critical]** _Test coverage_ — Add handler tests for the six chatlist RPCs using `newSvc` mocks — `service/chatlist_test.go` — Table per handler: user missing (`NotFound`), duplicate name (`Conflict`/`UserChatlistDuplicateName`), built-in immutable, bad permutation, store error on `GetUserChatlist`/`UpdateUserChatlist`, and assert `pub.Publish` is called for each site in `AllSiteIDs` except self. This alone lifts `service` past 90% and removes the largest client-facing blind spot.
2. **[high]** _Integration_ — Replace `HTTPConfig.MongoMaxPoolSize/MinPoolSize/MaxIdleTime` and `validateMongoPool` with `HTTPPool mongoutil.PoolConfig \`envPrefix:"HTTP_"\`` validated via `.Validate()` and applied with `mongoutil.WithPool(cfg.HTTPPool)` — `config.go:51-58,180-194`, `main.go:145-151` — restores the 2s server-selection bound on the HTTP pool and deletes the duplicate validator.
3. **[high]** _Integration_ — Introduce a `badgecache.TTLConfig` (set TTL, marker TTL, count cap) with the env tags, mounted as a named field by user-service, room-service and inbox-worker — `config.go:141-151` — so the three writers cannot diverge.
4. **[high]** _Test coverage_ — Add `mongorepo` integration tests for `GetUserChatlist`/`UpdateUserChatlist` — `mongorepo/users.go:167,177` — Mirror `settings_test.go`: never-customised → `Chatlist == nil`, inactive user → `(nil, nil)`, and assert the raw document's `chatlistUpdatedAt` equals `state.LastUpdatedAt` after the write.
5. **[high]** _Test coverage_ — Move `historyclient/client_test.go` to `//go:build integration`, `testutil.NATS(t)` + `TestMain(testutil.RunTests)`, and add `GetThreadList` cases — `historyclient/client_test.go:26` — Brings it in line with `roomclient`/`presenceclient` and drops `nats-server` from the unit binary.
6. **[high]** _Code quality_ — Replace `HTTPConfig.MongoMaxPoolSize/MinPoolSize/MaxIdleTime` with `HTTPPool mongoutil.PoolConfig \`envPrefix:"HTTP_"\``, validate via `cfg.HTTPPool.Validate()`, and build the HTTP client with `mongoutil.WithPool(cfg.HTTPPool)` — `config/config.go:51-58,183-194,246`, `main.go:145-151` — deletes `validateMongoPool`, aligns defaults with the owner, and gives the HTTP client the 2s server-selection bound it currently lacks. Env names are unchanged, so no deploy edits.
7. **[high]** _Maintainability_ — Replace `HTTPConfig.MongoMaxPoolSize/MinPoolSize/MaxIdleTime` with `Pool mongoutil.PoolConfig \`envPrefix:"HTTP_"\``, delete `validateMongoPool`, and wire the HTTP client with `mongoutil.WithPool(cfg.HTTP.Pool)` — `config/config.go:51-58,183-194`, `main.go:145-151` — removes the CLAUDE.md violation and picks up `ServerSelectionTimeout` and any future pool field for free; operator-facing names are unchanged.
8. **[medium]** _Integration_ — Make `subject.SettingsUpdate`/`ChatlistUpdate` call `EncodeAccount` like `SubscriptionUpdate` — `pkg/subject/subject.go:266-274` — and add a bot-account case to `service/settings_test.go`/`chatlist_test.go` asserting the encoded subject.
9. **[medium]** _Integration_ — Route the three inbox replications through `outbox.Publish` on the concurrent lane (or, minimally, publish after the reply with a per-publish timeout well under `HANDLER_TIMEOUT`) — `status.go:117`, `settings.go:154`, `chatlist.go:218` — so a degraded peer neither stalls the caller nor loses the event.
10. **[medium]** _Architecture_ — Route `user_status_updated`/`user_settings_updated`/`user_chatlist_updated` through `outbox.Publish` and add the three types to `outbox.ConcurrentEventTypes` (they are last-write-wins / HWM-guarded per the handler comments) — `user-service/service/settings.go:132-159`, `status.go:93-122`, `chatlist.go:200-223`, `pkg/outbox/outbox.go:22` — Makes the replica durable against a down peer, drops the per-peer PubAck wait out of the request path, and lets `publisher.Publisher` (JetStream) be retired in favour of the local OUTBOX publish. Update CLAUDE.md's "cross-site publishers" sentence in the same PR.


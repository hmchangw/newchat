# bot-room-service — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `1b686d0` (base `main`)  
**Overall score:** 2.5 / 5 (baseline 2026-09-01: 2.8, Δ -0.3)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

bot-room-service is the bot-platform counterpart of room-service, and its handler logic, error tiering and room-key rotation net read well — but three reviewers independently found the same broken wire contract. Its system messages publish a bare `model.Message` onto BOT-MESSAGES-CANONICAL while every consumer on that stream decodes a `MessageEvent` envelope, so each membership system message decodes to a zero-valued message: bot-message-worker tries to persist an empty partition key and NAK-loops it through the retry budget, and search-sync-worker drops it as poison. Every bot membership system message is lost from history, and the service's own tests pin the wrong shape. Two further contract breaks sit in MongoDB: rooms are written with a legacy `t:"c"/"d"` field instead of the canonical `type`, so room-service and the room-meta cache decode bot rooms as typeless, and subscription rows omit `joinedAt`, `open` and `roles`, which the member-list index and sidebar visibility depend on, so a locally-created membership and its federated copy diverge. The `member_added` dedup ID is keyed on the room/user/destination triple rather than the subscription ID that the remove path already uses, so a remove-then-re-add inside the duplicate window silently drops the re-add. Member batches are unbounded and processed as sequential N+1 round trips under one 10-second budget, and the deferred key-rotation safety net runs on the request context, so when the failure is the timeout the net cannot fire. Coverage is 49.4% with 14 of 30 functions at zero, no mockgen mocks, and no `deploy/azure-pipelines.yml` at all — a gap shared by all three bot services.

| Dimension | Score |
|---|---|
| Code quality | 3 |
| Architecture | 3 |
| Test coverage | 1 |
| Maintainability | 3 |
| Integration | 2 |
| Performance | 3 |

**Findings by severity:** 1 critical, 10 high, 18 medium, 19 low, 7 nitpick (55 total).  
**Highest-risk dimension:** Test coverage (1).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 3

### Evidence

- [high] Unit coverage is 49.4% (204/413 stmts), below the 80% floor CLAUDE.md marks as must-not-merge — `bot-room-service/store_mongo.go:23-196`, `bot-room-service/main.go:64-171` — Every `store_mongo.go` function, `Register`, `run` and `parsePeers` are 0%. `store_mongo.go` is covered only by `integration_test.go` (tagged), so the unit profile never sees it; `parsePeers` is pure string logic with no test at all.
- [high] No `deploy/azure-pipelines.yml`, so no per-service CI gate exists — `bot-room-service/deploy/` (only `Dockerfile`, `docker-compose.yml`) — CLAUDE.md §"Docker" and §"When Creating Services" require it; no root pipeline references `bot-room-service` either (`grep -rl bot-room-service --include=*.yml` hits only the compose file). Same gap in `bot-message-handler`, `bot-message-worker`, `broadcast-worker`, `notification-worker`, `roomlist-worker`, `push-notification-service`, so treat it as a bot-platform-wide omission, not a one-off.
- [medium] Store mocking bypasses the mandated mockgen path — `bot-room-service/store.go:13` (no `//go:generate mockgen` directive), no `mock_store_test.go`; `bot-room-service/roomkey_test.go:11-13` admits "no gomock/mockgen infrastructure" — 25 sibling services carry `mock_store_test.go`. The hand fake `fakeStore` (`handler_test.go:33-47`) calls `f.InsertRoomFn`/`UpsertSubscriptionFn`/`DeleteSubscriptionFn`/`FindUserFn` without nil checks, so any test that omits one panics with a nil func instead of a readable assertion.
- [medium] `parseIdentity` error paths (missing header, malformed JSON, empty id/account) have no test — `bot-room-service/handler.go:722-738` (66.7% covered; `grep parseIdentity|BotInvalidHeader *_test.go` returns nothing) — this is the caller-identity gate for all five RPCs; the `errcode.WithCause(err)` branch at `:731` is unexercised.
- [medium] No table-driven tests despite many scenario-per-handler cases — `bot-room-service/handler_test.go` (0 `t.Run`, 41 top-level `TestHandleX_Scenario` funcs) — CLAUDE.md §4 prefers table-driven subtests for input/output variations; only `handler_remove_key_test.go:185-193` uses the pattern. The rest of §4 is respected: `package main`, testify, `TestMain` via `testutil.RunTests` (`integration_test.go:20`), no shared mutable state.
- [low] Bare `return err` — `bot-room-service/handler.go:612` — `rotateAndFanOut` returns `roomkeystore.CommitRotation`'s error unwrapped; CLAUDE.md §3 says always wrap with what this function was doing. The callee does wrap (`pkg/roomkeystore/commit.go:29,38`), so the log line is still readable, but the rule is violated.
- [low] Store model structs carry no `json`/`bson` tags and field names are hand-mapped in three places — `bot-room-service/store.go:70-108`, `store_mongo.go:48-55` (insert map), `:72-84` (decode struct), `:196-219` (`participantBSON`) — CLAUDE.md §3 "All model structs get both json and bson tags". The triple mapping is where a renamed column silently diverges between insert and read.
- [low] `FindUser` projection over-fetches — `bot-room-service/store_mongo.go:158` selects `engName`, `chineseName`, `roles`; the handler reads only `u.ID`, `u.Account`, `u.SiteID` (`handler.go:126,155,254-256,335-336,495`). CLAUDE.md MongoDB rule: project only the fields the caller needs.
- [low] Service JetStream-publishes to OUTBOX and BOT-MESSAGES-CANONICAL but has no `Bootstrap bootstrapConfig`/`bootstrap.go` and its compose sets no `BOOTSTRAP_STREAMS` — `bot-room-service/main.go:25-62`, `deploy/docker-compose.yml` — CLAUDE.md "each such service's config includes `Bootstrap`"; sibling publisher `room-service/main.go:70,263` follows it. Dev-only impact (fresh NATS needs another service up first).
- [nitpick] Misleading dedup comment — `bot-room-service/sysmsg.go:61` claims the suffix "defeats double-emit on retry", but every suffix is `h.now().UnixMilli()` (`handler.go:278,372,545`), so a client retry mints a new `Nats-Msg-Id`. Handler-level idempotency (ErrDuplicate/all-dup batch/wasThere=false) is what actually prevents a second sysmsg; the comment should say so.
- [nitpick] Log key inconsistency: `"room_id"` at `bot-room-service/handler.go:149` vs `"roomID"` everywhere else (`handler.go:457,558,563`, `sysmsg.go:42,58,67`) — breaks log-field filtering across the service.
- [nitpick] Hard-coded 2s sysmsg publish timeout — `bot-room-service/sysmsg.go:62` — while `REQUEST_TIMEOUT` (`main.go:58`) governs the handler; not configurable, not tied to the request budget.

### Recommendations

- [high] Add `deploy/azure-pipelines.yml` cloned from `room-service/deploy/azure-pipelines.yml` (paths `bot-room-service/`, `pkg/`) — restores the lint/test/SAST gate for this service; do the same for the other bot-platform services listed above.
- [high] Raise unit coverage past 80%: add a table-driven `TestParsePeers` (`main.go:171`), a `TestParseIdentity` table (missing/malformed/empty-id/valid), tests for `Register` via `natsrouter` route inspection, and untested branches in `handleDMEnsure` (`FindRoom` warn path `:145-150`) and `handleCreate` (member not-found `:246-249`, remote seed `:263-268`) — these are the cheapest 100+ statements.
- [medium] Add `//go:generate mockgen -source=store.go -destination=mock_store_test.go -package=main` to `store.go:13` and migrate `fakeStore`/`fakeKeyStore` to gomock — removes the nil-func panic hazard and aligns with the 25 sibling services.
- [medium] Wrap the error at `handler.go:612`: `return fmt.Errorf("commit key rotation: %w", err)`.
- [low] Tag `Room`/`Participant`/`Subscription` with `bson`/`json` (`store.go:70-108`) and marshal them directly in `InsertRoom`/`FindRoom`/`participantBSON` — collapses three hand-kept field maps into one.
- [low] Trim the `FindUser` projection at `store_mongo.go:158` to `_id, account, siteId`.
- [nitpick] Normalise the `room_id` key at `handler.go:149` to `roomID`; fix the `sysmsg.go:61` comment.

## 3. Architecture — score 3

### Evidence

- [high] Sysmsg publishes a bare `model.Message` onto BOT-MESSAGES-CANONICAL, but every consumer decodes a `model.MessageEvent` envelope — `sysmsg.go:46-65` — The stream's producer contract is `bot-message-handler/handler.go:188-193`; `bot-message-worker/handler.go:32-39` and `search-sync-worker/messages.go:180-185` both unmarshal into `MessageEvent`. A bare `Message` decodes without error into a zero-value `evt.Message`, so bot-message-worker hands `SaveMessage` (`bot-message-worker/store_cassandra.go:88-121`, no validation) empty ids and a zero `created_at` — an unusable row, or an InvalidRequest that `isPermanent` (`:70-75`) treats as transient and NAK-loops — and search-sync-worker Ack-drops each as poison (`search-sync-worker/handler.go:124-131`). Every membership system message is lost from history. The service's own test asserts the wrong shape (`handler_test.go:417-418`) and the comment at `sysmsg.go:17` ("same wire shape as bot-message-handler") is false. The bare form also lacks the mandated event-level `Timestamp`.
- [medium] No startup verification of the two streams this service publishes to — `main.go:129,144` — Siblings `room-service/bootstrap.go:57-59` and `bot-message-handler/bootstrap.go:35-37` fail fast in production via `js.Stream()`. Here a misprovisioned OUTBOX or BOT-MESSAGES-CANONICAL surfaces only at first publish; for sysmsgs that is a `slog.Warn` (`sysmsg.go:65-68`), so the loss is silent. No `bootstrapConfig`/`BOOTSTRAP_` field exists.
- [medium] `store.go` has no `//go:generate mockgen` directive and no `mock_store_test.go`; tests use hand-written fakes — `store.go:13`, `handler_test.go:24-54`, `roomkey_test.go:11-13` — The per-service layout puts the mockgen directive in `store.go`; `roomkey_test.go:13` documents the gap. The repo-wide "mocks not stale" fact cannot cover a service that generates nothing.
- [medium] `handleCreate` is a multi-write, non-idempotent request/reply with a server-generated id — `handler.go:195-231` — Room insert, owner subscription, key `Set` and N member upserts run sequentially with no rollback and no idempotency key, so a caller's timeout-retry creates a second room and a mid-sequence failure orphans one. The `ErrDuplicate → Conflict` branch (`:208-209`) can never fire for a fresh random id.
- [medium] `deploy/azure-pipelines.yml` is absent — `bot-room-service/deploy/` — CLAUDE.md: "Each service also has `<service>/deploy/azure-pipelines.yml`"; 29 of 37 deploy dirs have it, `room-service` included; all three `bot-*` services lack it.
- [low] Dead dependency: `allSiteIDs` is stored on the handler but never read — `handler.go:62,80`; `main.go:140,171-184` — `parsePeers` exists only to fill it; federation targets come from each user's `SiteID`. It misleads readers about how destinations are chosen.
- [low] Sysmsg dedup id is not deterministic across retries, contrary to its comment — `sysmsg.go:63-64`; `handler.go:278,372,545` — The suffix is this call's wall-clock millisecond, so a retry never reproduces the id; "defeats double-emit on retry" holds only because add/remove retries are all-dup and emit nothing. The spec asked for `bot-create:{roomID}` (`docs/superpowers/specs/2026-07-15-bot-messaging-pipeline-design.md:349`).
- [low] Two collaborators are injected post-construction; one silently disables a feature when forgotten — `main.go:142-144`; `sysmsg.go:36-38` — `valkey` and `sysmsgPub` bypass `newHandler`; a nil `sysmsgPub` turns every sysmsg into an unlogged no-op. Fleet pattern (`room-service/main.go:385`), but a required collaborator should not be optional by omission.

### Recommendations

- [high] Wrap the sysmsg in `model.MessageEvent{Event: model.EventCreated, Message: msg, SiteID: h.siteID, Timestamp: now.UnixMilli()}`, fix the `sysmsg.go:17` comment, and change `handler_test.go:417-418` (and `:470`) to decode `MessageEvent` — `sysmsg.go:46-58` — Restores the stream contract so bot-message-worker persists and search-sync-worker filters the sysmsg instead of poisoning both.
- [high] Add a cross-service wire-compat test that runs bot-room-service's sysmsg bytes through `bot-message-worker`'s decoder and asserts a non-empty `evt.Message.ID` — new `_test.go` in either service — The shape drifted under a green suite because each side only tested itself.
- [medium] Add `bootstrap.go` with `Bootstrap bootstrapConfig` (`BOOTSTRAP_STREAMS`, default false) that verifies `stream.Outbox(siteID)` and `stream.BotMessagesCanonical(siteID)` via `js.Stream()` when disabled — `main.go` after `:97` — Mirrors `bot-message-handler/bootstrap.go:35-37`; a misprovisioned deploy fails at startup.
- [medium] Add `//go:generate mockgen -source=store.go -destination=mock_store_test.go -package=main` and migrate `fakeStore`/`fakeKeyStore` to gomock — `store.go:13,53` — Brings the service under `make generate`'s staleness guard.
- [medium] Give `handleCreate` an idempotency key (client `requestId`, or derive the room id from `(botID, requestId)` and treat `ErrDuplicate` as replay) — `handler.go:195-212` — A BP retry converges on one room; the Conflict branch becomes reachable.
- [low] Delete `allSiteIDs`, its `newHandler` parameter, `parsePeers` and `config.AllSiteIDs` — `handler.go:62,76-80`; `main.go:29-30,140,171-184` — Removes a dead knob.
- [low] Move `sysmsgPub` and `valkey` into `newHandler`'s parameters — `handler.go:76-86`; `main.go:141-144` — Makes the sysmsg publisher a declared dependency.

## 4. Test coverage — score 1

### Evidence

- [critical] coverage below repo minimum 80%, currently 49.4% (204/413 statements) — `bot-room-service/handler.go:88` — CLAUDE.md §4 floor is 80% / target 90% for handlers and stores; 14 of 30 functions are at 0% (`Register`, `main`, `run`, `parsePeers`, every `store_mongo.go` method, `jsPublishAdapter.PublishWithMsgID`). Per the dimension brief, <60% caps the score at 1.
- [high] Four store methods have zero tests in ANY tier (unit or integration) — `bot-room-service/store_mongo.go:47` (`InsertRoom`), `:71` (`FindRoom`), `:153` (`FindUser`), `:196` (`participantBSON`) — `integration_test.go` only exercises `EnsureIndexes`, `UpsertSubscription`, `DeleteSubscription` and `ListRoomMemberAccounts` (`integration_test.go:40-156`). Untested contracts include the `IsDuplicateKeyError → ErrDuplicate` mapping that `handleDMEnsure` relies on for idempotency (`handler.go:139`), the `FindRoom` projection/decode of `lastMsgAt` that federation carries, and the `rooms.u` owner shape (`id`/`username` keys, which differ from the `_id`/`account` keys `UpsertSubscription` writes at `store_mongo.go:106`) — no test pins that shape against what other services read.
- [high] `parseIdentity` has no test for any of its three rejection branches — `bot-room-service/handler.go:724`, `:729`, `:733` — every handler except `handleGet` gates on it; missing/malformed header and missing `id`/`account` are all uncovered, so a regression that lets an unauthenticated bot through would not be caught. CLAUDE.md §4 requires "invalid/malformed input" tests per handler.
- [high] Authorization/validation guards in `handleAdd`/`handleRemove` are untested — `bot-room-service/handler.go:291` (`roomID` missing), `:294`/`:406` (org expansion rejected), `:647-651` (room not found via `loadRoomAndAssertOwner`, both `ErrNotFound` and infra error). Only the non-owner branch (`:653`) has tests (`handler_test.go:501`, `:518`).
- [high] `handleDMEnsure` input validation and the target-lookup error path are untested — `bot-room-service/handler.go:108` (empty `targetUserId`), `:112` (self-DM → `BotCannotDMSelf`), `:118-123` (target not found / infra). The four DM tests (`handler_test.go:149`, `:259`, `:287`, `:301`) cover only happy and duplicate-insert paths.
- [medium] Store-error paths in create/add are untested — `bot-room-service/handler.go:207-211` (`InsertRoom` duplicate → `Conflict`/`BotRoomExists`, and infra), `:218`, `:245-251` (seed member not found → `BotMemberNotFound`), `:257`, `:327-332`, `:339`, `:482` (`DeleteSubscription` error). No test injects `errors.New` into `InsertRoomFn`, `FindRoomFn` or `UpsertSubscriptionFn`; the only error injections are on `FindUserFn`, the key store and publishers.
- [medium] `rotateAndFanOut` infra errors before `CommitRotation` are untested — `bot-room-service/handler.go:596` (`ListRoomMemberAccounts` error) and `:601` (`keyStore.Get` infra error); `keyPairOrHeal` `SetIfAbsent` failure on the add path is also untested (`:587`). These are the remove/add paths where a failure leaves an already-committed delete with no rotation — exactly the hazard the deferred-rotation comments describe (`:434-459`).
- [medium] `roomTypeToModel`'s DM branch is dead in tests — `bot-room-service/handler.go:380` — it is only reachable from `handleAdd`, which now rejects non-channel rooms at `:307`, so the branch is untestable through the handler and reads as dead code.
- [medium] `sysmsg.go` and `main.go` wiring have no tests — `bot-room-service/sysmsg.go:26` (`jsPublishAdapter.PublishWithMsgID` 0%, the error-wrap contract), `sysmsg.go:40` (nil publisher no-op), `main.go:171` (`parsePeers` 0%: empty, whitespace, self-exclusion). `parsePeers` is pure and trivially table-testable; the compose file sets `ALL_SITE_IDS=site-local` (`deploy/docker-compose.yml:14`), so the self-exclusion path runs in every dev deployment untested.
- [low] Table-driven structure is nearly absent — 1 `t.Run` across 9 test files (`handler_remove_key_test.go:193`); the validation matrix above (5 handlers × several rejection reasons) is the canonical table-driven case and is instead not written at all.
- [low] Hand-rolled fakes instead of `go.uber.org/mock` — `bot-room-service/roomkey_test.go:13`, `handler_test.go:24` — `store.go` has no `//go:generate mockgen` directive and there is no `mock_store_test.go`, unlike every other service. `fakeStore` methods nil-panic when a test forgets to set them (`handler_test.go:33`, `:40-47`), so failures surface as nil-derefs rather than unexpected-call messages.
- [nitpick] `captureOutbox.mu` atomic counter is never read — `bot-room-service/handler_test.go:57`, `:62`; `withIdentity` discards `json.Marshal`'s error — `handler_test.go:75`.

### Recommendations

- [critical] Raise unit coverage past 80% before the next merge touching this service — the concrete gap list above is ~55 uncovered blocks in `handler.go`, almost all one-line error/validation branches reachable with the existing fakes; target ≥90% for `handler.go` per CLAUDE.md.
- [high] Add a table-driven `TestParseIdentity` (`handler.go:722`) with cases: missing header, malformed JSON, missing `id`, missing `account`, valid — assert `errcode.CodeBadRequest` + `BotInvalidHeader`.
- [high] Add integration tests for `InsertRoom` (fresh + duplicate → `ErrDuplicate`), `FindRoom` (present with `lastMsgAt`, absent → `ErrNotFound`), `FindUser` (present/absent) and a golden-shape assertion on the `rooms.u` doc `participantBSON` writes — `store_mongo.go:47-100`, `:153`, `:196`.
- [high] Table-driven validation tests per handler: `handleDMEnsure` (empty target, self, not found), `handleAdd`/`handleRemove` (missing `roomID`, `OrgIDs` set, room not found, `FindRoom` infra), `handleCreate` (`Orgs` set, duplicate room → `Conflict`, seed member not found), `handleGet` (empty `roomId`, infra error) — `handler.go:103-640`.
- [medium] Cover the pre-commit infra failures in `rotateAndFanOut` (`:596`, `:601`) and `keyPairOrHeal` (`:587`) so the "rotation net" reasoning is pinned end-to-end, not only for `Rotate` failures.
- [medium] Add `TestParsePeers` (empty, single, with spaces, self excluded, all-self → empty) — `main.go:171` — and a `TestJSPublishAdapter_WrapsError` against a fake `Publish` — `sysmsg.go:26`.
- [low] Add `//go:generate mockgen` to `store.go` and migrate `fakeStore`/`fakeKeyStore` to generated mocks, or document the hand-fake exception in `CLAUDE.md`; either removes the nil-func panics.

## 5. Maintainability — score 3

### Evidence

- [medium] Room-key helper trio is a drifting copy of room-worker — `bot-room-service/handler.go:552,573,594` vs `room-worker/handler.go:2594,1727,352` — `fanOutKey`, `keyPairOrHeal`, `rotateAndFanOut` are near-verbatim, but room-worker already records `roomkeymetrics.RecordStoreError` on Get/SetIfAbsent failure (1730,1743) and `RecordFanoutErrors` on marshal failure (2608); the bot copy does neither, so bot-originated key failures are invisible on the shared dashboards. `roomkeystore.CommitRotation` (`pkg/roomkeystore/commit.go:21`) shows the extraction pattern was already started and stopped halfway.
- [medium] `ROOM_KEY_GRACE_PERIOD`/`ROOM_KEY_RETIRED_TTL` re-declared per service — `bot-room-service/main.go:39,42`, `room-service/main.go:65,67`, `room-worker/main.go:69,72`, `broadcast-worker/main.go:78,82` — CLAUDE.md says a shared knob is declared once in the owning package and MUST agree across these exact services; `pkg/roomkeystore` has `OpenConfig` (`open.go:18`) but no env-tagged struct, so nothing prevents the divergence the rule exists for. Fleet-wide, not bot-room-service-specific.
- [medium] Dead peer plumbing: `handler.allSiteIDs` is stored but never read — `handler.go:62,76,80`; fed by `parsePeers` (`main.go:140,171`, 0% covered) — Every sibling binds `ALL_SITE_IDS` as `[]string` with `envSeparator:","` (`broadcast-worker/main.go:52`, `outbox-worker/main.go:39`, `user-service/config/config.go:76`); this is the only hand-split, and `broadcast-worker/roomactivity.go:156 remotePeers` is a second self-exclusion helper. Untested dead code that a reader must assume is load-bearing.
- [low] Interface doc contradicts the implementation: `store.go:245` says `Get` returns `roomkeystore.ErrNoCurrentKey` and `handler.go:575,601` tolerate it, but `pkg/roomkeystore/roomkeystore_mongo.go:155-162` returns `nil, nil` for an absent key; `ErrNoCurrentKey` is a `Rotate` error (`roomkeystore.go:8`). room-worker (`handler.go:1728-1735`) reads the contract correctly. Dead tolerance plus a wrong doc that a new engineer will trust.
- [low] `parseIdentity` is byte-identical in `bot-room-service/handler.go:722-738` and `bot-message-handler/handler.go:207-223` — the header, struct and reason code are all `pkg/model`/`pkg/errcode` contracts; a third bot consumer will copy it a third time.
- [low] Two JetStream adapters with the same `(ctx, subj, data, msgID) error` shape: `sysmsg.go:20-31` (`js.Publish`, no headers) and `outboxpublish.go:33-40` (`js.PublishMsg` via `natsutil.NewMsg`, carries `X-Request-ID`). The sysmsg lane therefore drops request-id correlation into BOT-MESSAGES-CANONICAL. Both `sysmsgPub` and `valkey` are wired post-construction (`main.go:142-144`) while seven other deps go through `newHandler` — a nil `sysmsgPub` silently disables emission (`sysmsg.go:36`).
- [low] `handleRemove` is 148 lines (`handler.go:401-548`), 64 comment / 78 code, with two order-sensitive `defer` closures; `handleCreate` 107 (`182-288`), `handleAdd` 89 (`289-377`). The retry-gap rationale is restated three times (`428-459`, `509-529`, `686-703`). Correct, but a change to the ordering has to be reconciled against three narratives.
- [low] Layout deviations from CLAUDE.md: no `//go:generate mockgen` in `store.go` and hand-rolled fakes (`handler_test.go:24`, `roomkey_test.go:14`; siblings `room-service/store.go`, `room-worker/store.go` use mockgen); unguarded `InsertRoomFn` (`handler_test.go:33`) panics on nil. No `deploy/azure-pipelines.yml` for any of the three `bot-*` services.
- [nitpick] `federateMemberAdded` takes 9 positional params with adjacent same-typed strings (`handler.go:663-665`); call sites pass positional `""` (`handler.go:172,216`). `memberRemoval` (`391`) already shows the struct form for the remove side. `fanOutKey` takes its log message as a parameter (`552`).

### Recommendations

- [medium] Move `keyPairOrHeal`/`fanOutKey`/`rotateAndFanOut` into `pkg/roomkeystore` (or a `pkg/roomkey` sibling of `commit.go`) taking the `Committer`-style narrow interfaces, with the metrics calls included — `handler.go:552-619`, `room-worker/handler.go:352,1727,2594` — one implementation, one set of metrics, one test suite.
- [medium] Add an env-tagged `roomkeystore.Config{GracePeriod, RetiredTTL}` and mount it as a named field in all four services — `main.go:39,42` — closes the cross-service TTL drift CLAUDE.md calls out.
- [medium] Delete `allSiteIDs`, the constructor param and `parsePeers` (or, if federation ever needs the list, bind `[]string` with `envSeparator:","` and share `remotePeers` from `pkg/`) — `handler.go:62,76,80`, `main.go:140-184`.
- [low] Fix the `RoomKeyStore.Get` doc and drop the `ErrNoCurrentKey` checks — `store.go:245`, `handler.go:575,601` — match room-worker's reading of the contract.
- [low] Extract `parseIdentity` next to `model.BotIdentity` (e.g. `model.ParseBotIdentity(nats.Header)`) — `handler.go:722`, `bot-message-handler/handler.go:207`.
- [low] Collapse `jsPublishAdapter` into `newOutboxPublisher`'s shape using `natsutil.NewMsg`, and pass `sysmsgPub`/`valkey` through `newHandler` — `sysmsg.go:20-31`, `main.go:142-144` — restores request-id propagation and one wiring path.
- [low] Add `//go:generate mockgen` + `mock_store_test.go` and a `deploy/azure-pipelines.yml` — `store.go`, `deploy/` — aligns with the per-service layout the rest of the repo relies on.

## 6. Integration — score 2

### Evidence

- [high] Sysmsg publishes a bare `model.Message` to BOT-MESSAGES-CANONICAL while every consumer decodes `model.MessageEvent` — `bot-room-service/sysmsg.go:46-65` — `bot-message-handler/handler.go:188` wraps in `MessageEvent{Event, Message, SiteID, Timestamp}`; `bot-message-worker/handler.go:32` reads `evt.Message` (zero → empty `room_id` partition key, non-errcode error → NAK loop through the retry budget), `broadcast-worker/handler.go:206` hits the `default` branch on `Event==""` and drops it, `search-sync-worker/messages.go:184` rejects "missing message id". Every members_added/member_removed sysmsg this service emits is lost, and the comment at `sysmsg.go:17` claiming parity with bot-message-handler is stale. The tests cement the wrong shape (`handler_test.go:417,470,493` unmarshal into `model.Message`). Also no `Timestamp` rides the wire since the envelope is missing.
- [high] Rooms doc written with `t:"c"/"d"` instead of the canonical `type:"channel"/"dm"` — `bot-room-service/store_mongo.go:50,84` — `pkg/model/room.go:17` (`bson:"type"`), `room-service/store_mongo.go:211,221` and `pkg/roommetacache/roommetacache.go:211` all project `type`; a bot room decodes as `Type==""`, so e.g. `room-service/handler.go:677` treats it as non-channel. Only `bot-message-handler/store_mongo.go:53` reads `t`. Owner is also written as `u:{id,username}` (`store_mongo.go:199`), a shape no `model.Room` field or reader consumes.
- [high] Subscription rows lack `joinedAt`, `open`, `roles` that the rest of the platform requires — `bot-room-service/store_mongo.go:103-110` writes `createdAt` (no such field on `model.Subscription`, `pkg/model/subscription.go:86,110`) — `inbox-worker/handler.go:322-323` sets `JoinedAt`+`Open:true` for the same event on the remote side; `room-service/store_mongo.go:145,760` indexes and paginates the member list on `{joinedAt,_id}` (bot-added members sort as null-first) and `room-service/store_mongo.go:1327` keys sidebar visibility on `open`. Local and federated copies of one membership diverge.
- [medium] `member_added` dedup id is the `(room,user,dest)` triple, so remove→re-add inside the JetStream duplicate window drops the re-add forward — `bot-room-service/handler.go:681` — the remove path (`handler.go:686-717`) already documents and fixes exactly this collision by keying on the subscription id; the add path has the fresh `Subscription.ID` in hand (`handler.go:334-338`) and does not use it. Remote member stays unsubscribed with nothing to repair it.
- [medium] Room-key knobs re-declared per service with their own `envDefault` — `bot-room-service/main.go:39,42,46` — `ROOM_KEY_GRACE_PERIOD`/`ROOM_KEY_RETIRED_TTL`/`MONGO_KEY_READ_PREFERENCE` are duplicated verbatim in `room-service/main.go:65-67`, `room-worker/main.go:69-72`, `broadcast-worker/main.go:78-82`; `pkg/roomkeystore/open.go:18` exposes no env-tagged config struct to mount. Values are consistent today (24h/30m/primaryPreferred), so this is a CLAUDE.md pattern gap fleet-wide, not a live mismatch.
- [low] No `bootstrap.go`/`BOOTSTRAP_STREAMS` although the service publishes to OUTBOX and BOT-MESSAGES-CANONICAL — `bot-room-service/deploy/docker-compose.yml` (absent) vs `room-service/deploy/docker-compose.yml:55`, `room-worker/deploy/docker-compose.yml:28`, both of which ship `bootstrap.go`. Dev stand-up depends on bot-message-handler/outbox-worker having started first.
- [low] `docs/client-api.md` §10.7 omits the `non_channel_operation` reason returned at `bot-room-service/handler.go:308` — `docs/client-api.md:8706-8738` lists only `not_a_room_owner`/`cannot_remove_self`/`member_not_found` plus §10.4. Subjects are `chat.server.bot.request.*` (not `chat.user.*`), so the CLAUDE.md mandate does not strictly apply, but §10.6/10.7 explicitly describe this service's envelopes.

Verified clean: all subjects via `pkg/subject` builders (`handler.go:88-99`, `sysmsg.go:65`); OUTBOX only through `outbox.Publish` with `member_added`/`member_removed` (both in `pkg/outbox.OrderedEventTypes`) and JetStream PubAck with `Nats-Msg-Id` (`outboxpublish.go:34-38`); INBOX never written directly; `Timestamp` set on `MemberAddEvent`/`MemberRemoveEvent` (`handler.go:676,708`) and stamped by `roomkeysender.Marshal` on `RoomKeyEvent`; IDs exclusively via `idgen` (`handler.go:83-84,126,195`); `subauthcache.BustSubs` keyed identically to `inbox-worker/handler.go`; `MemberAddEvent.RoomType=botDM`+`RequesterAccount` matches `inbox-worker/handler.go:760-766`; retired-key TTL equal across the four services; Mongo-only (no Cassandra bucket concerns).

### Recommendations

- [high] Wrap the sysmsg in `model.MessageEvent{Event: model.EventCreated, Message: msg, SiteID: h.siteID, Timestamp: now.UnixMilli()}` and fix the three tests to decode that envelope — `sysmsg.go:46-55`, `handler_test.go:417,470,493` — restores delivery/persistence/indexing of bot membership sysmsgs and stops the bot-message-worker NAK churn.
- [high] Write `type` (`model.RoomTypeChannel`/`model.RoomTypeDM`) and drop the legacy `t`/`u` fields, or write both during a migration window; align `bot-message-handler/store_mongo.go:53` in the same PR — `store_mongo.go:44-58,84` — makes bot rooms readable by room-service, roommetacache and upload-service.
- [high] Persist `joinedAt` (from `sub.CreatedAt`), `open:true` and `roles` via `model.Subscription` bson tags instead of the hand-built `$setOnInsert` — `store_mongo.go:103-110` — puts local rows on the same shape as inbox-worker's federated rows and room-service's member-list index.
- [medium] Key the add dedup id on the subscription id (`bot-add:{room}:{subID}:{dest}`) exactly as the remove path does — `handler.go:681` — closes the remove→re-add duplicate-window drop.
- [medium] Introduce `roomkeystore.Config` (env-tagged `ROOM_KEY_GRACE_PERIOD`/`ROOM_KEY_RETIRED_TTL`/`MONGO_KEY_READ_PREFERENCE`) and mount it in the four services — `main.go:39-46` — per the single-declaration rule; fleet-wide change, not this service alone.
- [low] Add `bootstrap.go` + `Bootstrap bootstrapConfig` and `BOOTSTRAP_STREAMS=true` in compose — `main.go:25-62`, `deploy/docker-compose.yml` — matches room-service/room-worker.
- [low] Add `non_channel_operation` (400) to `docs/client-api.md` §10.7 and `room_not_found` to its add/remove table — `docs/client-api.md:8734-8738`.

## 7. Performance — score 3

### Evidence

- [medium] Member batches are unbounded and processed as sequential N+1 round trips under one 10s request budget — `bot-room-service/handler.go:240-269` (create), `:325-356` (add), `:460-508` (remove) — Every element costs `FindUser` + `UpsertSubscription`/`FindOneAndDelete`, plus a JetStream `PublishMsg` that blocks on PubAck for each remote member (`outboxpublish.go:35`). Nothing caps `req.UserIDs`/`req.Members` (room-service does: `room-service/handler.go:1216`), so a 500-id batch is ~1,000-1,500 serial RTTs against `REQUEST_TIMEOUT=10s` (`main.go:58`); a deadline mid-loop leaves committed partial state that the handler's own comments say a retry cannot repair (`handler.go:441-459`).
- [medium] The deferred rotation safety net runs on the request context, so when the failure IS the timeout it cannot do anything — `handler.go:451-459`, `:594-613` — `rotateAndFanOut` issues `ListRoomMemberAccounts`, `keyStore.Get` and `Rotate` on `c`; after `HandlerTimeout` (`pkg/natsrouter/middleware.go:191`) expires they all return `DeadlineExceeded` immediately, leaving a removed member with a working key. The sibling helpers already detach for exactly this reason (`pkg/valkeyutil/tier.go:97`, `pkg/roomkeystore/roomkeystore_mongo.go:343`); no test in `handler_remove_key_test.go` exercises an expired ctx.
- [low] Admission cap exceeds the Mongo pool — `main.go:57` (`MAX_CONCURRENCY=200`) vs `pkg/mongoutil/poolconfig.go:25` (`MONGO_MAX_POOL_SIZE=150`) — The comment at `main.go:52-55` says the pair keeps a burst from saturating the pool, but 200 admitted handlers each issuing 2+ serial ops can park 50+ waiters in the driver's checkout queue, spending the 10s budget waiting for a socket rather than being shed with `service busy` at the router.
- [low] `FindUser` projects three fields no caller reads — `store_mongo.go:157-159` — `engName`, `chineseName`, `roles` are fetched on the per-member hot read while all four call sites (`handler.go:126,155`, `:244-254`, `:326-336`, `:471-495`) use only `ID`/`Account`/`SiteID`. Projection discipline is met, but the widest read is the one executed N times per request.
- [low] Key fan-out is uninstrumented and uncorrelated — `handler.go:562` calls `SendData` (`pkg/roomkeysender/roomkeysender.go:63`, `context.Background`) and `main.go:127` builds the sender without `WithMetrics`, so the `Failure` counter at `roomkeysender.go:76` is inert. A rotation fans out to every survivor; a core-publish failure storm there is visible only as per-recipient Warn logs.
- [nitpick] `ListRoomMemberAccounts` is one `_id:0` short of an index-covered scan — `store_mongo.go:172-175` — filter `roomId` + projection `u.account` matches `roomId_1_u.account_1` (`store_mongo.go:36`), but the implicit `_id` forces a document fetch per member on large rooms.
- [nitpick] DM ensure on the duplicate path pays a second `FindRoom` round trip — `handler.go:138-151` — a single `FindOneAndUpdate` upsert returning `lastMsgAt` would replace insert-then-read.

### Recommendations

- [medium] Add a `MAX_BATCH_SIZE` config (mirror room-service `maxBatchSize`) and reject oversize `UserIDs`/`Members` before the first write — `handler.go:182`, `:289`, `:401` — bounds worst-case RTTs per request and keeps the partial-commit window small.
- [medium] Replace the per-member `FindUser` loop with one `$in` lookup per request (pattern: `pkg/userstore/userstore.go:110`), projecting `_id/account/siteId` only — `store_mongo.go:153`, `handler.go:244`, `:326`, `:471` — cuts per-member Mongo RTTs from 2 to 1 and removes the unused fields.
- [medium] Run the deferred rotation net (and ideally the post-delete federation pass) on `context.WithoutCancel(c)` with its own short timeout — `handler.go:455`, `:535` — so a request-timeout failure still gets the one extra rotation attempt the comment promises.
- [low] Set `MAX_CONCURRENCY` ≤ `MONGO_MAX_POOL_SIZE` (or raise the pool) and fix the comment — `main.go:57`, `deploy/docker-compose.yml:21` — shed load at the router instead of in the driver checkout queue.
- [low] Use `SendDataContext(ctx, …)` and construct the sender with `roomkeysender.WithMetrics(publishMetrics)` — `handler.go:562`, `main.go:127` — makes rotation fan-out failures countable and trace-correlated.
- [nitpick] Add `"_id": 0` to the `ListRoomMemberAccounts` projection — `store_mongo.go:174` — covered query on the survivor snapshot taken on every removal.

## 8. Prioritized action list

Ordered by severity (`critical` first), then by the dimension most likely to carry a correctness or contract cost (integration, architecture, coverage, code quality, performance, maintainability). Each item is a recommendation from the chapter named; the `file:line` and rationale are quoted from it.

1. **[critical]** _Test coverage_ — Raise unit coverage past 80% before the next merge touching this service — the concrete gap list above is ~55 uncovered blocks in `handler.go`, almost all one-line error/validation branches reachable with the existing fakes; target ≥90% for `handler.go` per CLAUDE.md.
2. **[high]** _Integration_ — Wrap the sysmsg in `model.MessageEvent{Event: model.EventCreated, Message: msg, SiteID: h.siteID, Timestamp: now.UnixMilli()}` and fix the three tests to decode that envelope — `sysmsg.go:46-55`, `handler_test.go:417,470,493` — restores delivery/persistence/indexing of bot membership sysmsgs and stops the bot-message-worker NAK churn.
3. **[high]** _Integration_ — Write `type` (`model.RoomTypeChannel`/`model.RoomTypeDM`) and drop the legacy `t`/`u` fields, or write both during a migration window; align `bot-message-handler/store_mongo.go:53` in the same PR — `store_mongo.go:44-58,84` — makes bot rooms readable by room-service, roommetacache and upload-service.
4. **[high]** _Integration_ — Persist `joinedAt` (from `sub.CreatedAt`), `open:true` and `roles` via `model.Subscription` bson tags instead of the hand-built `$setOnInsert` — `store_mongo.go:103-110` — puts local rows on the same shape as inbox-worker's federated rows and room-service's member-list index.
5. **[high]** _Architecture_ — Wrap the sysmsg in `model.MessageEvent{Event: model.EventCreated, Message: msg, SiteID: h.siteID, Timestamp: now.UnixMilli()}`, fix the `sysmsg.go:17` comment, and change `handler_test.go:417-418` (and `:470`) to decode `MessageEvent` — `sysmsg.go:46-58` — Restores the stream contract so bot-message-worker persists and search-sync-worker filters the sysmsg instead of poisoning both.
6. **[high]** _Architecture_ — Add a cross-service wire-compat test that runs bot-room-service's sysmsg bytes through `bot-message-worker`'s decoder and asserts a non-empty `evt.Message.ID` — new `_test.go` in either service — The shape drifted under a green suite because each side only tested itself.
7. **[high]** _Test coverage_ — Add a table-driven `TestParseIdentity` (`handler.go:722`) with cases: missing header, malformed JSON, missing `id`, missing `account`, valid — assert `errcode.CodeBadRequest` + `BotInvalidHeader`.
8. **[high]** _Test coverage_ — Add integration tests for `InsertRoom` (fresh + duplicate → `ErrDuplicate`), `FindRoom` (present with `lastMsgAt`, absent → `ErrNotFound`), `FindUser` (present/absent) and a golden-shape assertion on the `rooms.u` doc `participantBSON` writes — `store_mongo.go:47-100`, `:153`, `:196`.
9. **[high]** _Test coverage_ — Table-driven validation tests per handler: `handleDMEnsure` (empty target, self, not found), `handleAdd`/`handleRemove` (missing `roomID`, `OrgIDs` set, room not found, `FindRoom` infra), `handleCreate` (`Orgs` set, duplicate room → `Conflict`, seed member not found), `handleGet` (empty `roomId`, infra error) — `handler.go:103-640`.
10. **[high]** _Code quality_ — Add `deploy/azure-pipelines.yml` cloned from `room-service/deploy/azure-pipelines.yml` (paths `bot-room-service/`, `pkg/`) — restores the lint/test/SAST gate for this service; do the same for the other bot-platform services listed above.


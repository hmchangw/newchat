# search-sync-worker — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `e73a723` (base `main`)  
**Overall score:** 3.0 / 5 (baseline 2026-09-01: 3.2, Δ -0.2)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

search-sync-worker is a well-structured Elasticsearch indexer whose collection abstraction, bulk-error matrix and backoff discipline are all solid, but this audit found one silent production defect that outranks everything else: the `bot-message-sync` consumer's filter subject (`chat.msg.canonical.{site}.*`) never matches the BOT-MESSAGES-CANONICAL stream it binds (`chat.bot.canonical.{site}.>`), and nats-server v2.12.6 no longer rejects a non-subset filter, so the durable is created and delivers nothing — no bot message has ever been indexed, with no log or metric to show it. The fix exists on `origin/fix/search-sync-bot-filter` but is not on this branch, and because the durable's cursor already sits at stream end, correcting the filter in place will not backfill unless the durable is versioned or deleted. Three further gaps recur across dimensions: ES bulk/update-by-query calls have no deadline (a hung ES node wedges a whole collection and then the consumer loop), `main()` is a 319-line untestable monolith that holds most of the 12.6-point coverage shortfall, and the `Collection` interface carries no `context.Context`, so the thread-parent ES lookup and Teams identity Mongo lookup run outside the consumer span and outside shutdown cancellation. The Mongo `ResolveIdentities` resolver has no test at any tier, `ES error.reason` is logged verbatim (and for `mapper_parsing_exception` carries message content), and stream bootstrap is inlined in `main` instead of the repo's `bootstrap.go` helper.

| Dimension | Score |
|---|---|
| Code quality | 4 |
| Architecture | 3 |
| Test coverage | 2 |
| Maintainability | 3 |
| Integration | 3 |
| Performance | 3 |

**Findings by severity:** 0 critical, 7 high, 16 medium, 19 low, 7 nitpick (49 total).  
**Highest-risk dimension:** Test coverage (2).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 4

### Evidence

- [medium] Request/trace context is dropped at the Collection boundary — `search-sync-worker/collection.go:42` — `BuildAction(data []byte)` carries no `context.Context`, so the per-message consumer span delivered to `AddWithContext` (`handler.go:88`) never reaches the collections. `messages.go:220` and `messages.go:304` therefore call the ES parent lookup and the Mongo identity lookup with `context.Background()`, and the resulting `slog.Warn/Error` lines at `messages.go:240`, `:247`, `:306`, `:373` carry no correlation ID. CLAUDE.md §3 requires the ID to be propagated via context and present in all log lines; the 2s lookup timeout (`thread_parent_resolver.go:29`) is also detached from shutdown cancellation.
- [medium] ES `error.reason` is logged verbatim and can carry message content — `search-sync-worker/handler.go:295` — `results[i].Error` is the raw ES `reason` string (`pkg/searchengine/adapter.go:31`, `:183`). For `mapper_parsing_exception` (a 400, the exact case this branch drops as poison) Elasticsearch appends `Preview of field's value: '…'`, i.e. the indexed `content`. This contradicts the intent stated two functions up (`handler.go:156-157`: "the document body never belongs in an error that reaches the server log") and the CLAUDE.md rule never to log message bodies.
- [low] Batch-level failures logged without the span context in hand — `search-sync-worker/handler.go:242` and `:261` — `slog.Error(...)` is used while `bulkCtx` (the flush span with links to every source message) is available; `ErrorContext(bulkCtx, ...)` would keep the log trace-correlated like the per-item line at `:293`.
- [low] Bare `return nil, err` — `search-sync-worker/consumer_source.go:38` — `rawConsumerAdapter.Fetch` returns the JetStream error unwrapped. CLAUDE.md §3 forbids bare returns; the sibling `o11yConsumerAdapter.Fetch` is a pure pass-through, but this one branches on the error and should add the "fetch HR batch" context.
- [low] Discarded `json.Marshal` errors without the required justification comment — `search-sync-worker/messages.go:279`, `messages.go:376`, `spotlight_org.go:242` — the same pattern at `messages.go:160` and `spotlight.go:47` carries the "marshal cannot fail" comment; these three do not, violating "never ignore errors silently — comment if intentionally discarded". `messages.go:376` marshals a `MessageDoc` built from event data, so the assumption is weaker than for the static literal maps.
- [low] Dead production API — `search-sync-worker/handler.go:81` (`Add`) and `handler.go:223` (`Flush`) — neither is called outside `handler_test.go:91-199`; the consumer loop uses `AddWithContext` + `Take`/`FlushBatch`. `Add` also hardcodes `context.Background()`, so a future caller would silently lose tracing.
- [nitpick] Inconsistent template-name/doc-ID construction — `search-sync-worker/spotlight_org.go:85` builds `"%s_template"` with `fmt.Sprintf` while every other collection uses a `searchindex.XxxTemplateName` helper; `spotlight.go:83` uses `fmt.Sprintf("%s_%s")` on the fan-out path where `account + "_" + roomID` is cheaper and clearer.
- [nitpick] `Handler`, `NewHandler`, `Collection`, `Store`, `SpotlightOrgIndex` are exported inside `package main` — `search-sync-worker/handler.go:48`, `collection.go:14`, `store.go:12`, `spotlight_org.go:193` — CLAUDE.md asks for unexported handler/store implementations, but ten other `package main` services share this convention, so it is a repo-wide choice, not a service defect.

### Recommendations

- [medium] Add `ctx context.Context` to `Collection.BuildAction`/`BuildActionSeq`/`BuildByQuery` and thread it from `AddWithContext` through `resolveThreadParentCreatedAt` and `resolveTeamsIdentities`; switch the four `messages.go` log calls to `*Context` variants — `collection.go:42`, `handler.go:124`, `messages.go:220`, `:304` — restores request-ID/trace correlation for the ES and Mongo lookups and lets shutdown cancel an in-flight parent lookup.
- [medium] Stop logging `results[i].Error` raw; log `ErrorType` + status only, or pass the reason through a truncating/`Preview of field's value` stripping helper in `pkg/searchengine` — `handler.go:295` — closes the message-content leak on the 400/poison path without losing the diagnostic type.
- [low] Use `slog.ErrorContext(bulkCtx, ...)` at `handler.go:242` and `:261` — keeps whole-batch failures joined to the flush span and its source-message links.
- [low] Wrap the error at `consumer_source.go:38` (`fmt.Errorf("fetch HR consumer batch: %w", err)`) — brings the adapter in line with the repo error-wrapping rule at zero cost.
- [low] Add the "static literals, marshal cannot fail" comment at `messages.go:279`, `spotlight_org.go:242`, and at `messages.go:376` either handle the error (Ack-drop as poison via the existing `BuildAction` error path) or document why a `MessageDoc` can never fail to marshal.
- [low] Move `Handler.Add` and `Handler.Flush` into `handler_test.go` as test helpers (or make tests call `AddWithContext`/`Take`+`FlushBatch`) — `handler.go:81`, `:223` — removes two untraced entry points from production code.
- [nitpick] Add `searchindex.SpotlightOrgTemplateName` and use it at `spotlight_org.go:85`; replace the `fmt.Sprintf` doc-ID at `spotlight.go:83` with string concatenation.

## 3. Architecture — score 3

### Evidence

- [high] `bot-message-sync` consumer filter cannot match anything on its stream, so bot messages are silently never indexed — `search-sync-worker/messages.go:122-130` — `FilterSubjects` returns `subject.MsgCanonicalMessageWildcard` (`chat.msg.canonical.{site}.*`, `pkg/subject/subject.go:708-710`) for both the user and bot collections, but the bot collection binds `BOT-MESSAGES-CANONICAL` whose only subject is `chat.bot.canonical.{site}.>` (`messages.go:104-107`, `pkg/subject/bot.go:73-75`). nats-server v2.12.6 never raises `JSConsumerFilterNotSubsetErr` (defined in `jetstream_errors_generated.go:661`, no caller; `checkConsumerCfg` at `server/consumer.go:678-830` only checks filters against each other), so the durable is created and simply delivers nothing. `messages_test.go:621-622` pins the wrong filter. The audited tree has no `checkFilterSubjects` / `subjectoverlap.go`; the fix lives on `origin/fix/search-sync-bot-filter` (a699c11..8d483b5) and is not an ancestor of this branch.
- [high] Correcting the filter in place will not backfill: the durable is not versioned — `messages.go:79` — `getNextMsg` advances `o.sseq` to stream end on `ErrStoreEOF` (`server/consumer.go:4521-4528`), so the existing `bot-message-sync` cursor already sits past every bot message ever published. `DeliverPolicy=All` is honored only at creation (`pkg/stream/consumer.go:51-53`), and the fix branch keeps the same durable name, so merging it as-is leaves historical bot messages unindexed until someone deletes or renames the durable.
- [medium] Stream bootstrap is inlined in `main` instead of a `bootstrap.go` `bootstrapStreams(ctx, js, siteID, enabled)` helper — `search-sync-worker/main.go:290-309` — CLAUDE.md says "never inline"; the other ten JetStream services (`inbox-worker/bootstrap.go`, `bot-message-worker/bootstrap.go`, …) follow the helper convention. Behavior is otherwise correct (opt-in via `BOOTSTRAP_STREAMS`, `main.go:30-34`; INBOX and HR skipped, `main.go:295-301`; Name+Subjects only, `messages.go:99-112`, `inbox_stream.go:26-31`, `spotlight_org.go:71-74`), but the skip logic and the Name+Subjects invariant have no unit test (`main_test.go` covers only `pushMappings`).
- [low] `Collection.StreamConfig` returns a full `jetstream.StreamConfig` and its doc invites `Sources, SubjectTransforms` — `search-sync-worker/collection.go:14-17` — this contradicts the "schema = Name+Subjects, federation routing is ops/IaC" rule; returning `stream.Config` would make the rule structural instead of a comment.
- [low] `BuildAction(data []byte)` carries no context, so the ES parent lookup and the Mongo identity lookup run on `context.Background()` — `messages.go:220`, `messages.go:304` — the consumer span/request-id that `Handler.AddWithContext` (`handler.go:88`) already holds cannot reach them; the resolver's 2s timeout (`thread_parent_resolver.go:95`) is the only bound.
- [low] Dependencies are poked in after construction rather than injected — `main.go:232` (`teamsUsers`), `main.go:245-256` (`parentResolver`), `main.go:357` (`handler.metrics`) — and `NewHandler` takes a variadic `tracers ...trace.Tracer` (`handler.go:63`); a nil-safe field is workable but a mis-wired pod degrades silently instead of failing construction.
- [nitpick] The consumer loop is a third pattern (pull `Fetch` + batch flush + `flushPipeline`, `main.go:506-563`), not one of the two CLAUDE.md sanctions; it is the right shape for ES bulk and is well documented in code, but CLAUDE.md does not list it as an exception.

Verified clean: no bare `Nak`/`NakWithDelay(0)` (`handler.go:114`, `handler.go:317`, `handler.go:355` all via `pkg/jsretry`); backoff via `stream.DurableConsumerDefaults` with no hardcoded `cc.BackOff` (`main.go:611-618`, asserted by `consumer_config_test.go:62`); all subjects via `pkg/subject` builders (no raw `Sprintf` subjects); store interface in the consumer with only `Bulk`/`UpdateByQuery` (`store.go:14-19`); INBOX/HR consumed as non-owner; user/teams/spotlight/user-room/spotlight-org filters all overlap their streams (`chat.msg.canonical.{site}.*` ⊂ `.>`, `chat.teams…batch` ⊂ `chat.teams…>`, `InboxMemberEventSubjects` ⊂ `internal.>`/`external.>`, `chat.hr.{c}.employees.upsert` ⊂ `chat.hr.{c}.>`); shutdown order `close(stopCh)` → wait `doneChs` → `natsutil.Drain` → Mongo disconnect → health → obs (`main.go:400-419`) matches CLAUDE.md.

### Recommendations

- [high] Merge `origin/fix/search-sync-bot-filter` (bot filter = `BotCanonicalMessageWildcard`, startup `checkFilterSubjects` against the deployed stream, `subjectoverlap.go` + tests) — `messages.go:122-130` — restores bot-message indexing and makes a filter that selects nothing a startup failure instead of a silent no-op.
- [high] Version the bot durable when the filter lands (e.g. `bot-message-sync-v2`) or add a documented runbook step to delete `bot-message-sync` before rollout — `messages.go:79` — otherwise `DeliverPolicy=All` never fires and every bot message published so far stays unsearchable.
- [medium] Extract `main.go:290-309` into `bootstrap.go` with `bootstrapStreams(ctx, js, siteID, enabled)` behind a small `streamManager` interface (mirror `inbox-worker/bootstrap.go`), and unit-test the INBOX/HR skip and the disabled no-op — brings the service onto the repo convention and puts the ownership rule under test.
- [low] Change `Collection.StreamConfig` to return `stream.Config` and build the `jetstream.StreamConfig{Name, Subjects}` once in the bootstrap helper — `collection.go:14-17` — removes the per-collection copy and the invitation to add `Sources`/`SubjectTransforms`.
- [low] Thread `ctx` into `BuildAction`/`BuildActionSeq`/`BuildByQuery` (`collection.go:44`, `collection.go:59`) so the parent-createdAt and teams-identity lookups inherit the consumer span and request id — `messages.go:220`, `messages.go:304`.
- [low] Move `parentResolver`, `teamsUsers` and `metrics` into the constructors (`newMessageCollection`, `newTeamsMessageCollection`, `NewHandler`) — `main.go:232`, `main.go:245-256`, `main.go:357` — so a missing dependency is a compile/startup error, not a silently degraded index.
- [nitpick] Add the batch-flush pull loop to CLAUDE.md's consumer-pattern list as the sanctioned shape for bulk indexers, so the next reviewer does not flag `main.go:506-563` as a deviation.

## 4. Test coverage — score 2

### Evidence

- [high] coverage below repo minimum 80%, currently 67.4% — `search-sync-worker/main.go:102` — 503/746 statements. `main()` is a ~330-line function at 0% and holds every uncovered path that matters: the MODE / FETCH_BATCH_SIZE / BULK_BATCH_SIZE / BULK_FLUSH_INTERVAL / PIPELINE_DEPTH / `-v<N>` index-suffix fail-fast guards, the teams-vs-default collection wiring (`main.go:214-219`, `:224`, `:230`), the template/mapping/stored-script push loops (`main.go:249`, `:256`, `:265`) and the INBOX/HR bootstrap exclusion (`main.go:307-320`). None of it is extracted into a testable helper, so the CLAUDE.md invariant "search-sync-worker NEVER bootstraps INBOX" is asserted only by a comment.
- [high] Mongo store `ResolveIdentities` has zero tests, unit or integration — `search-sync-worker/teams_user_store.go:32` (constructor `:23`, both 0%) — two-query join (teams_user → account → users `_id`), the empty-account skip and the missing-users-row case are unverified; the service never calls `testutil.MongoDB`. The config comment at `main.go:46-52` says a resolver miss is durable (doc indexed with empty author, then Acked), so a bug here permanently mis-indexes every migrated Teams message. CLAUDE.md §4 requires store implementations to be covered by testcontainers integration tests.
- [medium] Integration tests register no cleanup at all — `search-sync-worker/integration_test.go:34-39`, `:66-86` — a package-level `sync.Once` NATS connection replaces the per-test `*nats.Conn` + `Drain` pattern, and no `t.Cleanup` deletes the ES indices/templates/stored scripts or the `MESSAGES-CANONICAL-site-test`, `INBOX-site-*`, `HR-*` streams and durable consumers created in `integration_test.go`, `inbox_integration_test.go:29-37` and `parent_resolver_integration_test.go`. Isolation currently rests on hand-picked unique names; CLAUDE.md asks for DELETE-on-cleanup for ES and per-test Drain for NATS.
- [medium] The time-based flush trigger (`BULK_FLUSH_INTERVAL`) is never exercised — `search-sync-worker/main.go:546`, `:565` — every `runConsumer` test passes `bulkFlushInterval: time.Hour` (`consumer_pipeline_test.go:122`, `:225`), so the only trigger that gets a partial batch to ES under low traffic has no test; a regression to `>` vs `>=` or a stale `lastFlush` would pass the suite.
- [medium] Teams sender-identity error path untested — `search-sync-worker/messages.go:305-308` (`resolveTeamsIdentities` 80%) — `fakeTeamsUserResolver` (`messages_test.go`) never returns an error, so "resolver error ⇒ index with empty author, do not drop the batch" is unpinned; this is the same durable mis-index behaviour flagged above.
- [medium] `newESRead` has no unit coverage — `search-sync-worker/thread_parent_resolver.go:45` (0%; `newESParentResolver :83` 0%) — the marshal-error, `Search` error, decode-error, empty-hits and zero-`createdAt` branches (`:53-77`) are reachable only via `TestNewESRead_Integration`, which covers found/missing only. A fake `esSearcher` would cover all five in milliseconds.
- [low] A test exercises the wrong function — `search-sync-worker/spotlight_org_test.go:221-228` — "malformed json" calls `coll.BuildAction`, which unconditionally errors (`spotlight_org.go:117-119`), so the unmarshal branch of `BuildActionSeq` (`spotlight_org.go:125-127`) is untested (89.5%). The test passes for the wrong reason.
- [low] Handler poison path for by-query builds untested — `search-sync-worker/handler.go:104-110` (`AddWithContext` 77.8%) — `BuildByQuery` errors are tested at collection level (`spotlight_test.go:461-491`) but the handler's Ack-as-poison + `dispAckedPoison` metric for that branch is not; `TestHandler_Add_RoomRenamed_UpdateByQuery` covers only success and `UpdateByQuery` failure.
- [low] Bot and Teams stream bindings unasserted — `search-sync-worker/messages.go:104`, `:109` (both 0%) — `TestMessageCollection_StreamConfig` (`messages_test.go:63`) checks only the user collection, so binding the bot consumer to the wrong stream would not fail a unit test.
- [nitpick] `time.Sleep` retry pacing in `waitForClusterGreen` — `search-sync-worker/integration_test.go:176` — polls an external service, not goroutine sync, so not a CLAUDE.md violation; `require.Eventually` would be the idiomatic form.

### Recommendations

- [high] Extract `validateConfig(cfg) error` and `newCollections(cfg, engine, db, metrics) ([]Collection, error)` from `main()` and table-test them — `main.go:102-233` — each invalid knob and the teams/default wiring become sub-tests; this alone recovers the bulk of the 12.6-point gap because `main()` holds most uncovered statements.
- [high] Add `//go:build integration` tests for `mongoTeamsUserResolver.ResolveIdentities` using `testutil.MongoDB(t, "ssw")` — `teams_user_store.go:32` — cases: hit with users row, hit without users row (empty `UserID`), empty account skipped, unknown id omitted, empty input.
- [medium] Move the bootstrap gate into a `bootstrap.go` `bootstrapStreams(ctx, js, siteID, enabled)` helper per CLAUDE.md and unit-test that INBOX and HR are skipped and duplicates deduped — `main.go:307-320`.
- [medium] Add a `runConsumer` test with `bulkFlushInterval` of tens of milliseconds and bulk size > messages to pin the interval trigger — `main.go:546`, `:565`.
- [medium] Register `t.Cleanup` in every integration test to DELETE its ES indices/templates/scripts and `js.DeleteStream`, and switch to a per-test `nats.Connect` + `Drain` — `integration_test.go:66-86`, `inbox_integration_test.go:29-37`.
- [medium] Unit-test `newESRead` with a fake `esSearcher` across all five branches, and add an erroring `teamsUserResolver` case — `thread_parent_resolver.go:53-77`, `messages.go:305-308`.
- [low] Point the spotlight-org malformed-JSON test at `BuildActionSeq`, assert bot/teams `StreamConfig`, and add a handler test for a `BuildByQuery` error — `spotlight_org_test.go:224`, `messages_test.go:63`, `handler.go:104`.

## 5. Maintainability — score 3

### Evidence

- [high] `main()` is a 319-line monolith (config validation, ES bootstrap, collection assembly, per-collection stream/consumer wiring, health, shutdown) with no seam for tests — `search-sync-worker/main.go:102-420` — `main.go` is 618 lines; the only extracted, tested pieces are `pushMappings` (424), `checkBatchAckCoupling` (575) and `buildConsumerConfig` (611). The MODE gate (214-241), the INBOX/HR bootstrap-skip logic (306-323) and the two near-identical consumer-creation branches (330-358) are untestable without live NATS; this is the primary reason the tree sits at 67.4% (below the CLAUDE.md 80% floor).
- [medium] Integration tests hand-roll `jetstream.ConsumerConfig{Durable, AckPolicy, FilterSubjects}` instead of calling `buildConsumerConfig` — `search-sync-worker/inbox_integration_test.go:168-172` (repeated at 259, 333, 456, 572; `integration_test.go:306, 675`) — production consumers get `DurableConsumerDefaults` (BackOff/MaxDeliver/MaxAckPending); none of the 7 integration consumers do, so the retry/backoff surface main.go ships is never exercised end-to-end and can drift silently.
- [medium] `SpotlightOrgIndex` and its template live in the service, not `pkg/searchindex`, unlike the other three collections — `search-sync-worker/spotlight_org.go:394-404, 406-445` — `search-service/response.go:122-125` re-declares the same field names by hand ("Fields mirror the SpotlightOrgIndex document maintained by search-sync-worker"), so a field rename is a two-repo-directory, comment-guarded contract. `spotlightOrgTemplateBody` is also a near-verbatim copy of `pkg/searchindex/template.go:120-164` that diverges by sending `token_chars` on a whitespace tokenizer (spotlight_org.go:432), the exact thing the pkg copy's comment (template.go:144-147) says not to do.
- [medium] The `Collection` interface has no `context.Context`, so collections that do I/O reach for `context.Background()` — `search-sync-worker/messages.go:220, 304` — the thread-parent ES lookup and the Teams identity Mongo lookup run outside the consumer span and outside shutdown cancellation. Fixing it means changing `BuildAction`/`BuildActionSeq`/`BuildByQuery` on all four collections plus the handler (`collection.go:421, 431, 447`).
- [medium] Documented per-collection invariant has no mechanism — `search-sync-worker/main.go:445` says "A collection added without [an ordering guard] MUST run at depth 1", but `PipelineDepth` is one global knob applied to every collection (`main.go:371-375`). A new collection cannot honour the rule without throttling all others; the guard is a comment, not a type or config.
- [low] `messageCollection` is three collections behind a mode flag — `search-sync-worker/messages.go:51` — `teamsOnly` is branched 9 times across `FilterSubjects`/`TemplateName`/`TemplateBody`/`MappingUpdate`/`BuildAction` (lines 123, 133, 141, 156, 169), plus `parentResolver`/`teamsUsers` fields that are nil in the other mode. Classic refactor-ready signal: a `teamsMessageCollection` type would delete every branch.
- [low] Test-only methods in production code — `search-sync-worker/handler.go:81` (`Add`), `:223` (`Flush`), `:363` (`MessageCount`) — no non-test caller (`main.go` uses `AddWithContext`/`Take`/`FlushBatch`/`ActionCount` only); CLAUDE.md puts test helpers in `_test.go`.
- [low] `engineAdapter` is dead indirection — `search-sync-worker/main.go:590-601` — `searchengine.SearchEngine` (`pkg/searchengine/searchengine.go:58, 78`) already has `Bulk` and `UpdateByQuery` with identical signatures, so it satisfies `Store` directly; the adapter is copied into 8 test sites too.
- [nitpick] Copy-pasted validation: four identical `<= 0` blocks and three identical `StripVersion` blocks — `search-sync-worker/main.go:126-155` — 30 lines that a table would make 8.
- [nitpick] Stale plan reference — `search-sync-worker/consumer_source.go:22-23` ("runConsumer (Task 2) will hold a msgFetcher") describes work already done at `main.go:500`.

Adding a new collection today touches: a new `<name>.go` (8-method interface + optional capabilities), a config field and its validation block in `main.go` (60-67, 144-155), the `collections` literal (`main.go:234-240`), `deploy/docker-compose.yml`, and by convention a doc struct + template in `pkg/searchindex` — five places, two of which are inside the untested `main()`. The `Collection` abstraction itself is sound and well-commented (no TODO/FIXME anywhere; comments are WHY-oriented, e.g. `handler.go:249-260`, `spotlight_org.go:237-239`).

### Recommendations

- [high] Extract `buildCollections(cfg, engine, db, metrics) []Collection` and `wireCollection(ctx, js, hrJS, cfg, coll) (msgFetcher, *Handler, error)` out of `main()` — `main.go:213-241, 310-376` — makes the MODE gate, bootstrap-skip and HR-domain branch unit-testable with the existing `fakeEngine`, and is the cheapest path to the 80% floor.
- [medium] Make integration tests build consumers via `buildConsumerConfig(cfg.Consumer, coll, siteID)` — `inbox_integration_test.go:168` et al. — so BackOff/MaxDeliver/MaxAckPending are exercised against real JetStream and cannot drift from production.
- [medium] Move `SpotlightOrgIndex`, `spotlightOrgTemplateBody` and `TemplateName` into `pkg/searchindex` beside `SpotlightDoc`, and have `search-service/response.go` decode into the shared type — `spotlight_org.go:285-291, 394-445` — one source of truth for the wire contract; also resolves the `token_chars` divergence.
- [medium] Add `ctx context.Context` to `BuildAction`/`BuildActionSeq`/`BuildByQuery` — `collection.go:421-447`, `handler.go:103, 124` — the handler already has the per-message ctx; threading it removes both `context.Background()` calls and puts the ES/Mongo lookups under the consumer span.
- [medium] Give `Collection` (or the wiring) a per-collection pipeline depth, e.g. an optional `unorderedCollection` capability that forces depth 1 — `main.go:445, 371-375` — turns the comment-only invariant into code.
- [low] Split `messageCollection` into `messageCollection` and `teamsMessageCollection` sharing `buildMessageAction` — `messages.go:42-58` — deletes the 9 `teamsOnly` branches and the nil-in-one-mode fields.
- [low] Delete `engineAdapter`, `Handler.Add`, `Handler.Flush`, `Handler.MessageCount`; move any test convenience into `handler_test.go` — `main.go:590-601`, `handler.go:81, 223, 363`.

## 6. Integration — score 3

### Evidence

- [high] Bot collection's consumer filter never matches its stream, so no bot message is ever indexed — `search-sync-worker/messages.go:122-130` — `newBotMessageCollection` (`messages.go:74-81`) binds BOT-MESSAGES-CANONICAL (`messages.go:104-107` → `pkg/stream/stream.go:94-99`, subjects `chat.bot.canonical.{site}.>`, `pkg/subject/bot.go:73-75`) but `FilterSubjects` is not `teamsOnly`, so it returns `subject.MsgCanonicalMessageWildcard` = `chat.msg.canonical.{site}.*` (`pkg/subject/subject.go:708-710`). nats-server v2.12.6 (go.mod) no longer rejects a non-subset filter (`NewJSConsumerFilterNotSubsetError` has no callers outside the generated errors file), so `main.go:348` creates the `bot-message-sync` durable without error and it sits at zero deliveries forever: silent, no log, no metric. `messages_test.go:621-622` pins the wrong value, and no integration test wires the bot collection (only `newMessageCollection` at `integration_test.go:261,508`). `stream.go:92-93` and `main.go:228` both promise bot indexing.
- [medium] A producer on the bot stream publishes a bare `model.Message`, not the `MessageEvent` envelope this service decodes — `bot-room-service/sysmsg.go:46-65` vs `bot-message-handler/handler.go:188-200` — once the filter above is fixed, each bot system message decodes with `Message.ID == ""` and hits `messages.go:184-185` → Ack-poison plus an `slog.Error` per event (`handler.go:128-131`). No data loss (system messages are filtered at `messages.go:204` anyway) but it is a contract mismatch on a consumed stream; bot-message-worker decodes the same envelope (`bot-message-worker/handler.go:32`), so the fix belongs in the producer.
- [medium] Stream bootstrap is inlined in `main` instead of the CLAUDE.md `bootstrapStreams(ctx, js, siteID, enabled)` helper — `search-sync-worker/main.go:310-323` — the gate (`BOOTSTRAP_STREAMS`, default false at `main.go:33`) and the INBOX/HR exclusions (`main.go:306-314`) are correct, but the rule says "via the service's `bootstrapStreams` helper, never inline"; 12 sibling services carry `bootstrap.go`, and this one cannot be unit-tested like theirs (`main_test.go` covers only `pushMappings`).
- [low] Resolver calls drop the delivery context — `search-sync-worker/messages.go:220` and `messages.go:304` — `ResolveParentCreatedAt` and `ResolveIdentities` are called with `context.Background()`, so the ES self-lookup and the two Mongo finds (`teams_user_store.go:326,344`) lose trace correlation and cannot be cancelled on shutdown; the per-message ctx is available one frame up (`handler.go:88,124`).
- [low] `docs/nats-subject-naming.md` omits both non-INBOX feeds this worker consumes — `docs/nats-subject-naming.md:162-171` lists only `chat.msg.canonical.*`; there is no row for `chat.bot.canonical.{site}.created` (BOT-MESSAGES-CANONICAL) or `chat.hr.{central}.employees.upsert` (HR-{central}, `spotlight_org.go:71-82`), and line 169 says the worker binds "the stream wildcard" while it binds `.*` (`messages.go:129`).

Verified clean (no finding): pure consumer — no `Publish`, no `chat.user.` handlers, no HTTP routes; INBOX filters `pkg/subject.InboxMemberEventSubjects` (`subject.go:370-380`) are all under the two lanes of `stream.Inbox` (`stream.go:66-74`), bound via `inbox_stream.go:161-171`; INBOX is never created (`main.go:307,314`); internal-lane producers wrap `InboxEvent{Payload}` (`room-worker/handler.go:498-505,1228-1235,2400-2407`) and the external lane arrives wrapped via `pkg/outbox/outbox.go:96-108` → `outbox-worker/handler.go:62`, matching `parseMemberEvent` (`inbox_stream.go:213-235`); `MemberAddEvent`/`MemberRemoveEvent` json tags (`event.go:302-327,680-687`) align with `InboxMemberEvent` (`event.go:125-137`); MESSAGES-CANONICAL producers publish `MessageEvent` with full `Message` on updated (`history-service/internal/service/messages.go:555-573`); HR payload is a bare zstd array with the `Nats-Encoding` header (`teams-hr-sync/publisher.go:76-82`) decoded by `natsutil.DecodePayload` (`compression.go:58-68`) and its json tags match `SpotlightOrgIndex` (`teams_employee.go:7-15`); no Cassandra/`msgbucket` access; IDs only via `teamsmigrate.DeterministicMessageID` (`messages.go:270`); shared knobs mounted from owners (`main.go:54` `mongoutil.PoolConfig`, `main.go:95` `stream.ConsumerSettings` with `CONSUMER_` prefix); external versions use producer `Timestamp` (`messages.go:354,363`, `spotlight.go:99,107`).

### Recommendations

- [high] Give the bot collection its own filter: return `[]string{subject.BotCanonicalCreated(siteID)}` (or a `BotCanonicalWildcard`-derived single-token pattern) when `streamCfg` is the bot stream — `messages.go:122-130` — then fix the pinned assertion at `messages_test.go:621-622` and add an integration test that creates `bot-message-sync` against `stream.BotMessagesCanonical` and asserts a doc lands; this turns a silently dead feed back on.
- [high] Add a startup guard that fails fast when a collection's filters are not a subset of its stream subjects (a few lines over `jetstream.StreamConfig.Subjects` with `subjectIsSubset` semantics, or `cons.Info()` after create) — `main.go:325,348` — the server will not catch this class of bug for you any more.
- [medium] Change `bot-room-service/sysmsg.go:56-65` to publish `model.MessageEvent{Event: EventCreated, Message: msg, SiteID, Timestamp}` like `bot-message-handler`, so every BOT-MESSAGES-CANONICAL consumer sees one shape.
- [medium] Move `main.go:310-323` into `bootstrap.go` `bootstrapStreams(ctx, js, siteID, enabled)` (skipping INBOX/HR by name as now) with a small `streamManager` interface and unit tests, matching the other 12 services.
- [low] Thread the delivery context into `resolveThreadParentCreatedAt` / `resolveTeamsIdentities` (`messages.go:207,220,260,304`) via `BuildAction(ctx, data)` or a ctx-carrying wrapper, so trace links and shutdown cancellation reach ES/Mongo.
- [low] Add BOT-MESSAGES-CANONICAL and HR-{central} rows (producer, consumers, envelope shape) to `docs/nats-subject-naming.md` next to the canonical table.

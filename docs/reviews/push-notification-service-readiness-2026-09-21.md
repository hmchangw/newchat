# push-notification-service — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `a7241fa` (base `main`)  
**Overall score:** 2.7 / 5 (baseline 2026-09-01: 2.7, Δ -0.0)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

push-notification-service is the last hop of the push pipeline and it does not deliver anything. The only dispatcher wired into production is a log stub: it writes a line, returns nil, and the message is acked and removed from the stream. There is no APNs or FCM client anywhere in the repository, no credential or endpoint configuration, and no switch to select a real sink, so every push event the notification pipeline produces is silently consumed. Its retry semantics also directly contradict its own written contract: the ops document says this service must ack on receipt before any provider call, must never NAK, and must run at `MaxDeliver=1`, because a duplicate push is user-visible spam — the code instead acks after dispatch, NAKs transient errors, and inherits `MaxDeliver=6`. That same document tells operators to provision a five-minute duplicate window on a stream whose producer refuses to start under one hour. The consume loop is missing three guards every sibling worker has: it is not registered in the WaitGroup, so a message can slip past the drain and be acked on a drained connection; it has no panic guard, so a real dispatcher's first crash becomes a crash loop; and it stamps no request ID, so none of its log lines correlate with the notification-worker trace that produced them. Worst of all, when the loop dies it returns with no log, no metric and no exit, while the health check probes only the NATS connection — the pod stays Ready while the stream backs up. Coverage is 26.9% because `run` holds the wiring, the retry choice and all three missing guards, and nothing tests it.

| Dimension | Score |
|---|---|
| Code quality | 3 |
| Architecture | 3 |
| Test coverage | 1 |
| Maintainability | 3 |
| Integration | 3 |
| Performance | 3 |

**Findings by severity:** 1 critical, 11 high, 23 medium, 14 low, 4 nitpick (53 total).  
**Highest-risk dimension:** Test coverage (1).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 3

### Evidence

- [high] No request-ID stamped at the JetStream entry point — `push-notification-service/main.go:78-87` — the consume loop hands `mCtx` straight to the handler; it never calls `logctx.ConsumeContext`, which nine sibling consumers use (`roomlist-worker/main.go:365`, `message-worker/main.go:392`, …). `pkg/logctx/consume.go:24-27` names this service explicitly as one that "stamps nothing at all". Result: every line in `handler.go` and the `request_id` field `pkg/jsretry/jsretry.go:116` emits are correlation-less, and the inbound X-Debug rung / payload capture are dropped. CLAUDE.md §"Request Logging & Tracing" makes extraction-at-entry-point a MUST.
- [high] Consume-loop error silently swallowed — `push-notification-service/main.go:78-81` — `if err != nil { return }` ends the only consuming goroutine with no log, no metric and no process exit. The health check is `natsutil.HealthCheck(nc)` alone (`main.go:92`), which probes the connection, so the pod stays Ready while pushes stop. `roomlist-worker/main.go:350-356` distinguishes shutdown from failure and logs ERROR. CLAUDE.md §3 "Never ignore errors silently — comment if intentionally discarded".
- [high] The production dispatcher is a stub, hardcoded with no config seam — `push-notification-service/main.go:61` wires `LogDispatcher{}` (`handler.go:54-62`), which logs and returns nil. A repo-wide grep for apns/fcm/firebase matches only this service's own comments — no real dispatcher exists, and `deploy/user/docker-compose.yml` has no knob to select one. The deployed service therefore acks every `PushNotificationEvent` without delivering it.
- [medium] Per-message goroutine has no panic guard — `push-notification-service/main.go:84-87` — every other JetStream worker in the repo wraps the handler in `pkg/jobguard` (broadcast-worker, message-worker, roomlist-worker, outbox-worker, inbox-worker, …). Here a panic inside `Dispatch` or on a malformed-but-parseable event kills the process; the message stays unacked and redelivers into a crash loop.
- [low] Ack errors discarded without comment on both drop paths — `push-notification-service/handler.go:33` and `handler.go:40` use `_ = msg.Ack()`, while `handler.go:48` logs the identical failure. Inconsistent within one function; CLAUDE.md asks for a comment on an intentional discard.
- [low] Startup failures leak the observability SDK — `push-notification-service/main.go:47-72` — `obsShutdown` is only registered inside `shutdown.Wait` (`main.go:113`), so any of the four early `return fmt.Errorf(...)` paths exits without flushing telemetry; likewise the iterator/goroutine started at `main.go:69-89` is never stopped if `health.ServeWithPprof` fails at `main.go:91`.
- [low] Exported symbols nothing can consume — `push-notification-service/handler.go:16` (`Dispatcher`) and `handler.go:55` (`LogDispatcher`) are exported inside `package main`. CLAUDE.md §3: "Export only what other packages consume".
- [nitpick] Startup log omits the pipeline — `push-notification-service/main.go:98` logs only `site`, yet one binary is deployed twice (`deploy/user` MODE=user, `deploy/bot` MODE=bot); `roomlist-worker/main.go:187` logs `mode`.
- [nitpick] `buildConsumerConfig` signature diverges from the three sibling dual-mode workers — `push-notification-service/main.go:120` takes `stream.Pipeline` and calls `ConsumerName` internally, while `roomlist-worker/main.go:134` / `broadcast-worker/main.go:338` pass the resolved durable name.

### Recommendations

- [high] Replace the raw per-message ctx with `ctx, _ := logctx.ConsumeContext(mCtx, msg.Headers(), msg.Subject(), msg.Data())` before `h.HandleJetStreamMsg` — `main.go:86` — restores request-ID correlation across notification-worker → push, plus the debug rung; zero behaviour change otherwise.
- [high] Log and classify the iterator exit — `main.go:78-81` — mirror `roomlist-worker/main.go:350-356` (Info on `jetstream.ErrMsgIteratorClosed` during shutdown, Error otherwise) and add a "consumer alive" signal to the health endpoint so a dead loop is not Ready.
- [high] Make the dispatcher selectable by config and implement the real one (Resty client with an explicit timeout per CLAUDE.md §HTTP) — `main.go:61` — or, if log-only is the intended state, say so in a comment at `handler.go:55` and gate it behind an explicit `PUSH_DISPATCHER=log` so a silent no-op cannot be mistaken for delivery in prod.
- [medium] Wrap the handler call in `jobguard` — `main.go:84-87` — matches all nine sibling workers and converts a poison-event panic from a crash loop into an Ack-drop.
- [low] Register `obsShutdown` (and iterator stop) via `defer` immediately after each resource is created — `main.go:47-96` — so startup failures still flush traces/logs.
- [low] Unexport `Dispatcher`/`LogDispatcher` and add a one-line comment on the two `_ = msg.Ack()` discards — `handler.go:16,33,40,55`.

## 3. Architecture — score 3

### Evidence

- [high] The only delivery sink is a log stub, wired unconditionally — `push-notification-service/main.go:61`, `push-notification-service/handler.go:54-63`. `newHandler(LogDispatcher{})` is the sole construction; `LogDispatcher.Dispatch` logs and returns nil, so every batch is Acked and removed from PUSH-NOTIFICATION with nothing sent. There is no APNs/FCM client, no credential/endpoint config in `config` (`main.go:21-31`), and no env switch between stub and real sink. The `Dispatcher` seam itself (consumer-defined interface, constructor DI) is right; the production implementation simply does not exist.
- [high] Consume loop runs without `jobguard` — `push-notification-service/main.go:84-87`. Every other JetStream worker in the repo (`notification-worker/main.go:363`, `broadcast-worker/main.go:556`, `message-worker`, `roomlist-worker`, `inbox-worker`, `outbox-worker`, `search-sync-worker`) wraps the per-message call in `jobguard.Run`. Here a panic in `Dispatch` (nil `evt.Data.Sender`, a real HTTP client bug) kills the process with the message un-acked, so JetStream redelivers it after restart — the crash-loop `pkg/jobguard/jobguard.go:1-18` exists to prevent.
- [high] Retry budget is ~12 min against an upstream contract sized for 1 h — `push-notification-service/main.go:120-124` vs `notification-worker/main.go:446`. `buildConsumerConfig` calls `stream.DurableConsumerDefaults(s)` raw, leaving `MaxDeliver=6` (`pkg/stream/consumer.go:141-145`), while the handler settles with `jsretry.DefaultBackoff` (`handler.go:45`), whose documented span across 6 deliveries is 12.6 min nominal and ~6 min at minimum jitter. notification-worker opts into `stream.WithOutageRetryBudget(s, jsretry.DefaultBackoff)` and its bootstrap enforces `Duplicates = stream.OutageRetryWindow` = 1 h on PUSH-NOTIFICATION (`notification-worker/bootstrap.go:43-46`, `pkg/stream/consumer.go:151`). An APNs/FCM outage longer than ~12 min therefore exhausts MaxDeliver and silently drops pushes at the last hop, inside a window the producer deliberately sized to survive.
- [medium] Consume-loop death is silent and invisible to `/healthz` — `push-notification-service/main.go:78-81`. A terminal `iter.Next()` error returns from the pump goroutine with no log, no metric, and no process exit; the health server only checks the NATS connection (`main.go:91-93`), so the pod stays Ready while delivering nothing. `pkg/natsmetrics/loop.go:48-52` (`consumer.LoopFailed`) is the shared handling this loop bypasses.
- [medium] The bounded-pull loop is hand-rolled instead of reusing `natsmetrics.Consume` — `push-notification-service/main.go:74-89` vs `pkg/natsmetrics/loop.go:39-64`. Consequence: no consumer metrics at all (no in-flight gauge, no delivery-attempt/terminal-drop counters, no loop-up gauge), where `broadcast-worker/main.go:442` and `notification-worker/main.go:353` have them. The last hop of the push pipeline is the one with the least telemetry.
- [medium] No `bootstrap.go` / `BOOTSTRAP_STREAMS` config, unlike every other stream-attached service — `main.go:21-31` (12 services ship `bootstrap.go`). CLAUDE.md requires the opt-in helper on services that consume a stream. Stream presence is only checked implicitly by `CreateOrUpdateConsumer` (`main.go:65`) failing; the duplicate-window invariant is verified solely by the producer, so a dev stack started without notification-worker cannot bring this service up.
- [medium] `Dispatcher` is all-or-nothing per batch, with no idempotency contract — `handler.go:16-18`, `handler.go:36-46`. `evt.ID` (`{messageID}-b{N}`, `pkg/model/push.go:5`) dedups *stream entries*, not *device sends*. A partial batch failure (60 of 100 recipients delivered, then an error) NAKs the whole message and re-dispatches all 100, so users get duplicate notifications. The interface exposes no per-recipient result or dedup key for a real sink to use.
- [low] The handler re-implements `jsretry.Settle` — `handler.go:36-47` duplicates the permanent→Ack / transient→Nak branch of `pkg/jsretry/jsretry.go:120-141`, including the log. `jsretry.Settle(ctx, msg, jsretry.DefaultBackoff, err)` is the one-liner.

### Recommendations

- [high] Implement (or explicitly gate) the real sink — `main.go:61`. Add a `PUSH_SINK` mode plus APNs/FCM config, keep `LogDispatcher` as the dev implementation, and fail startup if a production deploy resolves to the stub. Until then this service is a sink-hole for every push.
- [high] Wrap the per-message call in `jobguard.Run(msg, func(){ h.HandleJetStreamMsg(mCtx, msg) })` — `main.go:86`. One line; removes the poison-message crash-loop class.
- [high] Size the consumer with `stream.WithOutageRetryBudget(s, jsretry.DefaultBackoff)` — `main.go:121` — so the last hop rides out the same 1 h window the producer's `Duplicates` window and `MaxDeliver` already assume. Raise `MaxAckPending` alongside it, since parked messages hold slots for the whole window.
- [medium] Replace the hand-rolled loop with `natsmetrics.Start(...)` — `main.go:74-89` — to inherit `LoopFailed`, the in-flight/terminal-drop counters and the loop-up gauge for free, and add a loop-liveness check to `health.ServeWithPprof` so a dead consumer fails readiness.
- [medium] Add `bootstrap.go` with the standard `bootstrapConfig{Enabled}` no-op helper and a non-bootstrap path that verifies PUSH stream presence with a clear error — `main.go:65` — matching the 12 sibling services.
- [medium] Give `Dispatch` a per-recipient result (delivered/permanently-failed/retry) so a redelivery only re-sends the unresolved accounts — `handler.go:16-18`. Otherwise every NAK duplicates notifications on users' devices once a real sink lands.
- [low] Collapse `handler.go:36-47` onto `jsretry.Settle` and drop the manual `errcode.IsPermanent` branch.

## 4. Test coverage — score 1

### Evidence

- [critical] coverage below repo minimum 80%, currently 26.9% (21/78 statements) — `push-notification-service/main.go:40` — `run` (0.0%) and `main` (0.0%) hold ~57 of the 78 statements; only `handler.go` (3 funcs at 100/93.3/100%) and `buildConsumerConfig` (100%) are exercised. CLAUDE.md §4 sets 80% as a MUST-NOT-MERGE floor.
- [high] the entire consume pump and shutdown drain are untestable by construction — `push-notification-service/main.go:76-89` and `:101-110` — the iterator loop and the `wg.Wait()`-vs-`dctx.Done()` drain are inline closures inside `run`, so no test can reach them. The repo already solved this twice: `outbox-worker/main.go:203` extracts the identical pump as `drainPool(ctx, iter, sem, wg, process)` and `roomlist-worker/main.go:334` extracts `consumeLoop(iter messageIterator, …)` — the latter is at 100% via a fake iterator (`roomlist-worker/consumeloop_test.go`). The wiring here is extractable with no behaviour change.
- [high] no integration test and no `TestMain` — `push-notification-service/` has only `handler_test.go` and `consumer_config_test.go`; every peer worker (`notification-worker`, `inbox-worker`, `outbox-worker`, `broadcast-worker`, `roomlist-worker`) ships `integration_test.go`. Nothing verifies that the consumer created from `wiring.PushStream.Name` / `wiring.PushInputWildcard` (`main.go:63-68`) actually binds and delivers.
- [medium] the nak test cannot detect the one nak bug CLAUDE.md forbids — `push-notification-service/handler_test.go:52-53` — `Nak()` and `NakWithDelay()` increment the same `naks` counter and the delay is never asserted, so replacing `jsretry.Nak` (`handler.go:45`) with a bare `Nak()`/`NakWithDelay(0)` would keep `TestDispatchTransientNaks` green.
- [medium] redelivery backoff escalation is never exercised — `push-notification-service/handler_test.go:45` — `Metadata()` returns `(nil, nil)`, a state a real `jetstream.Msg` never produces; `jsretry.backoffFor` (`pkg/jsretry/jsretry.go:155`) then always falls back to `backoff[0]`. The repo-norm fake returns `&jetstream.MsgMetadata{NumDelivered: 1}` (`roomlist-worker/consumeloop_test.go:31-33`).
- [medium] `config` is never parsed in a test — `push-notification-service/main.go:21-31` — no `config_test.go`, unlike `broadcast-worker/config_test.go`, `notification-worker/config_test.go`, `roomlist-worker/config_test.go`. `MODE` being required and rejected for a bad value (via `stream.Pipeline.UnmarshalText`), and the `MAX_WORKERS`/`HEALTH_ADDR` defaults, are unverified at the service boundary.
- [medium] the ack-failure branch is the uncovered 6.7% of `HandleJetStreamMsg` — `push-notification-service/handler.go:48-51` — the fake's `Ack()` always returns nil (`handler_test.go:50`), so the only error path in the handler that is not asserted is the one that silently loses a push on a NATS hiccup.
- [low] four near-identical handler tests written as separate funcs instead of one table-driven test — `push-notification-service/handler_test.go:65,83,95,106` — same construct/dispatch/assert shape with only the dispatcher outcome varying; CLAUDE.md §4 prefers table-driven + `t.Run` here. `consumer_config_test.go:20-29` does it correctly.
- [low] test names do not follow `Test<Type>_<Method>[_<Scenario>]` — `push-notification-service/handler_test.go:65,83,95,106` — e.g. `TestDispatchSuccessAcks` should be `TestHandler_HandleJetStreamMsg_Success`.
- [low] no payload edge cases — `push-notification-service/handler_test.go:69-74` — only one well-formed event shape is ever fed; empty `Accounts`, zero `Data`, absent `UnreadCounts` and a valid-JSON-but-empty body are untested, though the handler dispatches all of them unconditionally (`handler.go:36`).

### Recommendations

- [critical] Extract the pump and the drain out of `run` into `drainPool(ctx, iter, sem, wg, process)` and a `drainWorkers(dctx, wg) error`, mirroring `outbox-worker/main.go:203`, and drive them from a fake `MessagesContext` as `roomlist-worker/consumeloop_test.go` does — `push-notification-service/main.go:74-110`. This is the single biggest lever: it converts most of the 57 uncovered statements into testable units and lifts the service over the 80% floor.
- [high] Add `integration_test.go` (`//go:build integration`, `package main`, `func TestMain(m *testing.M) { testutil.RunTests(m) }`) using `testutil.NATS(t)` to publish a `model.PushNotificationEvent` and assert the consumer built by `buildConsumerConfig` receives and acks it — proves the `stream.Resolve` binding for both `user` and `bot` modes, which `consumer_config_test.go` only checks structurally.
- [medium] Fix the fake message: separate `nak`/`nakDelay` fields, record the duration, and return a real `&jetstream.MsgMetadata{NumDelivered: n}` — `push-notification-service/handler_test.go:45,52-53` — then assert `delay > 0` and that it escalates across redeliveries.
- [medium] Add `config_test.go` with `t.Setenv` cases for required `NATS_URL`/`SITE_ID`/`MODE`, an invalid `MODE`, and the `MAX_WORKERS`/`HEALTH_ADDR`/`PPROF_ENABLED` defaults — `push-notification-service/main.go:21-31`.
- [medium] Cover the ack-failure branch by letting the fake return an error from `Ack()` — `push-notification-service/handler.go:48-51`.
- [low] Collapse the four handler tests into one table-driven `TestHandler_HandleJetStreamMsg` with `t.Run` subtests (success / transient / permanent / malformed / empty accounts / ack error) and rename per CLAUDE.md §4 — `push-notification-service/handler_test.go:65-111`.
- [low] Gate the service in CI with the repo's existing `tools/coveragecheck` (`Makefile:155` pattern): `-include push-notification-service/ -min 80`, so the floor is enforced rather than discovered in audit.

## 5. Maintainability — score 3

### Evidence

- [high] The service is a log-only stub whose retry semantics contradict its own written contract — `push-notification-service/handler.go:45` vs `docs/notification-worker-downstream-contracts.md:265-305` — the doc says push MUST ack on receipt before any provider call, MUST NOT NAK on provider failure, and MUST set `MaxDeliver=1` (duplicate push = user-visible spam). The code instead NAKs transient dispatch errors with `jsretry.DefaultBackoff` and inherits `MaxDeliver=6`/`AckWait=30s` from `pkg/stream/consumer.go:19-20`. Neither artifact references the other, so whoever plugs in APNs/FCM will pick one and silently break the other.
- [high] Consume-loop death is silent and invisible to the health probe — `push-notification-service/main.go:78-81`, `main.go:91-93` — `iter.Next()` error returns with no log, no metric, no self-signal; the only health check is `natsutil.HealthCheck(nc)`. `roomlist-worker/main.go:175-181` documents exactly this hazard ("NATS stays healthy in precisely the failure where the loop has died") and pairs `consume.Check()` with the NATS check plus `shutdown.WaitOn` self-signal. This service is a black hole that stays Ready.
- [medium] The copied consume loop dropped two guards its siblings carry — `push-notification-service/main.go:76-89` — (a) the loop goroutine itself is not in the `WaitGroup`, so a message returned by `iter.Next()` and blocked on `sem <-` can be un-tracked when `wg.Wait()` returns and `nc.Drain()` runs (`notification-worker/main.go:338-343` adds `wg.Add(1)` for the loop with a comment naming this exact window); (b) no `jobguard.Run` panic recovery, so a panic in a real APNs/FCM dispatcher crash-loops the pod on redelivery (`notification-worker/main.go:361-362`).
- [medium] No request-ID propagation — `push-notification-service/handler.go:28-51` — the handler never calls `logctx.ConsumeContext`, so none of its four log lines carry a correlation ID, against CLAUDE.md §3 "Request Logging & Tracing". Every other stream consumer does it (message-gatekeeper, broadcast-worker, message-worker, notification-worker, inbox-worker, outbox-worker, roomlist-worker); `room-worker/main.go:446` is the only deliberate exception and says so. Only `bot-message-worker` shares this gap.
- [medium] ~40 lines of iterator+semaphore+WaitGroup+drain wiring are copy-pasted across 9 services with no `pkg/` helper — `push-notification-service/main.go:69-113`, identical shape at `message-gatekeeper/main.go:222`, `message-worker/main.go:257`, `broadcast-worker/main.go:434`, `notification-worker/main.go:328`, `inbox-worker/main.go:941`, `outbox-worker/main.go:136`, `room-worker/main.go:304`, `bot-message-worker/main.go:153`. This duplication is precisely why the two guards above went missing here — the pattern is copied by eye, not shared.
- [medium] No `bootstrap.go`/`Bootstrap` config despite consuming `PUSH-NOTIFICATION-{siteID}` — `push-notification-service/main.go:21-31` — CLAUDE.md requires every stream-touching service to carry the opt-in helper (10 services have one). The stream is created only by `notification-worker/bootstrap.go:38-48`; here a missing stream surfaces as an opaque `create consumer:` error at `main.go:67` rather than a named startup check.
- [low] `run()` (main.go:40-116, 76 lines) is entirely untested — service coverage is 26.9% (21/78), and all covered statements are `handler.go`. Wiring, shutdown ordering and the drain timeout have no regression net.
- [low] Exported symbols in `package main` — `handler.go:16` (`Dispatcher`), `handler.go:55` (`LogDispatcher`) — nothing can import them; CLAUDE.md says export only what other packages consume.
- [nitpick] `deploy/user/Dockerfile` and `deploy/bot/Dockerfile` are byte-identical — a base-image bump must be applied twice. Repo-wide pattern (same in `broadcast-worker`, `notification-worker`).

### Recommendations

- [high] Reconcile handler.go:45 with the ack-first/`MaxDeliver=1` contract before any real dispatcher lands — either change the code and pin `CONSUMER_MAX_DELIVER=1` in both compose files, or amend `docs/notification-worker-downstream-contracts.md:265-305` and cite the decision in `handler.go`. Leaving both is the single biggest trap for the next engineer.
- [high] Add a consume-loop health check and self-signal, copying `roomlist-worker/main.go:167-190` verbatim (`consumeState` + `shutdown.WaitOn`) — main.go:78-93. Without it a dead loop is undetectable.
- [medium] Extract the shared pull-iterator worker pool into `pkg/` (e.g. `pkg/jsworker`) with the loop-in-WaitGroup guard, `jobguard.Run`, `logctx.ConsumeContext` and loop-liveness baked in; migrate this service first as the smallest consumer. Removes ~40 duplicated lines × 9 and makes the guards impossible to forget.
- [medium] Until that lands, patch the three local gaps directly: `wg.Add(1)` for the loop goroutine, wrap the handler call in `jobguard.Run`, and start the handler from `logctx.ConsumeContext` — main.go:76-89, handler.go:28.
- [medium] Add `bootstrap.go` + `Bootstrap bootstrapConfig` (verify-only branch is enough — this service never creates the stream) so a misconfigured deploy fails with a named error — main.go:21-31.
- [low] Give `Dispatcher` a doc comment naming the concrete work a production impl owns (device-token resolution, FCM multicast ≤500 tokens, 4KB truncation, per-recipient HTTP retry — all already specified at `docs/notification-worker-downstream-contracts.md:67-69,290-295`) and note where its config/store must be wired (main.go:61). Today nothing in the service tree says what "real" means; no device-token store exists anywhere in the repo.
- [low] Cover `run()`'s shutdown ordering and `buildConsumerConfig` wiring against a `testutil.NATS(t)` integration test — the service has no `integration_test.go` at all.

## 6. Integration — score 3

### Evidence

- [high] The consumer implements at-least-once while its own contract doc mandates at-most-once — `push-notification-service/handler.go:36,45,48`. `docs/notification-worker-downstream-contracts.md:266-278` says push-service "MUST ack the JetStream message **on receipt**, before any provider HTTP call, and MUST NOT NAK", and "**Set `MaxDeliver=1`**", because "a duplicate push is user-visible spam". The handler acks only *after* `Dispatch` (`handler.go:48`), NAKs transient errors (`handler.go:45`), and `buildConsumerConfig` takes `stream.DurableConsumerDefaults` → `MaxDeliver=6`, `AckWait=30s` with a `{30s…8m}` BackOff (`main.go:120-124`, `pkg/stream/consumer.go:19-20,60-68`). A real dispatcher that fails after a partial FCM multicast, or a provider call exceeding `AckWait`, re-pushes the whole batch up to 6 times. Latent today only because `LogDispatcher` never errors (`handler.go:55-63`). Either the code or the doc must move; they cannot both ship.
- [medium] Doc contradicts the producer's hard startup check on the shared stream's dedup window — `docs/notification-worker-downstream-contracts.md:390` ("`Duplicates` window ≥ ~5 min", math at `:242`) vs `notification-worker/bootstrap.go:71-74`, which returns a startup error when `Duplicates < stream.OutageRetryWindow` (1h, `pkg/stream/consumer.go:151`). Ops provisioning `PUSH-NOTIFICATION-{siteID}` from §3.1 gets a notification-worker that refuses to boot.
- [medium] Cross-service correlation ID is dropped at this boundary. The producer builds a bare header carrying only `Content-Type` + `Nats-Msg-Id` (`notification-worker/emit.go:54-60`) — no `X-Request-ID` — and the consumer never reads `msg.Headers()` or calls `logctx.ConsumeContext`/`natsutil.WithRequestID`, so no log line in this service carries `request_id` (`handler.go:31-33,39-40,44,50-51`). Every sibling worker does this at the entry point (`notification-worker/main.go:360`, `room-worker/main.go:452-460`). CLAUDE.md §"Request Logging & Tracing" requires extraction/propagation at the NATS message handler.
- [medium] Shutdown race can break even the weaker delivery guarantee — `main.go:76-89` vs `notification-worker/main.go:338-343`. The consume-loop goroutine is not counted in `wg`; between `iter.Next()` returning a message and the worker's `wg.Add(1)`, `iter.Stop()` + `wg.Wait()` (`main.go:100-110`) can observe zero in flight and let `natsutil.Drain` proceed, so the handler acks on a drained connection → redelivery → duplicate push. notification-worker documents this exact window and guards it.
- [low] The producer/consumer codec asymmetry is unpinned. `notification-worker/emit.go:42` marshals with `sonic` — including the `UnreadCounts map[string]int` field (`pkg/model/push.go:17`) — while this service decodes with `encoding/json` (`handler.go:30`) and every test re-encodes with stdlib (`handler_test.go:89-94`). CLAUDE.md requires a wire-compat test before adopting sonic on a path that marshals `map` fields; `notification-worker/pretouch_test.go:16` only checks codec compilability, not stdlib round-trip of a populated `PushNotificationEvent`.
- [low] `PushNotificationEvent.Timestamp` is decoded but never used (`pkg/model/push.go:13`; no reference in `handler.go`). With `MaxDeliver=6` on `jsretry.DefaultBackoff` a parked event can arrive ~12 min late and is still dispatched as a live push; `data.pushTime` is likewise unread.
- [low] The outbound push payload shape (PushNotificationEvent → APNs/FCM) is documented nowhere; `docs/notification-worker-downstream-contracts.md:284-292` gives prose guidance (truncate body, multicast, 2 HTTP attempts) but no mapping, and the shipped `Dispatcher` is a log-only stub (`handler.go:55-63`).
- [low] Docs still describe this service as unbuilt: "Until the push service consumes the stream, push events accumulate / are dropped" (`:17-19`), rollout step 3 "Ship the push service consumer" (`:451`); `docs/architecture.md:207,228` draws `PUSH-NOTIFICATION-{site}` as a terminal node with no consumer, and its worker list (`:85-94`) omits the service.
- Verified clean: subjects/streams come only from `stream.Resolve` → `pkg/subject` (`main.go:63-65`, `pkg/stream/pipeline.go:59-66`) — no raw `fmt.Sprintf`; durable named after the service with the bot prefix (`main.go:122`); no hardcoded `cc.BackOff`, no bare `Nak()`/`NakWithDelay(0)` (routes via `jsretry.Nak`); high-throughput `Messages()`+semaphore with `PullMaxMessages(2*MaxWorkers)` (`main.go:69-89`); no stream creation (bootstrap correctly absent); shared knobs mounted, not re-declared (`main.go:26`); no ID generation; no INBOX/OUTBOX or Cassandra participation; no `chat.user.*` handler or HTTP route, so `docs/client-api.md` and its derived views need no entry.

### Recommendations

- [high] Resolve the delivery-semantics conflict explicitly — `handler.go:36-48` vs contract `:266-278`. Either ack immediately after decode and drop the NAK path (matching the doc), or amend the doc to the at-least-once design and state how duplicate multicast sends are suppressed provider-side.
- [medium] Fix `docs/notification-worker-downstream-contracts.md:390` to `Duplicates ≥ 1h` (`stream.OutageRetryWindow`), citing `notification-worker/bootstrap.go:71-74`, and refresh `:17-19`/`:451` plus `docs/architecture.md:85-94,228` now that the consumer exists.
- [medium] Stamp `X-Request-ID` on publish (`notification-worker/emit.go:54-60`, via `natsutil.HeaderForContext`) and adopt `logctx.ConsumeContext` at `handler.go:28` so push logs join the message's trace.
- [medium] Count the consume-loop goroutine in `wg` (`main.go:76`), mirroring `notification-worker/main.go:338-343`, so drain cannot overtake an in-flight message.
- [low] Add a wire-compat test encoding a fully populated `PushNotificationEvent` (incl. `UnreadCounts`) with `sonic` and decoding it here with `encoding/json`, asserting field equality — closes the only untested producer/consumer link.
- [low] Have the handler read `evt.Timestamp` and drop or downgrade pushes older than a configurable staleness bound before calling the provider.

## 7. Performance — score 3

### Evidence

- [medium] Consume loop dies silently — no log, no metric, no exit — `push-notification-service/main.go:78-81` — `iter.Next()` error just `return`s. The only health probe is NATS connectivity (`pkg/natsutil/health.go:29-35`), so a deleted/expired consumer leaves a Ready pod consuming nothing while the PUSH stream backs up. Peers call `consumerMetrics.LoopFailed` (`notification-worker/main.go:347`).
- [medium] Zero consumer instrumentation — `push-notification-service/main.go:76-89` — the service imports no `pkg/natsmetrics`; there is no in-flight gauge, no delivery-count, no terminal-reason metric. There is therefore no signal at all for "can it keep up with notification-worker's batch rate", which is the core question for this dimension.
- [medium] No panic guard on the worker goroutine — `push-notification-service/main.go:84-87` — every other JetStream worker wraps the handler in `jobguard.Run` (`broadcast-worker/main.go:556`, `message-worker/main.go:391`, `notification-worker/main.go:363`, `outbox-worker/main.go:104`, …). A panic inside `Dispatch` (a real APNs/FCM client will parse remote JSON) kills the process and stalls the whole stream, then crash-loops on redelivery.
- [medium] No per-message deadline; the raw message context reaches the dispatcher — `push-notification-service/handler.go:36`, `main.go:86` — the o11y iterator returns a baggage-restored context with no deadline (`flywindy/o11y@v0.12.0/nats/jetstream.go:627-632`). With `MaxWorkers=100` and a real push sink, a hung outbound call would pin a worker slot and an ack-pending slot indefinitely, far past `AckWait=30s`.
- [medium] Codec drift on a hot path: producer marshals with sonic, consumer decodes with stdlib — `push-notification-service/handler.go:5,30` vs `notification-worker/emit.go:42` and `notification-worker/pretouch.go:13` (which pretouches `model.PushNotificationEvent`). Same struct, same event rate, opposite codec, and no `jsonwarm.Pretouch` here. The service is a message hot-path worker but is absent from CLAUDE.md's sonic list — the list has drifted, not the code. `UnreadCounts` is `map[string]int` (string-keyed), so sonic decode is safe; the `message-gatekeeper` exception is about struct-keyed maps.
- [medium] Retry schedule is the wrong class for a user-visible delivery worker — `push-notification-service/handler.go:45` uses `jsretry.DefaultBackoff` (1s first rung) where `pkg/jsretry/jsretry.go:74-77` documents `LowLatencyBackoff` for exactly "fan-out / delivery workers". `buildConsumerConfig` also takes plain defaults (`main.go:121`), skipping `stream.WithOutageRetryBudget` that `broadcast-worker/main.go:565` applies, so the real budget is ~6-12 min under jitter while the PUSH stream's dedup window is sized at `stream.OutageRetryWindow` = 1h (`notification-worker/bootstrap_test.go:80`). A 20-minute APNs incident drops pushes the dedup window was sized to protect.
- [medium] Dispatch loop goroutine is not registered in the WaitGroup — `push-notification-service/main.go:76` — shutdown does `iter.Stop()` then `wg.Wait()` (`main.go:100-110`), so a message already returned by `Next` but not yet handed to a worker can slip past the drain and be processed after `nc.Drain()`. Peers add the loop deliberately and comment on this exact race (`notification-worker/main.go:337-339`, `pkg/natsmetrics/loop.go:24-29`). Shared with `bot-message-worker/main.go:160`, which this loop was copied from.
- [low] The production dispatcher does not exist — `push-notification-service/handler.go:57-62` (`LogDispatcher`), wired at `main.go:61`. There is no HTTP client, so there is nothing to review for timeouts, connection pooling or HTTP/2 stream limits, and the `MaxWorkers=100` / `PullMaxMessages(200)` sizing (`main.go:69,74`) is untested against any real sink.
- [nitpick] Health + pprof start after the consumer (`main.go:91`), so profiling is unavailable during the first backlog drain, and an error there returns without `iter.Stop()`/`Drain`.

### Recommendations

- [medium] Move the loop onto `natsmetrics.Start` + `guardedProcessor` — `main.go:76-89` — one change buys the WaitGroup registration, `LoopFailed` terminal metrics, per-message tracking and `jobguard` panic recovery, all already written and tested in `pkg/natsmetrics/loop.go` and `broadcast-worker/main.go:552-557`.
- [medium] Add a consume-loop liveness check to `health.ServeWithPprof` (`main.go:91`) so a dead iterator fails readiness instead of parking a silent pod.
- [medium] Bound each dispatch: `ctx, cancel := context.WithTimeout(mCtx, cfg.DispatchTimeout)` before `h.dispatcher.Dispatch` (`handler.go:36`), with the timeout safely under `EffectiveAckWait` (`pkg/stream/consumer.go:120-131`). Do this before any real APNs/FCM client lands, not after.
- [medium] Switch to `jsretry.LowLatencyBackoff` (`handler.go:45`) and wrap the consumer settings in `stream.WithOutageRetryBudget(s, jsretry.LowLatencyBackoff)` (`main.go:121`), matching `broadcast-worker`. A late push is useful; a dropped one is not.
- [medium] Decide the codec explicitly: either adopt `sonic` + `jsonwarm.Pretouch(model.PushNotificationEvent{})` and add the service to CLAUDE.md's hot-path list, or add a one-line comment at `handler.go:30` saying batch rate makes stdlib sufficient. Today it is neither.
- [low] When the real dispatcher is written, give it one shared pooled client (Resty per CLAUDE.md) with `MaxConnsPerHost`/HTTP-2 limits sized against `MAX_WORKERS=100`, not a client per message.

# teams-chat-sync — production readiness review

**Date:** 2026-09-21  
**Branch:** `claude/production-readiness-report-2026-09-21` @ `c4b461d` (base `main`)  
**Overall score:** 3.2 / 5 (baseline 2026-09-01: 3.5, Δ -0.3)  
**Method:** six independent expert reviews (one per dimension) against `CLAUDE.md` and Go-at-scale practice; every finding cites `file:line` on this tree.

## Executive summary

`teams-chat-sync` is one of the better-built services in the fleet on pure craftsmanship — `syncer.go` is at 100% statement coverage with genuine error-path tests (a Graph failure holds the watermark, an upsert failure holds it, a `SetFrom` failure fails only that user), files are small, wraps are honest, seams are injectable, and there are zero TODOs. It lands at 3.2 because two decisions outside that core are load-bearing and wrong.

The first is a silent data-loss path that two reviewers found independently from opposite directions. `buildChat` decides a roster is complete when a non-1:1 chat comes back from Graph with fewer than 25 inline members (`syncer.go:315`), and then `$set`s `members` plus `needCreateRoom: true` as authoritative. But `msgraph.Chat` decodes only `members` — never `members@odata.nextLink` — so a *truncated* `$expand=members` response is indistinguishable from a complete one, and a group chat that comes back with **zero** members takes the same "complete" branch. `teams-room-creation` republishes that roster and `room-worker/teamsroomcreate.go:153-177` reconciles subscriptions to it, calling `DeleteSubscriptionsByAccounts` for everyone absent — so one degraded Graph response mass-unsubscribes a room. The threshold's own comment says it MUST stay at or below Graph's inline cap, but nothing derives, asserts or cross-checks that number, and `syncer_test.go:129` actually pins "empty group finalized inline" as the intended behaviour. The fix is small: decode the `nextLink`, and defer to `teams-chat-member-sync` whenever the roster is empty or possibly truncated.

The second is the TLS default. `GRAPH_TLS_INSECURE_SKIP_VERIFY` is `envDefault:"true"` (`main.go:51`), so a Kubernetes CronJob that simply omits the variable runs with verification off — and the same `http.Client` carries the OAuth token request to the public `login.microsoftonline.com`, so `GRAPH_CLIENT_SECRET` traverses an unverified connection. The service's own compose file ships `false`, the accepted design plan specified `false`, and `teams-hr-sync` and `user-presence-service/sync` default `false` while this service, `teams-user-sync` and `teams-chat-member-sync` default `true`. That divergence is the direct consequence of a CLAUDE.md §Configuration violation: the whole Graph config block — tags, defaults and comments — is copy-pasted into six services instead of being declared once in `pkg/msgraph` and mounted as a named field, the way `Pool mongoutil.PoolConfig` already is in the same struct. gosec sees none of it, because the `#nosec G402` lives in `pkg/msgraph`.

Beyond those: there is no run deadline anywhere, explicitly delegated to a Kubernetes CronJob manifest that does not exist in `deploy/`, and no `context.WithTimeout` on any Graph or Mongo call — so a hung primary parks all workers indefinitely. The dispatcher never selects on `ctx.Done()`, so a routine SIGTERM eviction drains every remaining user through a cancelled context and reports itself as a mass outage. And the per-user Graph window is unbounded with an all-or-nothing watermark, so a backfill that can't complete in four throttled attempts refetches the identical six-month window on every subsequent run, forever.

Coverage is 67.6%, but the more useful finding is that the service's pipeline runs `go test` with no `-tags=integration` — so `EnsureIndexes`, the whole Mongo layer, and the named "re-sync must not clobber member-sync's `needCreateRoom`" regression guard are verified by tests CI never executes. Counting them would put the service at 80.1%, just over the floor.

| Dimension | Score |
|---|---|
| Code quality | 3 |
| Architecture | 4 |
| Test coverage | 2 |
| Maintainability | 4 |
| Integration | 3 |
| Performance | 3 |

**Findings by severity:** 0 critical, 11 high, 15 medium, 15 low, 7 nitpick (48 total).  
**Highest-risk dimension:** Test coverage (2).

**Repo-wide inputs used by every dimension:** `make generate` → no stale mocks; gosec (medium+) and the 20 repo-owned semgrep rules → 0 findings; govulncheck and the semgrep registry packs could not run (sandbox egress 403) so dependency-CVE status is unverified; unit coverage from one `go test -race -covermode=atomic ./...` run with 0 failing packages.


## 2. Code quality — score 3

### Evidence

- [high] TLS verification is disabled by default, and the same transport carries the OAuth client-secret POST — `teams-chat-sync/main.go:51` — `GRAPH_TLS_INSECURE_SKIP_VERIFY` is `envDefault:"true"`, so production (which simply omits the var) fails *open*. `pkg/msgraph/msgraph.go:272-278` installs `InsecureSkipVerify: true` on the single `http.Client` that `pkg/msgraph/msgraph.go:506-513` also uses for the token request to the **public** `login.microsoftonline.com` endpoint (`msgraph.go:285-288`), so `GRAPH_CLIENT_SECRET` (and `GRAPH_PROXY_PASSWORD` on a Basic-auth proxy) traverse an unverified connection.
- [high] That insecure default contradicts both the service's design plan and its own compose file — `teams-chat-sync/deploy/docker-compose.yml:18` — compose ships `false`, and the plan specified `envDefault:"false"`, "opt-in" (`docs/superpowers/plans/2026-07-14-teams-chat-sync.md:1801`). It reads as unreviewed drift, not a decision.
- [high] A knob shared by five services is re-declared per service with divergent defaults — `teams-chat-sync/main.go:51` vs `teams-hr-sync/config.go:28` (`false`), `user-presence-service/sync/main.go:44` (`false`), `teams-user-sync/config.go:20` (`true`), `teams-chat-member-sync/main.go:44` (`true`). CLAUDE.md §Configuration MUSTs that such a knob be declared once in the owning package (`pkg/msgraph`) and mounted as a named field — "never re-declare the env tag and `envDefault` in a service".
- [medium] A graceful SIGTERM is logged as a mass failure — `teams-chat-sync/syncer.go:161-176` — the dispatch loop sends on `jobs` without selecting on `ctx.Done()`, so on cancellation every remaining user drains through `syncUser`, fails with `context.Canceled`, and emits one `slog.Error("teams chat sync: user failed")` each plus an aggregate `"%d of %d users failed"` and exit 1. A routine eviction is indistinguishable from a Graph/Mongo outage.
- [low] A defensive branch that production can never reach carries most of the worker tests — `teams-chat-sync/syncer.go:218-221` — the empty-`SiteID` skip is unreachable because `SYNC_DEFAULT_SITE_ID` is `required,notEmpty` (`main.go:39`), as its own comment concedes; yet `worker_test.go:32` builds every syncer with an empty `DefaultSiteID`, and `worker_test.go:202`/`:218` assert on the skip. The branch silently drops chats.
- [low] `syncer.run` deadlocks instead of failing fast on a non-positive worker count — `teams-chat-sync/syncer.go:156` + `:175` — no goroutine drains the unbuffered `jobs` channel, so the send blocks forever. Only `validateConfig` (`main.go:82`) prevents it; `newSyncer` (`syncer.go:50`) accepts the value unchecked.
- [nitpick] `fmt.Errorf` with no format verbs where `errors.New` belongs — `teams-chat-sync/main.go:83` and `:86`. Neither `go vet` nor the repo's golangci config catches it.
- [nitpick] Five `//nolint:gocritic // hugeParam` suppressions in ~400 lines — `main.go:80`, `store_mongo.go:123`, `syncer.go:103`, `:194`, `:206`. The four non-startup ones sit on the per-user/per-chat path; pointers would remove both copy and suppression.

### Recommendations

- [high] Flip `GRAPH_TLS_INSECURE_SKIP_VERIFY` to `envDefault:"false"` — `main.go:51` — restores fail-closed behaviour, matches the design plan and compose, and stops the client secret riding an unverified connection to Azure AD.
- [high] Move the knob into `pkg/msgraph` as a mounted config struct (alongside `ProxyURL`/`ProxyUsername`/`ProxyPassword`, which have the same problem) and delete the per-service `env`/`envDefault` tags in all five services — resolves the CLAUDE.md §Configuration violation and makes the default unfalsifiable.
- [medium] Split the peer TLS decision from the Graph host: if an on-prem TLS-intercepting proxy is genuinely required, ship its CA via a `GRAPH_CA_BUNDLE` root pool rather than `InsecureSkipVerify` — keeps verification on for the token endpoint, which is never on-prem.
- [medium] `select { case jobs <- u: case <-ctx.Done(): }` in the dispatch loop, and branch on `errors.Is(err, context.Canceled)` in the worker to log at `Info` and skip the `Failed` counter — `syncer.go:161-176` — so a graceful stop reads as a graceful stop.
- [low] Validate `MaxWorkers > 0` in `newSyncer` (or buffer `jobs`) — `syncer.go:50` — turns a silent hang into an immediate error.
- [low] Drop the unreachable empty-`SiteID` skip and give `newTestSyncer` a non-empty `DefaultSiteID` — `syncer.go:218`, `worker_test.go:32` — so the tests pin the production configuration instead of an impossible one.

## 3. Architecture — score 4

### Evidence

- [high] The inline-roster completeness invariant is comment-only and undetectable at runtime — `teams-chat-sync/syncer.go:94` (and `:124`). `inlineMemberThreshold = 25` decides the handoff to `teams-chat-member-sync`; the comment says it "MUST stay at or below Graph's inline-expansion cap". But `msgraph.Chat` (`pkg/msgraph/chats.go:40-47`) decodes only `members`, never `members@odata.nextLink`, so a truncated `$expand=members` roster is indistinguishable from a complete one. A chat in that band is then finalized inline (`store_mongo.go:159-166`, `$set members` + `needCreateRoom=true`) with a silently short roster and a room is created missing members. Nothing guards it.
- [high] TLS verification to Microsoft Graph is disabled by default — `teams-chat-sync/main.go:51` (`envDefault:"true"`). The family disagrees with itself: `teams-hr-sync/config.go:28` and `user-presence-service/sync/main.go:44` default `false`. `deploy/docker-compose.yml:18` sets `false`, so the safe value exists only in local dev — a k8s CronJob omitting the var runs MITM-able against Graph while carrying a client secret. gosec is clean only because `pkg/msgraph/msgraph.go:277-278` carries a `#nosec`/`nolint` pair, so no scanner sees the default.
- [medium] No run deadline anywhere, delegated to an artifact that does not exist — `main.go:110-113` ("The run deadline is owned by the Kubernetes CronJob"). `deploy/` holds only Dockerfile, docker-compose.yml and azure-pipelines.yml — no CronJob manifest, so `activeDeadlineSeconds`/`concurrencyPolicy` are an unverifiable external contract. With unbounded `@odata.nextLink` paging (`pkg/msgraph/chats.go:102-116`) and a shared gate that can park every worker 5 min per 429 (`chats.go:63`, `:222-235`), only the 30 s per-request timeout bounds a run.
- [medium] Cancellation is not observed by the dispatcher, so SIGTERM reports a mass failure — `syncer.go:170-176`. The dispatch loop has no `ctx.Done()` arm; on eviction it keeps feeding every remaining user into `jobs`, each failing at once on the cancelled Graph call, incrementing `sum.Failed` and emitting `slog.Error` (`syncer.go:161-165`). `run` returns `"%d of %d users failed"` (`:186`), making routine eviction indistinguishable from an outage. No data loss (watermarks advance only on success, `:230`), but the alert signal is destroyed. `teams-chat-member-sync/syncer.go:154-156` has the identical gap.
- [medium] Graph degradation is handled for throttling only, not transient failure — `pkg/msgraph/chats.go:137-139` and `:172-185`. A transport error or 500/502/504 returns on the first attempt; only 429/503 retry. For a CronJob, one transient 5xx costs that user a full sync interval of chat visibility. Compensation is correct (watermark held, window re-fetched), so this is latency, not loss.
- [low] The service creates indexes it never queries, for three siblings, best-effort — `store_mongo.go:44-69` builds `needMemberSync_pending`, `needCreateRoom_pending`, `needVerify_pending`; the consumers are `teams-chat-member-sync`, `teams-room-creation`, `teams-room-verify`, none of which call `EnsureIndexes` (repo-wide grep: this is the only non-test call site), and `main.go:123-125` downgrades failure to `slog.Warn`. Ownership is defensible, but downstream scans have no index guarantee of their own.
- [nitpick] Store interfaces exported inside `package main` — `store.go:15`, `:29` export `TeamsUserStore`/`TeamsChatStore` while `chatsFetcher` (`:35`) is unexported, in the same file. Nothing outside `package main` can consume any of them.

### Recommendations

- [high] Add `MembersNextLink string \`json:"members@odata.nextLink"\`` to `msgraph.Chat` (`pkg/msgraph/chats.go:40`) and force `NeedMemberSync = true` in `buildChat` (`syncer.go:124`) when non-empty — turning an unverifiable assumption into a detected condition that falls back to the authoritative member-sync path. Removes the only silent-data-loss path here.
- [high] Flip `GRAPH_TLS_INSECURE_SKIP_VERIFY` to `envDefault:"false"` (`main.go:51`), making on-prem proxy deployments opt in; do the same in `teams-user-sync` and `teams-chat-member-sync` so the family has one default.
- [medium] Add `select { case jobs <- u: case <-ctx.Done(): }` at `syncer.go:170-176` and classify `context.Canceled` apart from `sum.Failed` (`:161`), so eviction exits promptly and does not look like an outage.
- [medium] Commit the CronJob manifest under `deploy/` with `activeDeadlineSeconds`, `concurrencyPolicy: Forbid`, `backoffLimit` — or replace the comment at `main.go:110-113` with a real `context.WithTimeout` from a `SYNC_RUN_TIMEOUT` knob.
- [medium] Extend `getThrottled` (`pkg/msgraph/chats.go:166-185`) to retry transport errors and 5xx with bounded jittered backoff, keeping the tenant-wide gate for 429/503 only. Benefits all `msgraph` consumers.
- [low] Have each downstream consumer assert its own index (or share one ensure helper), and raise `EnsureIndexes` failure above `slog.Warn` (`main.go:123-125`).

## 4. Test coverage — score 2

### Evidence

- [high] Coverage below repo minimum 80%, currently 67.6% (92/136 statements) — `teams-chat-sync/main.go:1` — Single `package main`, no sub-packages. Per-file from the provided profile: `syncer.go` 100.0% (72/72), `store_mongo.go` 37.0% (10/27), `main.go` 27.0% (10/37). CLAUDE.md §4 sets an 80% floor, so this dimension is floored at 2.
- [high] The service's entire MongoDB layer is verified only by build-tagged integration tests that CI never runs — `teams-chat-sync/deploy/azure-pipelines.yml:45` — The only test step is `go test ./$(SERVICE_DIR)/... -v -race -coverprofile=coverage-sync.out`, with no `-tags=integration`. `integration_test.go` is the sole coverage of `EnsureIndexes` (`store_mongo.go:230`), `ListUsers` (`:268`), `SetFrom` (`:279`) and `UpsertChats` (`:291`), and of the load-bearing invariants (siteId immutability, "re-sync must not clobber member-sync's `needCreateRoom`"). Other pipelines in this repo do run the tag (e.g. `data-migration/oplog-connector/deploy/azure-pipelines.yml`), so this is a per-service omission, not a repo norm. Those 17 statements are exactly the gap: counting them would put the service at 80.1%.
- [medium] `run()` is 0% covered and structurally untestable — `teams-chat-sync/main.go:100` — 24 of the service's 44 uncovered statements. Config parse, signal wiring (`:113`), Mongo connect, Graph client build and the deliberate warn-and-continue on `EnsureIndexes` failure (`:123-125`) are all inline, so the one genuinely policy-bearing branch there ("indexes are best-effort, keep going") has no test. `validateConfig` was correctly extracted for testability (100%, `main.go:81`); the rest of `run` was not.
- [medium] Context cancellation — the documented SIGTERM story — has no test, and the dispatcher does not honour it — `teams-chat-sync/syncer.go:170` — `main.go:110-113` claims the run "aborts between operations instead of being killed mid-batch", but `run`'s dispatch loop (`syncer.go:170-176`) never checks `ctx.Err()`; on cancel it keeps feeding every remaining user to workers, each failing fast through Graph/Mongo and inflating `sum.Failed`. No test passes a cancelled/cancellable context to `s.run`, so neither the claim nor the resulting failure-count behaviour is pinned.
- [low] The shared syncer test fixture configures a state the production config forbids — `teams-chat-sync/worker_test.go:32` — `newTestSyncer` builds `syncConfig` with `DefaultSiteID` empty, which `syncer.go:29-31` and `:216-218` describe as only reachable "via direct construction with an empty default (tests)". Nine of the eleven `TestRun_*` cases therefore run the defensive skip path rather than the production one; only `TestRun_EmptySiteIDVote_ConfiguredDefaultIsUsed` (`worker_test.go:241`) exercises a realistic config.
- [low] Syncer tests are split across a file with no production counterpart — `teams-chat-sync/worker_test.go:1` — There is no `worker.go`; `run`/`syncUser` live in `syncer.go`, whose `syncer_test.go` covers only the pure mappers. CLAUDE.md's per-service layout pairs `X_test.go` with `X.go`; the orphan name makes the test-to-code mapping non-obvious.

What is genuinely good (context for the score, not padding): `syncer.go` is at 100% with real error-path tests — Graph failure holds the watermark (`worker_test.go:159`), upsert failure holds it (`:178`), `SetFrom` failure fails the user (`:191`), `ListUsers` failure aborts (`:267`) — plus a named data-loss regression guard (`:119-157`). Table-driven `t.Run` structure is used where it fits (`main_test.go:79`, `syncer_test.go:210`, `:290`). Mocks are `go.uber.org/mock` via the `//go:generate` directive in `store.go:165`, in `mock_store_test.go`. Integration tests are correctly tagged (`integration_test.go:1`), in `package main`, use only `testutil.MongoDB`, and have `TestMain(m) { testutil.RunTests(m) }` (`:21`). Helpers are all in `_test.go`; package-level test vars are immutable values, and `unsetProxyEnv` (`main_test.go:169`) restores prior env via `t.Cleanup`, so tests are order-independent.

### Recommendations

- [high] Add an integration test step to the pipeline — `teams-chat-sync/deploy/azure-pipelines.yml:45` — e.g. a `go test -tags=integration ./$(SERVICE_DIR)/... -race` step mirroring the data-migration pipelines. This alone lifts verified coverage from 67.6% to ~80.1% and puts the siteId-immutability and `needCreateRoom`-clobber guards on the merge path.
- [high] Extract `run()`'s wiring behind a seam — `teams-chat-sync/main.go:100` — Split the dependency construction from the orchestration (as `validateConfig` already was) so the `EnsureIndexes`-fails-but-continue branch (`:123`) and the sync-error wrap (`:149`) get unit tests. Target: `main.go` above 80% on its own.
- [medium] Add `TestRun_ContextCancelled` and make the dispatch loop honour cancellation — `teams-chat-sync/syncer.go:170` — Select on `ctx.Done()` when feeding `jobs`, and assert that a cancelled run stops dispatching rather than marking every remaining user failed. Write the test first: it should fail against today's code.
- [medium] Enforce the 80% floor in CI instead of only writing the profile — `teams-chat-sync/deploy/azure-pipelines.yml:45-46` — `coverage-sync.out` is produced and never asserted on, so the drop to 67.6% was invisible. A `go tool cover -func` threshold check turns the CLAUDE.md MUST into a gate.
- [low] Give `newTestSyncer` a non-empty `DefaultSiteID` and keep the empty-default case as an explicit opt-in — `teams-chat-sync/worker_test.go:32` — so the default fixture matches the `required,notEmpty` production config and the skip branch is tested deliberately rather than by accident.
- [low] Rename `worker_test.go` to match its subject (fold into `syncer_test.go`, or split `syncer_run_test.go`) — `teams-chat-sync/worker_test.go:1`.

## 5. Maintainability — score 4

### Evidence

- [medium] Graph env-config block copy-pasted into 6 services, defaults already diverged — `teams-chat-sync/main.go:41-65`. The same 25 lines (tags + comments, word-for-word) sit at `teams-chat-member-sync/main.go:37-58`, `teams-user-sync/config.go:11-34`, `teams-hr-sync/config.go:28-41`, `user-presence-service/sync/main.go:33-56`. `GRAPH_TLS_INSECURE_SKIP_VERIFY` defaults `true` here (`main.go:51`) but `false` at `teams-hr-sync/config.go:28` and `user-presence-service/sync/main.go:44`. CLAUDE.md §Configuration requires a multi-service knob be declared once in the owning package and mounted as a named field — `Pool mongoutil.PoolConfig` (`main.go:30`) does this; Graph does not.
- [medium] Code and compose defaults for that knob disagree — `main.go:51` (`envDefault:"true"`) vs `deploy/docker-compose.yml:18` (`:-false`). Neither file tells a reader which value production gets when the var is unset.
- [medium] `msgraph.Config.UserAgent` unreachable from this job — no `GRAPH_USER_AGENT` field in `Config` (`main.go:24-66`), though `room-service/main.go:86` and `user-presence-service/sync/main.go:64` expose it. `pkg/msgraph/msgraph.go:170-177` documents the pinned browser UA as per-environment overridable "since a pinned browser version ages"; for the teams-* family it is not, so a WAF/proxy UA rejection needs a code change.
- [low] CronJob skeleton duplicated, not shared — `main.go:68-125` (JSON slog, error-returning `run()`, `signal.NotifyContext`, Mongo connect + deferred disconnect, `MaxWorkers`/`Pool.Validate`) is structurally identical to `teams-chat-member-sync/main.go:61-110` and near-identical in the other four job services.
- [low] Fan-out + `summary` accounting duplicated, with a third competing idiom — `syncer.go:131-189` (atomics, unbuffered `jobs` chan, `MaxWorkers` goroutines, final log, `"%d of %d ... failed"`) is a line-for-line twin of `teams-chat-member-sync/syncer.go:113-168`, while `teams-room-creation/runner.go:58` and `teams-room-verify/runner.go:76` use a semaphore shape instead.
- [low] `deploy/` duplicated 7× under two naming conventions — `deploy/Dockerfile:1-13` is byte-identical to all six siblings modulo the service name; `deploy/azure-pipelines.yml` differs only in `SERVICE_DIR`/`IMAGE_NAME` (`:22-23`) vs `SERVICE_NAME` (`teams-user-sync/deploy/azure-pipelines.yml:22`) and the profile filename (`:45`). ~580 lines needing a 7-way edit.
- [low] Pipeline runs neither lint nor SAST and bypasses `make` — `deploy/azure-pipelines.yml:39-49` runs raw `go vet`/`test`/`build` only. CLAUDE.md §2 mandates `make` targets, §5 makes SAST a blocking gate.
- [low] Weaker secret validation than the sibling that copied the block — `main.go:43` is `required` only vs `required,notEmpty` at `teams-user-sync/config.go:13`; an empty secret starts the job and fails later at token acquisition.
- [nitpick] `worker_test.go:49` tests `syncer.run`/`syncUser` but no `worker.go` exists — 273 lines orphaned from the layout convention.
- [nitpick] `Config` (`main.go:24`), `TeamsUserStore`/`TeamsChatStore` (`store.go:15,29`) exported from `package main` with no external consumer; `teams-user-sync/config.go:10` uses lowercase `config` for the same thing.

Sizes are healthy: largest file `syncer.go` (234 lines); largest functions `syncer.run` (`syncer.go:140`, 55 lines) and `run` (`main.go:100`, 55 lines) — far under 500/80. Zero TODO/FIXME/HACK. ~30% comment density, overwhelmingly WHY (`syncer.go:34-42`, `:89-93`); `store_mongo.go:95-99` and `:112-122` restate the branch structure, the one WHAT-leaning spot. Six `//nolint:gocritic` (`main.go:80`, `syncer.go:103,194,206`, `store_mongo.go:102,123`), all justified. Seams are good: `validateConfig` (`main.go:81`) pure and tested, `syncConfig.Now` (`syncer.go:27`) injectable, store interfaces in the consumer with only the needed methods. A new engineer could safely change this service in a day; the risk is the six places a family-wide change must land.

### Recommendations

- [medium] Move the Graph knobs into `pkg/msgraph` as an env-tagged `msgraph.EnvConfig` and mount it as `Graph msgraph.EnvConfig` — `main.go:41-65`. Removes ~150 duplicated lines across 6 services, makes the TLS default one decision instead of five, and exposes `GRAPH_USER_AGENT` everywhere for free.
- [medium] Settle the TLS default in that one place and make `deploy/docker-compose.yml:18` agree with `main.go:51` — today the effective production value is invisible at both sites.
- [low] Extract the CronJob bootstrap into a shared package (JSON slog, `signal.NotifyContext`, Mongo connect/disconnect, error-returning `run`) — `main.go:68-125`. ~50 lines per job service and one shutdown/exit contract instead of five hand-copied ones.
- [low] Extract the fan-out + counters helper and converge the family on it — `syncer.go:131-189` vs `teams-chat-member-sync/syncer.go:113-168` vs `teams-room-creation/runner.go:58`. One idiom means one place to fix a cancellation or accounting bug.
- [low] Template `deploy/Dockerfile` and `deploy/azure-pipelines.yml` across the seven services and standardize the variable name — `deploy/azure-pipelines.yml:22-23`.
- [low] Add `make lint` / `make sast` and switch to `make test SERVICE=…` / `make build SERVICE=…` — `deploy/azure-pipelines.yml:39-49`.
- [nitpick] Rename `worker_test.go` → `syncer_run_test.go`, tighten `main.go:43` to `required,notEmpty`, unexport `Config`/`TeamsUserStore`/`TeamsChatStore` (`main.go:24`, `store.go:15,29`).

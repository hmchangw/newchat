apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../configmap.yaml
  - ../service.yaml
  - ../seed-job.yaml
  - ../soak-job.yaml
  - ../teardown-job.yaml

# Validation-only aggregate. Do not apply this Kustomization: it intentionally
# contains all mutually exclusive phase Jobs.
